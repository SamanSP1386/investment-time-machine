# DEVLOG.md

Permanent engineering journal. One entry is appended per completed milestone. **Never modify a previous entry — only append.** See [.claude/DOCUMENTATION_POLICY.md](../.claude/DOCUMENTATION_POLICY.md) for the policy governing this file.

---

## 2026-07-02 — M0: Repository & Environment Foundation

**Version**: 0.1.0

**Objective**: Establish a working, disciplined repository foundation — local dev loop, migrations tooling, and CI — that every later milestone builds on, without any product logic yet.

**Scope**: Backend FastAPI skeleton, PostgreSQL via Docker Compose, Alembic initialized (no models), `/health` endpoint, pytest smoke test, ruff/black/pre-commit, GitHub Actions CI with gitleaks secret scanning, `.env.example`, `.gitignore`, setup guide. Explicitly excluded from scope: database models, ingestion, simulation logic, auth, frontend logic, Redis.

**Implementation Summary**: Built a minimal FastAPI app (`app/main.py`) backed by a `pydantic-settings`-based Core Configuration Layer (`app/core/config.py`, `app/core/logging.py`) that centralizes all environment-variable reads and startup logging — no module reads `os.environ` directly outside this layer, and FastAPI `Depends` is the intended DI mechanism going forward. Alembic was initialized pointed at `DATABASE_URL` via `Settings`, with `target_metadata = None` as an explicit placeholder until the Database Schema milestone introduces models. Docker Compose defines Postgres and the backend only — Redis was deliberately left out (see ADR-004 in `ARCHITECTURE_DECISIONS.md`). CI runs ruff, black --check, pytest, and a gitleaks secret scan on every push/PR; pre-commit mirrors the same checks locally.

**Files Created**: `backend/app/main.py`, `backend/app/core/config.py`, `backend/app/core/logging.py`, `backend/app/__init__.py`, `backend/app/core/__init__.py`, `backend/tests/test_health.py`, `backend/tests/__init__.py`, `backend/pyproject.toml`, `backend/requirements.txt`, `backend/requirements-dev.txt`, `backend/Dockerfile`, `backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/script.py.mako`, `backend/alembic/versions/.gitkeep`, `docker-compose.yml`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml`, `.env.example`, `.gitignore`, `docs/setup_guide.md`.

**Files Modified**: `.claude/SYSTEM.md` (Redis wording), `.claude/CODING_STANDARDS.md` (tooling + Core Configuration Layer section), `.claude/GIT_WORKFLOW.md` (Conventional Commits), `.claude/MVP_RULES.md` (Financial Analytics milestone split).

**Architecture Decisions**: See `ARCHITECTURE_DECISIONS.md` ADR-001 through ADR-007 (deploy trigger resolution, `calculation_version` non-negotiable, nullable output columns, Redis deferral, Conventional Commits adoption, Auth-before-Frontend reordering, Financial Analytics milestone split). ADR-004 and ADR-005 were made during this milestone; ADR-001, 002, 003, 006, 007 were made during the preceding architecture-review pass and are logged here retroactively per the new documentation policy.

**Problems Encountered**: (1) `ruff` flagged an unsorted import block in `alembic/env.py`. (2) No Docker daemon available in the working sandbox, so `docker compose up` could not be executed end-to-end in this session.

**Solutions**: (1) Fixed via `ruff check --fix .`, re-verified clean. (2) Backend verified independently instead — dependencies installed into a local venv, `ruff check .`, `black --check .`, and `pytest -v` all run and passed directly against the FastAPI app; `docker-compose.yml` and `Dockerfile` follow a standard, widely-used pattern but a real `docker compose up --build` pass is still owed before M1 begins (tracked as KI-001).

**Lessons Learned**: Wiring CI + pre-commit + secret scanning at commit #1, rather than deferring them as "future workflow expansion" (as the Founder Specification itself allows), costs almost nothing and closes the "Documentation Drift" / "Secret Exposure" threat-model gaps before any real secret or feature exists to leak.

**Security Review Summary**: See `SECURITY_LOG.md` M0 entry. No secrets committed; gitleaks active in CI and pre-commit; all configuration flows through `Settings`.

**Testing Summary**: See `TESTING_REPORT.md` M0 entry. 1 unit test (`test_health_check_returns_success`), 1/1 passing, 100% coverage of the only endpoint that exists.

**Technical Debt Introduced**: `alembic/env.py` has `target_metadata = None` — documented, intentional, temporary placeholder until the Database Schema milestone. No other debt introduced.

**Performance Notes**: See `PERFORMANCE_LOG.md` M0 entry. No meaningful performance surface yet (single health-check endpoint, no DB queries).

**Recruiter Value**: Medium. Demonstrates Docker/CI/tooling discipline from the first commit — the foundation that makes later, higher-value milestones (Simulation Engine, Data Ingestion) credible rather than ad hoc. Not itself a differentiator.

**Production Readiness Score**: 2/10 — appropriate for a foundation milestone with zero product logic. Blocking items before this number moves: real database schema (M1), and a verified (not just authored) Docker Compose run (KI-001).

**Next Milestone**: M1 — Database Schema & Migrations.

---

## 2026-07-05 — M1: Database Schema & Migrations

**Version**: 0.2.0

**Objective**: Implement the nine Founder Specification database domains as SQLAlchemy 2.0 models and a corresponding Alembic migration, with the three founder-approved fixes (nullable simulation/AI-explanation outputs, `calculation_version` from migration 1, conservative Economic Indicators design) applied, plus derived ERD documentation.

**Scope**: SQLAlchemy models, one initial Alembic migration, PostgreSQL native enums, UUID PKs, UTC timestamps, relationships, constraints, indexes, tests, ERD, and this documentation update. Explicitly excluded: ingestion, API endpoints, simulation logic, authentication, frontend logic.

**Implementation Summary**: Built `backend/app/models/base.py` with a shared `Base` (naming-convention-enforced `MetaData`), `UUIDPrimaryKeyMixin`, `TimestampMixin`, and a `pg_enum()` helper. Defined five native Postgres enums (`app/models/enums.py`) and ten tables across nine domains (`assets`, `historical_prices`, `dividends`, `stock_splits`, `economic_indicators` + `economic_indicator_values`, `users`, `simulations`, `audit_logs`, `ai_explanations`). Applied all three approved fixes directly in the models rather than as follow-up patches. Since no Docker/live Postgres was expected to be available (per M0's KI-001), the initial migration's DDL was generated mechanically from the models via SQLAlchemy's offline DDL compiler (`create_mock_engine`) rather than hand-retyped, to guarantee zero drift — this turned out to matter less than expected, because a live Postgres 16 instance (matching the M0 `docker-compose.yml` credentials exactly) was unexpectedly reachable in this session, so the migration was additionally verified end-to-end: applied, diffed against the models (zero drift), downgraded cleanly, and re-applied to leave the dev database at `head`.

**Files Created**: `backend/app/models/__init__.py`, `base.py`, `enums.py`, `asset.py`, `historical_price.py`, `dividend.py`, `stock_split.py`, `economic_indicator.py`, `user.py`, `simulation.py`, `audit_log.py`, `ai_explanation.py`; `backend/alembic/versions/0001_initial_schema.py`; `backend/tests/test_models.py` (25 tests), `backend/tests/test_migrations.py` (2 tests); `docs/erd.md`.

**Files Modified**: `backend/alembic/env.py` (`target_metadata = Base.metadata`, replacing the M0 placeholder), `backend/pyproject.toml` (registered `integration` pytest marker), `.github/workflows/ci.yml` (added a Postgres service + `alembic upgrade head` step), `docs/setup_guide.md` (migration commands, test/dev-DB interaction note), `.claude/DATABASE_RULES.md` (updated from "gaps to fill" to "as implemented"), `docs/KNOWN_ISSUES.md` (KI-001 updated, KI-004/KI-005 updated, KI-008/KI-009 added), `docs/ARCHITECTURE_DECISIONS.md` (ADR-008, ADR-009 added).

**Architecture Decisions**: ADR-008 (Economic Indicators as a separate catalog + time-series pair, not folded into `assets`) and ADR-009 (`audit_logs.user_id` uses `ON DELETE SET NULL`) — both in `docs/ARCHITECTURE_DECISIONS.md`. ADR-002 and ADR-003 (from the M0 entry) were implemented, not just planned, in this milestone.

**Problems Encountered**: (1) SQLAlchemy's `Enum(PyEnum)` defaults to storing the Python enum member **name** (`"STOCK"`) rather than the `.value` (`"stock"`) I'd defined — would have silently diverged from every lowercase value documented in `.claude/DATABASE_RULES.md` and the Founder Specification. (2) `historical_prices` initially carried a standalone `asset_id` index in addition to a composite `(asset_id, price_date)` index — redundant, since the composite's leading column already serves asset-only lookups. (3) Minor ruff/black issues (line length, one unused test variable) on first pass.

**Solutions**: (1) Added a `pg_enum()` helper (`app/models/base.py`) with an explicit `values_callable` forcing `.value` usage — caught by inspecting the generated DDL directly before it ever reached a migration file, specifically because a live Postgres was reachable and I dumped real DDL to check it. (2) Dropped the redundant index, documented the reasoning inline. (3) `ruff --fix` / `black .`, re-verified clean.

**Lessons Learned**: Generating migration DDL mechanically from the models (via a mock engine) rather than hand-typing it eliminates an entire class of drift bugs, and is worth doing even when a live database *might* be available — it removes the question entirely rather than trusting careful manual transcription. Separately: always dump and read the actual generated DDL for enum/default-heavy schemas before trusting an ORM's defaults — the enum name-vs-value behavior is a well-known but easy-to-miss SQLAlchemy gotcha that would have shipped silently wrong otherwise.

**Security Review Summary**: See `SECURITY_LOG.md` M1 entry. No new exploitable risk (no code reads/writes this schema yet); structural risks noted for carry-forward: `audit_logs.ip_address` PII retention/redaction is an application-layer responsibility, `users.password_hash` nullability requires future application-level invariant enforcement.

**Testing Summary**: See `TESTING_REPORT.md` M1 entry. 27 tests total (1 from M0 + 25 metadata-only model tests + 2 DB-integration tests), all passing. The DB-integration tests ran for real against a live Postgres 16 instance in this session (not skipped) and confirmed zero schema drift.

**Technical Debt Introduced**: None judged unintentional. Two items are explicitly flagged pending founder review rather than treated as settled: the Economic Indicators two-table design (ADR-008, KI-005) and the derived ERD (KI-004). One new minor operational note: running `pytest` locally against the same database as `docker compose`'s dev Postgres will downgrade it — documented in `docs/setup_guide.md` and KI-009, not treated as a blocking issue.

**Performance Notes**: See `PERFORMANCE_LOG.md` M1 entry. No representative query workload exists yet; one proactive indexing decision made (dropped redundant `historical_prices.asset_id`-only index) given this table's ~50M-row projection.

**Recruiter Value**: High. This milestone is a strong, concrete artifact: a financial-domain schema with deliberate NUMERIC precision, native enum handling (including catching a real ORM footgun), a documented FK-less exception, delete-behavior reasoning (ON DELETE SET NULL for audit integrity), and a migration verified against a live database with an automated zero-drift check — the kind of detail that reads well in a technical interview.

**Production Readiness Score**: 4/10 — the schema itself is solid and verified, but nothing yet reads or writes through it in a real workflow (no ingestion, no API, no auth), and two design choices (Economic Indicators shape, ERD) remain pending founder sign-off. Blocking items before this number moves: Data Ingestion (M2) actually exercising this schema at volume, and founder review of KI-004/KI-005.

**Next Milestone**: M2 — Historical Data Ingestion Pipeline.

---

## 2026-07-06 — Repository Hygiene Pass (not a feature milestone)

**Version**: 0.2.1

**Objective**: Improve repository consistency and developer experience ahead of M2 — no application logic, no architecture change. Standardize line endings across Windows/Linux/Docker/CI, add editor defaults, and clean up structural issues found along the way.

**Scope**: `.gitattributes`, `.editorconfig`, `.gitignore` review, pre-commit hygiene hooks, a stray nested git repository, and documentation of the LF-standardization rationale. Explicitly excluded: any change to application behavior, database schema, or architecture.

**Implementation Summary**: Added `.gitattributes` forcing LF for `.py`/`.md`/`.yml`/`.yaml`/`Dockerfile`/`.sh`, CRLF for `.bat`/`.ps1` (neither of which exist in the repo yet, but the rule is now in place for when they do), and explicit `binary` declarations for `.pdf`/`.docx`/common image formats. Added `.editorconfig` mirroring the same line-ending rules plus UTF-8, 4-space indentation, required final newline, and trailing-whitespace trimming (except Markdown, where trailing double-spaces are an intentional line-break marker). Ran `git add --renormalize .` to confirm the existing tree was already fully LF-consistent (it was — no files changed). Extended `.gitignore` with a few generically useful entries (`.mypy_cache/`, `.coverage.*`, `*.log`, `*.bak`, `*.orig`, `*.rej`) not currently exercised by the toolchain but harmless and standard. Added five hygiene hooks from `pre-commit/pre-commit-hooks` (trailing-whitespace, end-of-file-fixer, mixed-line-ending, check-merge-conflict, check-added-large-files, check-yaml, check-toml) so the new `.editorconfig`/`.gitattributes` rules are enforced automatically, not just documented. While reviewing directory structure, found and removed an empty, commit-less, remote-less nested `.git` directory inside `backend/` — almost certainly an accidental `git init` from earlier tooling, verified to contain zero history before deletion.

**Files Created**: `.gitattributes`, `.editorconfig`.

**Files Modified**: `.gitignore` (additional ignore patterns), `.pre-commit-config.yaml` (hygiene hooks added), `docs/ARCHITECTURE_DECISIONS.md` (ADR-010), `docs/KNOWN_ISSUES.md` (KI-010).

**Files Removed**: `backend/.git/` (stray nested repository — zero commits, zero refs, zero remotes; confirmed empty before removal).

**Architecture Decisions**: ADR-010 — Standardize on LF line endings, CRLF only for Windows-native scripts (`docs/ARCHITECTURE_DECISIONS.md`). Not an architecture change in the application sense; recorded as an ADR because it's a deliberate, project-wide, hard-to-reverse-cheaply convention.

**Problems Encountered**: (1) A stray nested `.git` directory inside `backend/` was discovered during directory-consistency review — not caused by this pass, but surfaced by it. (2) None of the line-ending rules actually changed any file content, since the repository was already LF-consistent — the risk the ADR addresses was latent, not yet manifested.

**Solutions**: (1) Verified the nested repo had no commits (`git log` inside it: "does not have any commits yet"), no refs, and no configured remote, before deleting it — nothing of value was at risk. (2) `git add --renormalize .` confirmed zero content changes; the new `.gitattributes`/`.editorconfig` are preventative, not corrective.

**Lessons Learned**: A hygiene pass is a good moment to check for structural artifacts (nested `.git` folders, accidentally-tracked build output) that don't show up in day-to-day feature work — nobody notices a stray nested repo until it causes a confusing `git add` warning or submodule-like behavior down the line.

**Security Review Summary**: No security surface touched. `check-added-large-files` (new pre-commit hook) incidentally guards against accidentally committing an oversized binary (e.g. a database dump) in the future.

**Testing Summary**: No new tests (none applicable to configuration files). Full existing suite re-run to confirm no regression: `ruff check .`, `black --check .`, `pytest -v` — 27/27 passed, unchanged from the M1 baseline. `.pre-commit-config.yaml` validated as well-formed YAML.

**Technical Debt Introduced**: None. One debt-adjacent note recorded (KI-010): `.gitattributes` only covers file types that exist today; future milestones must extend it deliberately rather than relying on the catch-all rule indefinitely.

**Performance Notes**: Not applicable.

**Recruiter Value**: Low-to-Medium on its own, but signals exactly the kind of engineering discipline (cross-platform consistency, catching a stray nested repo, enforcing conventions via tooling rather than tribal knowledge) that reads well alongside the higher-value milestones.

**Production Readiness Score**: Not applicable to a hygiene pass — does not change the M1 score (4/10).

**Next Milestone**: M2 — Historical Data Ingestion Pipeline.

---

## 2026-07-07 — CI Reliability Fix: Stable Secret Scanning (not a feature milestone)

**Version**: 0.2.2

**Objective**: Fix a GitHub Actions failure in the `secret-scan` job — `gitleaks/gitleaks-action@v2` was failing intermittently on an ambiguous commit range after a merge, despite reporting "no leaks found." No secret leak was ever involved; this is a CI reliability fix only.

**Scope**: `.github/workflows/ci.yml` `secret-scan` job only. `lint-and-test` job left unchanged. No application logic touched.

**Implementation Summary**: The wrapper action `gitleaks/gitleaks-action@v2` infers a commit range from the GitHub event's push/PR refs to scan only the diff — that inference fails once local/remote history is merged (a merge commit or an unrelated-history pull leaves no single unambiguous range), and the action exits non-zero even when the underlying scan found nothing. Replaced it with a direct `gitleaks detect --source . --redact --verbose` CLI call: `detect` scans the full git history of the checked-out repository unconditionally, so there is no ref-to-ref range to resolve and this failure mode cannot occur. Installed gitleaks by downloading the pinned v8.18.4 Linux release binary directly (matching the version already pinned in `.pre-commit-config.yaml`, so local pre-commit scans and CI scans are now on the same version). No SARIF upload existed before this fix and none was added, per the "keep tests/lint/format unchanged, CI-reliability-only" scope.

**Files Modified**: `.github/workflows/ci.yml` (secret-scan job only), `docs/KNOWN_ISSUES.md` (KI-011, logged and resolved in the same entry).

**Architecture Decisions**: None — this is a CI implementation detail, not a design decision warranting an ADR.

**Problems Encountered**: The prior CI approach (a maintained wrapper action) offered less control over exactly how the commit range was computed, and no documented flag was reachable to force a full-repository scan instead of an event-based range within that action.

**Solutions**: Bypassed the wrapper entirely in favor of the underlying `gitleaks` CLI, which has always supported unconditional full-history scanning via `detect --source .` — verified the exact release download URL, archive contents, and extraction command locally before committing to this approach (the Linux binary itself couldn't be executed in this Windows-based session, but the download/extract steps were confirmed to produce a valid Linux ELF executable matching what the `ubuntu-latest` runner needs).

**Lessons Learned**: Wrapper GitHub Actions that infer scan scope from event context are convenient in the common case but introduce a failure mode (ambiguous ref resolution) that a direct CLI invocation with an explicit, unconditional scope avoids entirely. When a CI tool's automatic behavior is the source of flakiness, replacing "automatic" with "explicit and unconditional" is usually the more stable fix, even at the cost of a slightly longer workflow step.

**Security Review Summary**: Secret-scanning coverage is unchanged or arguably stronger — the new command scans full git history every run rather than depending on a correctly-resolved event diff range. No secrets were exposed by the original bug; it was a false-positive CI failure, not a missed scan.

**Testing Summary**: `lint-and-test` job unaffected (not modified). Locally re-ran `ruff check .`, `black --check .`, `pytest -v` (27/27 passed) to confirm the CI-only change had zero effect on the application test suite, as expected. CI YAML re-validated as well-formed; the gitleaks download URL, archive structure, and extraction command were verified locally against the exact pinned version and platform CI will use.

**Technical Debt Introduced**: None.

**Performance Notes**: Not applicable — CI-step change only, no measurable effect on application performance. The new step adds a small, one-time binary download to the `secret-scan` job's runtime.

**Recruiter Value**: Low-to-Medium on its own, but demonstrates the ability to diagnose a CI tool's actual failure mechanism (not just retry-and-hope) and fix it at the right layer.

**Production Readiness Score**: Not applicable — CI-only fix, does not change the M1 score.

**Next Milestone**: M2 — Historical Data Ingestion Pipeline.

---

## 2026-07-07 — M2: Historical Data Ingestion Pipeline

**Version**: 0.3.0

**Objective**: Build the Historical Data Ingestion Pipeline — Retrieve → Validate → Normalize → Store → Audit — for yfinance (stocks/ETFs), CoinGecko (crypto), and FRED (economic indicators), with dry-run support, structured Import Reports, and explicit error handling. No simulation logic, no API endpoints, no auth, no frontend.

**Scope**: Five independent layers (Provider, Validation, Normalization, Storage, Audit), an orchestrator tying them together, a CLI entrypoint, and the Core Configuration Layer addition needed to support it (`app/core/database.py`). Full test suite across all layers. Explicitly excluded: simulation calculations, API endpoints, business logic beyond ingestion, authentication, frontend, AI.

**Implementation Summary**: Provider Layer adapters (`app/ingestion/providers/`) communicate only — no validation, no DB access — using capability protocols (`PriceProvider`, `DividendProvider`, `SplitProvider`, `IndicatorProvider`, ADR-011) rather than one forced-uniform interface, since CoinGecko has no dividends/splits and FRED has no prices at all. yfinance is wrapped via a single isolated `_fetch_history` method (patchable in tests without touching yfinance internals); CoinGecko and FRED use injectable `httpx.Client` instances (`httpx.MockTransport` in tests — zero live network calls anywhere in the suite). CoinGecko's free-tier date-range endpoint only returns one price point per day, not true OHLC — rather than fabricate High/Low, the adapter sets O=H=L=C and the orchestrator attaches an explicit disclosure warning to every affected report (ADR-012). Validation (`app/ingestion/validation/rules.py`) returns rejection reasons instead of raising, so one bad record never aborts an entire import; malformed values are coerced-and-checked via `Decimal(str(value))`, never silently zeroed (FRED's "." missing-value marker is preserved as `None`, not 0). Normalization converts to platform-standard, Decimal-typed dicts matching the M1 schema exactly. Storage (`IngestionRepository`) resolves/creates `Asset`/`EconomicIndicator` rows and performs idempotent `INSERT ... ON CONFLICT DO NOTHING ... RETURNING` upserts per table, each wrapped in its own SAVEPOINT (ADR-013) so a genuine constraint violation can't silently discard rows already upserted earlier in the same batch. Audit (`app/ingestion/audit/`) writes exactly one `audit_logs` row per real (non-dry-run) import attempt — success or failure — with the full Import Report embedded in `details`; dry runs write none, since persisting an audit row would itself violate "dry run must not modify the database" (ADR-014). The orchestrator (`app/ingestion/orchestrator.py`) wires all five layers together for `import_asset_prices`/`import_asset_dividends`/`import_asset_splits`/`import_economic_indicator`, plus an `import_asset` convenience wrapper that includes dividends/splits only for capability-checked providers. A thin `argparse` CLI (`app/ingestion/cli.py`) makes the pipeline runnable today without inventing an API endpoint or scheduler.

**Files Created**: `backend/app/core/database.py`; `backend/app/ingestion/__init__.py`, `exceptions.py`, `orchestrator.py`, `cli.py`; `backend/app/ingestion/providers/{__init__,base,yfinance_provider,coingecko_provider,fred_provider}.py`; `backend/app/ingestion/validation/{__init__,rules}.py`; `backend/app/ingestion/normalization/{__init__,normalizers}.py`; `backend/app/ingestion/storage/{__init__,repository}.py`; `backend/app/ingestion/audit/{__init__,recorder}.py`; `backend/app/ingestion/reports/{__init__,import_report}.py`; `backend/tests/ingestion/` — `conftest.py`, `test_providers_{yfinance,coingecko,fred}.py`, `test_validation.py`, `test_normalization.py`, `test_import_report.py`, `test_storage.py`, `test_audit.py`, `test_orchestrator.py` (63 new tests total); `docs/MILESTONE_REPORTS/M2_REPORT.md`.

**Files Modified**: `backend/app/core/config.py` (`fred_api_key`, `ingestion_http_timeout_seconds`), `.env.example`, `backend/requirements.txt` (`yfinance`, `httpx`, `requests`), `backend/requirements-dev.txt` (removed now-redundant `httpx` line), `docs/ARCHITECTURE_DECISIONS.md` (ADR-011–014), `docs/KNOWN_ISSUES.md` (KI-012–015).

**Architecture Decisions**: ADR-011 (provider capability protocols over one uniform interface), ADR-012 (CoinGecko OHLC approximation disclosed, not fabricated), ADR-013 (per-record SAVEPOINT in storage upserts), ADR-014 (single audit row per real import, none for dry runs) — all in `docs/ARCHITECTURE_DECISIONS.md`.

**Problems Encountered**: (1) A naive Storage Layer implementation would have let one genuine constraint violation mid-batch silently discard every row already upserted in the same transaction (Postgres aborts the whole transaction on the first unhandled statement error). (2) CoinGecko's only arbitrary-date-range endpoint doesn't provide true OHLC, only a single daily price point — discovered while researching the API, before any code was written against a wrong assumption. (3) Running the full test suite (which includes M1's `test_migrations.py` upgrade/downgrade cycle) left the dev database schema-less, causing 13 otherwise-passing ingestion tests to fail with "relation does not exist" on a subsequent standalone run.

**Solutions**: (1) Wrapped each upsert in a SAVEPOINT (`session.begin_nested()`) so a failure rolls back only that one insert, not the whole import — directly verified by a dedicated test. (2) Designed the CoinGecko adapter around the honest constraint from the start (O=H=L=C, explicit disclosure) rather than discovering the limitation after building something that assumed real OHLC was available. (3) Re-ran `alembic upgrade head` — the same known, already-documented M1 behavior (KI-009), not a new defect.

**Lessons Learned**: Designing dry-run support and the Import Report *together* with the real-execution path (rather than bolting dry-run on afterward as a special case) meant the two code paths share almost all logic, differing only at the exact point of a database write — this structural approach is what makes the dry-run/real-run parity guarantees checkable by tests rather than just asserted by comment. Separately: researching a third-party API's actual constraints (CoinGecko's OHLC limitation) before writing the adapter avoided building something that would have silently fabricated data — worth the extra research time up front.

**Security Review Summary**: See `docs/SECURITY_LOG.md` M2 entry. No provider adapter has database access (structural, not just conventional); all writes are parameterized (no raw SQL); `FRED_API_KEY` sourced from environment only, never logged; idempotent upserts + per-record SAVEPOINTs mean a malformed record can be rejected but never corrupt or discard prior legitimate work.

**Testing Summary**: See `docs/TESTING_REPORT.md` M2 entry. 63 new tests (47 non-DB, mocked/isolated; 14 DB-integration, transaction-isolated and rolled back). Full suite: 93/93 passing. Every required error-handling category has dedicated coverage.

**Technical Debt Introduced**: Documented and tracked, not silent — KI-012 (TOCTOU race in asset/indicator get-or-create, acceptable for single-process MVP ingestion), KI-013 (CoinGecko OHLC fidelity limitation), KI-014 (no ticker-to-CoinGecko-id mapping), KI-015 (no retry/backoff or rate-limit awareness — a scheduler-milestone concern, not this one's).

**Performance Notes**: See `docs/PERFORMANCE_LOG.md` M2 entry. No representative production-scale import has been run; in-memory (non-streaming) record handling is a known, documented scale limitation (KI-015-adjacent), not yet a problem at MVP asset-catalog volumes.

**Recruiter Value**: High. This is a genuine data-engineering artifact: multi-provider abstraction via capability protocols (not a single leaky interface), idempotent storage design with a real, subtle correctness bug (batch-wide rollback) caught and fixed via SAVEPOINT, an honest engineering tradeoff disclosed rather than papered over (CoinGecko OHLC), and dry-run support that is structurally guaranteed rather than a bolted-on flag.

**Production Readiness Score**: 5/10 — the pipeline mechanism is solid, tested, and idempotent, but has never ingested a real multi-year dataset, has no scheduler/automation, no retry/backoff, and one real data-fidelity compromise (CoinGecko OHLC) that a future Financial Analytics milestone must account for. Blocking items before this number moves: a real backfill run against production-scale date ranges, and resolution of KI-012 before any concurrent ingestion path is introduced.

**Next Milestone**: M3 — Simulation Engine.

---

## 2026-07-08 — M3 Design Review & Founder Decision 001 (not a coding milestone)

**Version**: 0.3.1

**Objective**: Before implementing the Simulation Engine, re-validate the proposed calculation model directly against the Founder Specification (not memory), formalize the founder's decision to use `close_price` instead of the specification's own stated `adjusted_close_price` recommendation, and complete a design review (weaknesses, edge cases, risks, testing challenges) before any code was written.

**Scope**: Documentation only — `docs/simulation_formulas.md` (rewritten to APPROVED status), `docs/FOUNDER_DECISIONS.md` (new, Founder Decision 001), ADR-015, `docs/KNOWN_ISSUES.md` (KI-016 through KI-018 added), `.claude/DATABASE_RULES.md` updated. No application code.

**Implementation Summary**: Re-read Part 2.14 (Simulation Engine Architecture, full), Parts 2.6.7/2.6.20/2.6.21/2.6.22/2.6.24 (Database Architecture sections), Parts 3.3.2–3.3.4 (Functional Requirements), and Part 3.5 (Financial Analytics) directly from the extracted specification text. Discovered that the specification's own text recommends `adjusted_close_price` as the MVP default in three separate places (2.6.7, 2.6.20, 2.6.22) — a genuine, repeated deviation the founder's decision explicitly overrides. Recorded as Founder Decision 001, with the full citation trail, in `docs/FOUNDER_DECISIONS.md`; the engineering implementation rationale recorded separately as ADR-015. The design review surfaced three concrete open items, tracked as KI-016 (the split-consistency assumption underlying `close_price`-based calculation is empirically unverified — no network access was available), KI-017 (no trading-day resolution policy), and KI-018 (precision/rounding convention specified but not yet implemented or tested).

**Files Created**: `docs/FOUNDER_DECISIONS.md`.

**Files Modified**: `docs/simulation_formulas.md` (status DRAFT → APPROVED, every formula cross-referenced to its exact spec citation), `docs/ARCHITECTURE_DECISIONS.md` (ADR-015), `docs/KNOWN_ISSUES.md` (KI-008 resolved, KI-016/017/018 added), `.claude/DATABASE_RULES.md` (close_price vs. adjusted_close_price question marked resolved).

**Architecture Decisions**: ADR-015 (`docs/ARCHITECTURE_DECISIONS.md`) — engineering rationale for building the Simulation Engine around `close_price` rather than `adjusted_close_price`, implementing Founder Decision 001.

**Problems Encountered**: None — this was a research and documentation pass, not implementation.

**Solutions**: N/A.

**Lessons Learned**: Re-reading the actual specification text (rather than relying on the summarized understanding carried over from the M0 planning pass) surfaced a real, three-times-repeated deviation that a memory-based review would very likely have missed or under-weighted. "Always consult the source, never rely on memory" earned its keep here.

**Security Review Summary**: Not applicable — no code changed.

**Testing Summary**: Not applicable — no code changed.

**Technical Debt Introduced**: None — KI-016/017/018 are pre-existing design gaps made explicit, not new debt created by this pass.

**Performance Notes**: Not applicable.

**Recruiter Value**: Medium — demonstrates the discipline of re-validating a design against its source of truth before writing code, and formally recording a founder-level specification override with full traceability (a real-world pattern: specs get amended, and the amendment itself needs to be as well-documented as the original).

**Production Readiness Score**: Not applicable — documentation-only pass.

**Next Milestone**: M3 — Simulation Engine (implementation).

---

## 2026-07-09 — M3: Simulation Engine (Implementation)

**Version**: 0.4.0

**Objective**: Implement the Simulation Engine per the approved design (Founder Decision 001, ADR-015, `docs/simulation_formulas.md`): `close_price` as the sole calculation input, explicit one-time dividend processing, `stock_splits` disclosure-only, `Decimal` precision with `prec=38`/`ROUND_HALF_EVEN`, exact-date historical data requirements, controlled error handling, determinism tests, known-answer tests, and a documented verification path for KI-016.

**Scope**: `backend/app/simulation/` (exceptions, precision, formulas, repository, engine) and `backend/tests/simulation/` (36 tests). No API endpoints, no frontend, no portfolio simulation, no advanced analytics beyond the six MVP-required calculations.

**Implementation Summary**: Built five focused modules. `exceptions.py` implements the exact error taxonomy from Founder Specification 2.14.14 plus the 3.3.2 investment-amount/date-range split. `precision.py` provides a scoped `decimal.localcontext()` (never mutating the global context) and explicit `ROUND_HALF_EVEN` quantization helpers matching the `NUMERIC(20,8)`/`NUMERIC(10,6)` column scales. `formulas.py` holds every calculation as a pure, DB-free function — shares purchased, final value, total return %, CAGR (with an independent `math`-module cross-check in tests), inflation adjustment, and the dividend-reinvestment loop (price lookup injected as a callable, keeping the function testable without a database). `repository.py` is read-only, deliberately separate from the M2 `IngestionRepository`, with exact-date price lookups (never nearest-trading-day), ordered dividend/split retrieval, and as-of (never interpolated) CPI lookups. `engine.py`'s `run_simulation` is the sole entry point, implementing a deliberate error-handling asymmetry discovered while writing it: pre-flight validation failures (asset not found, invalid date range, invalid investment amount) never persist a `Simulation` row, because there is no valid `asset_id` to reference and the Founder Specification's own error table classifies these as "Validation error"; mid-simulation failures (missing historical data) persist a failed `Simulation` row with a descriptive `error_message`, per Founder Specification 2.6.24's explicit instruction to store failed simulations for debugging.

One design correction was made during test-writing, before it became a bug: `docs/simulation_formulas.md`'s original draft described dividends being "collected as uninvested cash" when `dividends_reinvested = false`. Re-reading Founder Specification 2.14.10/3.3.3 while writing the corresponding test showed the specification describes only two modes — reinvest, or ignore entirely — with no middle ground. Corrected in both the code and the design note before any test asserted the invented behavior.

**Files Created**: `backend/app/simulation/__init__.py`, `exceptions.py`, `precision.py`, `formulas.py`, `repository.py`, `engine.py`; `backend/tests/simulation/__init__.py`, `conftest.py`, `test_formulas.py`, `test_precision.py`, `test_engine_known_answer.py`, `test_engine_errors.py`, `test_engine_determinism.py`, `test_split_disclosure.py`; `docs/MILESTONE_REPORTS/M3_REPORT.md`.

**Files Modified**: `docs/simulation_formulas.md` (dividends-disabled correction, implementation status), `docs/KNOWN_ISSUES.md` (KI-016 updated with a concrete manual verification runbook, KI-017/018 resolved, KI-019/020 added).

**Architecture Decisions**: No new ADR — this milestone implements ADR-015 and Founder Decision 001 exactly as designed, with one documented correction (dividends-disabled behavior) rather than a new architectural decision.

**Problems Encountered**: (1) Postgres was unreachable at the start of this session — Docker Desktop's engine was not running (unlike prior sessions where it happened to already be up). (2) The dividends-disabled design imprecision described above. (3) The known M1/M2 full-suite-run behavior (KI-009: `test_migrations.py`'s upgrade/downgrade cycle leaves the dev database schema-less) recurred twice during this session's verification passes.

**Solutions**: (1) Started Docker Desktop directly (`Start-Process`) and brought up the `postgres` service via `docker compose up -d postgres`, confirmed healthy, then ran all 36 new tests for real against a live database rather than relying solely on the (still fully passing) DB-independent pure-math suite. (2) Corrected before any test encoded the wrong behavior, per the Lessons Learned above. (3) Re-ran `alembic upgrade head` each time; DB left at `head` with zero residual rows (transaction-rollback isolation confirmed) at the end of the session.

**Lessons Learned**: The pure-math formula tests (no DB required) and the DB-integration engine tests caught the same categories of correctness by design, but for genuinely different reasons — the pure tests are fast and always runnable regardless of environment state; the DB-integration tests are what actually prove the full stack (repository queries, precision context, persistence, error-path branching) works together. Neither layer alone would have been sufficient. Separately: attempting to bring up the actual database environment (rather than reporting "DB unavailable" and stopping) paid off directly — it upgraded the known-answer verification from "formulas are correct in isolation" to "the whole engine is correct end-to-end against a real Postgres instance."

**Security Review Summary**: See `docs/SECURITY_LOG.md` M3 entry. No new attack surface; `adjusted_close_price`-never-read is verified by a dedicated test, not just documented; all queries parameterized.

**Testing Summary**: See `docs/TESTING_REPORT.md` M3 entry. 36 new tests (18 non-DB, 18 DB-integration), all passing against a live Postgres instance. Full project suite: 129/129 passing.

**Technical Debt Introduced**: None new. KI-016 (split-consistency empirical verification) remains the one substantive open item, now with a concrete, actionable manual runbook rather than a vague "should verify eventually" note. KI-020 (Dividend Contribution metric) explicitly deferred, matching this milestone's scope boundary.

**Performance Notes**: See `docs/PERFORMANCE_LOG.md` M3 entry. Query shape (bounded, indexed point/range lookups) is designed to meet the <2s target by construction; not yet benchmarked against production-scale data.

**Recruiter Value**: Very High. This is the platform's centerpiece: a deterministic financial calculation engine with formulas cited line-by-line to a founder specification, known-answer tests reproducing the specification's own worked examples, a caught-before-shipping design correction (dividends-disabled behavior), an explicit and tested error-handling asymmetry grounded in the spec's own error taxonomy, and a documented, honest boundary between "verified by test" and "verified against live data" (KI-016) — exactly the kind of nuanced, defensible engineering judgment that reads well in a technical interview.

**Production Readiness Score**: 6/10 — the engine is correct against every known-answer test available, fully deterministic, and precisely scoped, but carries one unverified empirical dependency (KI-016) that should be closed before production use, and has no caller yet (no API layer exists until M4). Blocking items before this number moves: the KI-016 live-data verification runbook, and M4 (API Layer) actually exercising this engine end-to-end.

**Next Milestone**: M4 — API Layer.

---

## 2026-07-10 — M4: API Layer

**Version**: 0.5.0

**Objective**: Give the Simulation Engine (M3) its first real caller — a public HTTP API for asset search/lookup and simulation creation/retrieval — following a founder-approved design review with five explicit decisions on auth posture, growth-chart placement, deferred endpoints, field naming, and the asset exchange field.

**Scope**: `GET /api/v1/assets`, `GET /api/v1/assets/{symbol}`, `GET /api/v1/assets/{symbol}/availability`, `POST /api/v1/simulations`, `GET /api/v1/simulations/{id}`; response envelope + exception-handler mapping; Redis-backed rate limiting; a Simulation Engine extension for the Growth Chart output. Explicitly excluded per the founder's instruction: authentication, frontend, AI, portfolio simulation, admin routes, Simulation History (designed in `docs/api_design.md`, not implemented — deferred to M5).

**Implementation Summary**: Extended the Simulation Engine first, before touching the API layer at all — `app/simulation/formulas.py::calculate_growth_series` replays the existing dividend-reinvestment event loop at every stored price date in range (not just the two endpoints), added a `SimulationRepository.get_prices_ordered` method to support it, and threaded the result through `SimulationOutcome.growth_series`. This kept "no financial calculation logic in the API layer" a structural fact rather than a convention. Built the API layer in the standard router → service → engine/repository order: Pydantic schemas first (external field names matching the Founder Specification's own vocabulary, `include_dividends`/`adjust_for_inflation`, mapped internally to the existing `dividends_reinvested`/`inflation_adjusted` at the service boundary), then `asset_service`/`simulation_service`, then thin routers, then a Redis-backed fixed-window `RateLimiter` (fails open on Redis unreachability, logged), then `exception_handlers.py` mapping ten named exception types plus one legitimate catch-all to the standard envelope, then wired all of it into `app/main.py` alongside a request-ID middleware and CORS. The one subtle correctness issue requiring real design work: `run_simulation` flushes (never commits) and, for `MissingHistoricalDataError`/`CalculationError`, persists a failed `Simulation` row before re-raising — so the FastAPI DB-session dependency deliberately does *not* auto-rollback on exception (only `close()`s), and `simulation_service.create_simulation` explicitly commits or rolls back per exception type, preserving Founder Specification Part 2.6.24's "failed simulations should be stored" guarantee. Documented as ADR-016.

**Files Created**: `backend/app/api/__init__.py`, `v1/__init__.py`, `v1/errors.py`, `v1/exception_handlers.py`, `v1/dependencies.py`, `v1/schemas/{common,assets,simulations}.py`, `v1/services/{asset_service,simulation_service}.py`, `v1/routers/{assets,simulations}.py`; `backend/app/core/rate_limit.py`, `request_id.py`; `backend/tests/api/{conftest,test_assets,test_simulations}.py`, `backend/tests/core/{__init__,test_rate_limit}.py`, `backend/tests/simulation/test_growth_series.py`; `docs/MILESTONE_REPORTS/M4_REPORT.md`.

**Files Modified**: `backend/app/main.py` (mounts `/api/v1`, registers exception handlers, adds request-ID + CORS middleware — `/health` unchanged); `backend/app/simulation/exceptions.py` (`SimulationError.simulation_id`), `formulas.py` (`calculate_growth_series`, `PricePoint`/`GrowthSeriesPoint`), `repository.py` (`get_prices_ordered`), `engine.py` (wires `growth_series` into `SimulationOutcome`, sets `exc.simulation_id` on the failed-simulation path); `backend/app/core/config.py`, `.env.example`, `backend/requirements.txt`, `docker-compose.yml` (Redis); `backend/pyproject.toml` (`flake8-bugbear` FastAPI `Depends`/`Query` exemption); `docs/api_design.md`, `docs/KNOWN_ISSUES.md` (KI-021 resolved with a documented remaining gap, KI-022/023/024 resolved per founder decision, KI-025/026 added).

**Architecture Decisions**: ADR-016 (`docs/ARCHITECTURE_DECISIONS.md`) — service-owned transaction boundary (not a framework-default auto-commit/rollback), engine-computed growth series (not route-handler-computed), and SQLAlchemy 2.0 `join_transaction_mode="create_savepoint"` for API-layer DB-integration tests whose code-under-test itself commits/rolls back.

**Problems Encountered**: (1) An early draft of `simulation_service.create_simulation` used a bare `except Exception: session.commit()` "for safety," which would have committed the session on a genuinely unexpected error — caught before finalizing, since it violates this project's "no generic exception handling" rule and risks persisting corrupted state. (2) An early draft of `routers/assets.py` invented a duplicate `AssetNotFoundHTTPError` class instead of reusing `app.simulation.exceptions.AssetNotFoundError`, which already fit exactly — caught and removed before finalizing. (3) Running the API-layer DB-integration tests initially produced a real (non-fatal) `SAWarning: transaction already deassociated from connection` for every test whose request path called `session.commit()`/`session.rollback()`, because the test fixture's outer `connection.begin()` transaction was the same one being ended by the service layer. (4) After a full local `pytest` run (which includes `tests/test_migrations.py`'s upgrade/downgrade cycle), a second full run failed 45 tests with "relation does not exist" / duplicate-key errors — the same known M1/M2/M3 behavior (KI-009), not a new bug. (5) `ruff`'s `flake8-bugbear` rule B008 flagged every FastAPI route using the standard `param: X = Depends(...)` pattern.

**Solutions**: (1) Removed the bare `except Exception`, replaced with a specific `except (MissingHistoricalDataError, CalculationError): session.commit(); raise` — truly unexpected exceptions now propagate with no explicit commit/rollback at this layer, which is safe because `get_db_session` only ever `close()`s. (2) Rewrote `routers/assets.py` to import and reuse the existing `AssetNotFoundError`. (3) Bound API test sessions with `join_transaction_mode="create_savepoint"` (`tests/api/conftest.py`) so a service-layer commit/rollback operates on a nested SAVEPOINT, leaving the fixture's outer transaction intact for its own final rollback. (4) Re-ran `alembic upgrade head` to restore the schema before continuing. (5) Added `[tool.ruff.lint.flake8-bugbear] extend-immutable-calls = ["fastapi.Depends", "fastapi.Query"]` to `pyproject.toml` — the documented, standard way to tell ruff this is FastAPI's DI idiom, not the mutable-default-value bug B008 exists to catch.

**Lessons Learned**: Extending the Simulation Engine with `calculate_growth_series` *before* writing any route or service code (rather than reaching for a "quick" route-handler calculation first) made "no financial calculation logic in the API layer" free to verify by inspection rather than something to remember to check later. Separately: a design document produced during an approved design-review turn (`docs/api_design.md`'s audit-logging requirement) can still drift from what actually gets implemented in the following implementation turn if it isn't re-checked line-by-line against the final code — caught only during this documentation pass, now tracked as KI-026 rather than silently dropped.

**Security Review Summary**: See `SECURITY_LOG.md` M4 entry. Rate limiting is the MVP-appropriate control for the public, unauthenticated simulation-creation endpoint (founder decision, KI-022); every named exception maps to an explicit status/code with no internal detail leaked to the client; no admin or auth-requiring routes implemented or exposed.

**Testing Summary**: See `TESTING_REPORT.md` M4 entry. 26 new tests (155 total across the full project: 129 from M0–M3 + 26 new from M4), all passing against live Postgres and Redis instances.

**Technical Debt Introduced**: KI-026 (audit-logging requirement from `docs/api_design.md` not implemented for simulation creation) is the one genuine gap discovered in this milestone, not a deliberate scope cut. KI-021/023/024 close as founder-approved scope decisions, not debt. KI-025 (asset `exchange` field) remains open, non-blocking, founder-acknowledged.

**Performance Notes**: See `PERFORMANCE_LOG.md` M4 entry. No load testing performed; query shape unchanged from M3's already-bounded design (the growth series adds one ordered range read over `historical_prices`, not a full scan).

**Recruiter Value**: High. This milestone demonstrates full-stack API design discipline: a founder design-review-then-implement workflow, a real (not hypothetical) transaction-boundary bug avoided by design rather than caught in production, Redis-backed rate limiting with an explicit fail-open policy, and an honest post-implementation documentation audit that caught and disclosed its own scope drift (KI-026) rather than only reporting what went right.

**Production Readiness Score**: 6/10 — the API surface is functionally complete for its declared M4 scope, fully tested, and cleanly layered, but carries one undisclosed-until-now implementation gap (KI-026), no load testing, and still depends on KI-016 (M3's unverified split-consistency assumption) being closed before real user-facing use.

**Next Milestone**: M5 — Authentication (unblocks Simulation History and Admin Import endpoint implementation, both already designed in `docs/api_design.md`).

---

## 2026-07-10 — M4 Follow-Up: Simulation Audit Logging (KI-026)

**Version**: 0.5.1

**Objective**: Close KI-026 before M4 merges — `POST /api/v1/simulations` must write an `audit_logs` entry for every request (success, missing historical data, validation failure where possible, and calculation error), without implementing authentication, simulation history, admin routes, or any other M5 work.

**Scope**: One new module (`app/api/v1/audit.py`), wiring into `simulation_service.create_simulation` and the `RequestValidationError` exception handler, and tests proving the audit trail is written for both success and every failure category. Explicitly excluded, per the founder's instruction: authentication, simulation history, admin routes, M5.

**Implementation Summary**: Added `record_simulation_audit` — called from `simulation_service.create_simulation` after every commit/rollback decision already established in M4 (ADR-016), so the audit write never risks the already-durable `Simulation` row for the mid-simulation failure paths. It reuses the existing `AuditEventType.SIMULATION_CREATED` enum value rather than adding a `SIMULATION_FAILED` counterpart — `app/models/enums.py`'s own docstring says to "expand deliberately, not speculatively," and a schema migration (`ALTER TYPE ... ADD VALUE`) was judged a larger, riskier change than this fix warranted; `details.status`/`details.error_code` (JSONB) carry the outcome instead, so every literal field the requirement listed is still present. The one genuinely new problem: Pydantic-level request validation failures (e.g. a non-positive `investment_amount`) never reach `simulation_service.create_simulation` at all — FastAPI rejects the request before the endpoint function runs — so there is no request-scoped DB session available to reuse at that point. Solved with a second, narrowly-scoped function, `record_simulation_request_validation_audit`, called from the `RequestValidationError` handler, opening a short-lived session directly and guarded to only fire for `POST /api/v1/simulations` (not every validated route in the API).

**Files Created**: `backend/app/api/v1/audit.py`, `backend/tests/api/test_simulation_audit.py`.

**Files Modified**: `backend/app/api/v1/services/simulation_service.py` (audit call on every path, new required `request_id` parameter), `backend/app/api/v1/routers/simulations.py` (threads `X-Request-ID` into the service call), `backend/app/api/v1/exception_handlers.py` (`RequestValidationError` handler now audits), `docs/KNOWN_ISSUES.md` (KI-026 resolved).

**Architecture Decisions**: No new ADR — this follows the transaction-boundary and failure-isolation reasoning already established in ADR-016 (SAVEPOINT-isolated writes, fail-open on `SQLAlchemyError`, mirroring the Redis rate-limiter's own fail-open policy).

**Problems Encountered**: (1) Testing the request-validation-failure audit path (the one that opens its own session) meant that path's write is genuinely, permanently committed on a separate DB connection — not covered by the test suite's usual rollback-based isolation, since nothing in that specific code path touches the request-scoped, rollback-isolated test session at all. (2) `session.begin_nested()` (SAVEPOINT) needed to compose correctly with the API test fixtures' own `join_transaction_mode="create_savepoint"` binding (ADR-016) — a savepoint nested inside a savepoint.

**Solutions**: (1) The affected test (`test_audit_log_written_on_request_validation_failure`) explicitly deletes the row it caused via a second, separate session-and-commit in a `finally` block, with a comment explaining why this one test deviates from the suite's normal "everything rolls back" pattern — verified with a direct `psql` row-count check after the full suite ran (`audit_logs`, `assets`, `simulations` all at 0 rows). (2) Confirmed by running the full suite: nested SAVEPOINTs composed correctly with no errors or warnings.

**Lessons Learned**: A design note's audit-logging requirement is easy to defer accidentally when it sits orthogonal to the "main" success-path work (schemas, routers, calculation) — this is the second time in this milestone (after KI-021's growth-chart gap) that treating the Founder Specification/design note as the actual acceptance criteria, not just a starting outline, caught something the test suite alone wouldn't have (there's no way for a test to fail against a requirement nobody wrote a test for). Going forward: when a design note lists a cross-cutting requirement (audit, logging, rate limiting) alongside the main feature, write its test first, not last.

**Security Review Summary**: See `SECURITY_LOG.md`'s updated M4 entry. Every simulation-creation attempt is now audited, including pre-execution validation failures; the audit write cannot become a new availability risk (SAVEPOINT-isolated, fails safe).

**Testing Summary**: See `TESTING_REPORT.md`'s updated M4 entry. 4 new tests (159 total across the full project), all passing against a live Postgres instance; DB confirmed empty of leaked test data after the full suite run.

**Technical Debt Introduced**: None new. KI-026 closes with one disclosed, deliberate deviation from the original design note (no new enum value) rather than a silent one.

**Performance Notes**: One additional small `INSERT` per simulation request (inside a SAVEPOINT, same transaction as the simulation write it's attached to) — not expected to be measurable against `.claude/PERFORMANCE_BUDGET.md`'s targets, not benchmarked.

**Recruiter Value**: Medium-High. Demonstrates responding to review feedback precisely — implementing exactly what was asked, documenting exactly where the implementation diverges from the letter of the request (no new enum value) and why, and catching a subtle test-isolation wrinkle (a code path that legitimately needs its own DB session) rather than either ignoring it or leaving the dev database dirty.

**Production Readiness Score**: 7/10 for the API Layer overall (up from 6/10) — the one disclosed gap from the M4 report is now closed; KI-016 (carried from M3) remains the platform's highest-priority open item before production use.

**Next Milestone**: M5 — Authentication.

---

## 2026-07-11 — M5: Identity Management (Authentication)

**Version**: 0.6.0

**Objective**: Implement the Identity Management system — registration, login, logout, refresh, password hashing/validation, role-based authorization, session management, and authentication middleware — per a founder-approved design review (Founder Decision 002) that resolved every Founder Specification silence on token lifecycle, cookie strategy, anonymous-use boundaries, roles, and lifecycles (see `docs/KNOWN_ISSUES.md` KI-006). OAuth, MFA, email verification, and password reset are explicitly excluded, per direct instruction.

**Scope**: `backend/app/auth/` (the Identity domain: exceptions, password hashing, tokens, account lockout, repository, orchestrating service), a new `refresh_tokens` table (migration `0002`), the API layer (`schemas/auth.py`, `services/auth_service.py`, `routers/auth.py`), authentication middleware (`get_current_user_optional`/`_required`, `get_current_admin_user`) wired into the existing simulation routes, and a startup-time secret-misconfiguration guard added during self-review. Explicitly excluded: OAuth, MFA, email verification, password reset, Simulation History/Admin Import endpoints (auth middleware for them now exists; the endpoints themselves remain future work per KI-023), and M6 (AI Explanations).

**Implementation Summary**: Built `app/auth/` as a domain module mirroring `app.simulation`'s shape exactly — pure business logic, no HTTP/cookie/audit concerns, following the same "engine computes, API-layer service commits and audits" split established by `simulation_service`/`simulation.engine`. `password.py` uses Argon2 (`argon2-cffi`), an 8-character minimum floor (the Founder Specification's own "must meet requirements" is undefined — a documented, not spec-derived, choice), and a fixed dummy-hash comparison so `verify_password` runs the same Argon2 cost whether or not a real user/hash exists, closing a login-timing side channel between "unknown email" and "wrong password." `tokens.py` issues a 15-minute stateless JWT access token (`pyjwt`) and generates/hashes (SHA-256) opaque 256-bit refresh tokens — the raw refresh token is never persisted, only its hash, matching the existing `password_hash` discipline. `lockout.py` is a Redis-backed, per-account (keyed by normalized email) failure counter — deliberately distinct from the existing per-IP `RateLimiter`, since a per-IP-only control cannot stop a distributed credential-stuffing attempt against one target account (Founder Specification 3.6.7). `service.py`'s `authenticate()` enforces a specific, security-motivated ordering: lockout is checked before any password verification (so a locked account never scores another guess), and `is_active` is checked only *after* a correct password is verified (so a wrong-password attempt against a suspended account still yields the same generic `InvalidCredentialsError` as any other failure, never leaking that the account exists and is merely suspended). `refresh_session()` implements rotation-with-reuse-detection: every refresh revokes the presented token and issues a new one chained via `replaced_by_id`; presenting an already-rotated token is treated as a theft signal and revokes every active session for that user. The API layer (`auth_service.py`) mirrors `simulation_service`'s transaction-boundary discipline exactly, with one new wrinkle: `RefreshTokenReuseDetectedError` must be caught and *committed* (not rolled back) separately from a plain `InvalidRefreshTokenError`, because the domain layer's precautionary "revoke everything" write happens before the exception is raised and must survive. Both tokens are delivered exclusively via httpOnly, Secure, `SameSite=Strict` cookies (access token path `/`, refresh token path-scoped to `/api/v1/auth` only) — never in a JSON body, matching the approved Cookie Strategy decision. `get_current_user_optional` (new in `app.api.v1.dependencies`) re-loads the user from the database on every request rather than trusting the JWT's `is_admin` claim, so a mid-session privilege change or suspension takes effect immediately rather than after the token's full lifetime. Wired into the existing M4 routes: `POST /api/v1/simulations` now attaches `user_id` opportunistically when authenticated (anonymous creation remains fully supported, per the approved "Anonymous Users may run simulations" decision), and `GET /api/v1/simulations/{id}` now enforces real ownership instead of M4's fail-closed placeholder.

**Files Created**: `backend/app/models/refresh_token.py`; `backend/alembic/versions/0002_refresh_tokens.py`; `backend/app/auth/{__init__,exceptions,password,tokens,lockout,repository,service}.py`; `backend/app/api/v1/schemas/auth.py`, `services/auth_service.py`, `routers/auth.py`; `backend/tests/auth/{__init__,conftest,test_password,test_tokens,test_lockout,test_service}.py`; `backend/tests/api/{test_auth,test_dependencies}.py`; `backend/tests/core/test_config.py`; `docs/MILESTONE_REPORTS/M5_REPORT.md`.

**Files Modified**: `backend/app/models/{__init__,user}.py` (register `RefreshToken`, add the `user.refresh_tokens` relationship); `backend/app/api/v1/__init__.py` (mount the auth router); `backend/app/api/v1/dependencies.py` (`get_current_user_optional`/`_required`, `get_current_admin_user`, `rate_limit_auth`); `backend/app/api/v1/errors.py` (`UnauthorizedError`, updated `ForbiddenError` docstring); `backend/app/api/v1/audit.py` (`record_auth_audit`); `backend/app/api/v1/exception_handlers.py` (seven new handlers); `backend/app/api/v1/routers/simulations.py` (wired `get_current_user_optional` into both routes); `backend/app/core/config.py` (M5 settings + the JWT-secret startup guard); `backend/requirements.txt` (`argon2-cffi`, `pyjwt`, `email-validator`); `backend/tests/api/conftest.py` (added `rate_limit_auth` to the default no-op overrides); `.env.example`; `.claude/SECURITY_POLICY.md` (token-lifecycle and password-reset gap notes updated to reflect Founder Decision 002); `.claude/DATABASE_RULES.md` (`refresh_tokens` documented); `docs/FOUNDER_DECISIONS.md` (Founder Decision 002); `docs/ARCHITECTURE_DECISIONS.md` (ADR-017–020); `docs/KNOWN_ISSUES.md` (KI-006 resolved, KI-023 updated, KI-027–031 added); `docs/PROJECT_STATE.md`.

**Architecture Decisions**: ADR-017 (opaque, hashed, rotation-chained refresh tokens over a second JWT), ADR-018 (httpOnly/Secure/SameSite=Strict cookie delivery over response-body/`localStorage`), ADR-019 (account lockout as a distinct, email-keyed mechanism alongside IP-based rate limiting), ADR-020 (reject the default `JWT_SECRET` outside development at startup) — all in `docs/ARCHITECTURE_DECISIONS.md`, all implementing Founder Decision 002.

**Problems Encountered**: (1) A genuine transaction-boundary bug drafted and caught before finalizing: an early version of `auth_service.refresh` rolled back on every `InvalidRefreshTokenError`, including `RefreshTokenReuseDetectedError` (a subclass) — since the domain layer's reuse-detection precaution (`revoke_all_for_user`) had already been flushed before raising, a blanket rollback would have silently discarded the very security response the detection exists to perform. (2) `tests/auth/conftest.py`'s `make_user()` originally defaulted to a fixed email (`jane@example.com`); since account-lockout state lives in real Redis, not the per-test-rolled-back database transaction, multiple tests sharing that default email silently accumulated failed-login counts against each other, causing two lockout tests to fail with a lockout the test itself didn't cause. (3) `TestClient`'s cookie jar (httpx-based) correctly refuses to *resend* a `Secure`-flagged cookie over the plain-http scheme it uses internally (`http://testserver`) — the same behavior a real browser exhibits over non-TLS — which silently broke every multi-request auth test relying on automatic cookie persistence. (4) A test asserting JWT tamper-detection flipped only the token's very last character, which occasionally lands on a base64url "don't-care" padding bit some decoders (including PyJWT, per RFC 7515) tolerate without changing the decoded signature bytes — an intermittently no-op tamper, not a real test of the rejection path. (5) The default `argon2-cffi`/`pyjwt`/`email-validator` dependencies needed installing into the existing project venv before anything could run.

**Solutions**: (1) Split the `except` handling in `auth_service.refresh` into two clauses — `RefreshTokenReuseDetectedError` (caught first, since it subclasses the other) now commits before auditing and re-raising; the plain `InvalidRefreshTokenError`/`AccountInactiveError` case still rolls back, since nothing was written in that branch. Directly verified by `test_refresh_reuse_is_detected_and_revokes_every_active_session`, which asserts a second, uninvolved "device"'s session is also revoked. (2) Changed `make_user()`'s default to a fresh `uuid4()`-suffixed email per call, isolating every test's Redis-backed lockout state unless a test explicitly requests a shared email. (3) Tests now pass `cookies=dict(client.cookies)` explicitly on follow-up requests, bypassing the jar's (correct, browser-accurate) automatic scheme filtering — tracked as KI-030, a test-infrastructure-only wrinkle, not a production concern (verified separately that the actual `Secure` attribute is present on the real Set-Cookie header). (4) The tamper test now flips a character ten positions from the end (solidly within the signature's non-boundary bytes), confirmed reliable across five repeated runs. (5) Installed into `backend/.venv` directly; verified via a clean `app.main` import before writing any test.

**Lessons Learned**: The M4-established pattern of "the domain layer only flushes, the API-layer service owns commit/rollback per exception type" needed a genuine new case this milestone (reuse detection's precautionary write), not just a repetition of the existing pattern — a reminder that "matches an existing pattern" and "is exactly the existing pattern with zero new reasoning required" are different claims, and the former still needs to be checked, not assumed. Separately: any test involving Redis-backed state needs the same "isolate per test" discipline as the database gets from transaction rollback — Redis has no such automatic isolation, and a shared fixture default (a fixed email) that looks harmless for stateless tests becomes a genuine cross-test bug the moment any test touches accumulating external state.

**Security Review Summary**: See `docs/SECURITY_LOG.md`'s M5 entry for the full Red Team Review. One finding was fixed during this milestone rather than merely documented: a forgotten `JWT_SECRET` env var in a real deployment would silently allow token forgery (including forged `is_admin: true` claims) with no error anywhere — now a hard startup failure outside `development`/`test` environments (ADR-020). Four residual risks were identified and documented, not fixed, as deliberate, tracked technical debt: a low-severity refresh-token rotation race under concurrent use (KI-027), the inherent 15-minute non-revocability window of a stateless access token (KI-028), an unsurfaced lockout retry-after duration (KI-029), and the deferred password-reset flow itself (KI-031).

**Testing Summary**: See `docs/TESTING_REPORT.md`'s M5 entry. 62 new tests (221 total across the full project: 159 from M0–M4 + 62 new), all passing against live Postgres and Redis instances. Covers unit (password, tokens, lockout), DB-integration (the full `app.auth.service` behavior matrix, including the reuse-detection multi-device scenario), HTTP-integration (all four endpoints, cookie attributes, the enumeration-resistance property, wiring into the existing simulation routes), and direct authorization-dependency tests (`get_current_admin_user`'s rejection path, untestable via HTTP since no admin route exists yet — the same honest gap M4 disclosed for `ForbiddenError`).

**Technical Debt Introduced**: KI-027 (refresh-token rotation race, low severity, mirrors KI-012's precedent), KI-028 (stateless access token's inherent non-revocability window, an accepted architectural tradeoff not a bug), KI-029 (lockout retry-after not surfaced to the client, a minor UX gap), KI-030 (a deprecated httpx test parameter used as a workaround, test-only), KI-031 (password reset itself, deliberately deferred per direct instruction — must ship before production per `.claude/SECURITY_POLICY.md`). All five documented in `docs/KNOWN_ISSUES.md`, none silent.

**Performance Notes**: See `docs/PERFORMANCE_LOG.md`'s M5 entry. Every authenticated request now does one additional database lookup (`get_current_user_optional` loading the user fresh, rather than trusting the JWT claim) — a deliberate correctness-over-marginal-performance choice, consistent with this project's engineering priority order. Login/refresh add a Redis round-trip (lockout check) and register/login/refresh each add one `Argon2`/token operation — none benchmarked against `.claude/PERFORMANCE_BUDGET.md`'s targets yet.

**Recruiter Value**: Very High. This milestone demonstrates full-stack identity-system design under an explicitly incomplete specification: every token/cookie/lockout parameter traces to a documented decision resolving a confirmed spec silence, not an unstated default; a genuine transaction-boundary bug (reuse-detection's write surviving a rollback) caught and fixed via reasoning from an existing pattern rather than copying it blindly; a real, fixable security finding (the JWT-secret footgun) discovered via deliberate self-attack and closed with a hard startup guard, not just written down; and an honest, executed red-team pass distinguishing "fixed now" from "documented, deliberate residual risk" — exactly the kind of judgment call that separates a shipped auth system from a checklist implementation.

**Production Readiness Score**: 6/10 for Identity Management specifically (fully tested, no known unfixed vulnerability, but password reset — a real launch-blocking gap per this project's own security policy — is deliberately not yet built, and KI-027's rotation race is unhardened). Platform-wide production readiness remains gated primarily on KI-016 (carried from M3, still the single highest-priority open item) and the still-unbuilt Frontend/AI Explanations/Deployment milestones.

**Next Milestone**: M6 — AI Explanations (per the approved MVP build order; do not begin without a separate design review, per this milestone's own closing instruction).

---

## 2026-07-12 — M6: Educational AI System

**Version**: 0.7.0

**Objective**: Implement the Educational AI System — renamed from "AI Analyst" to "Educational AI System" per direct founder instruction — comprising an Explanation Engine (structured educational explanations for completed simulations) and a Financial Tutor (scoped follow-up Q&A), following a founder-approved design review (Founder Decision 003) that resolved every Founder Specification silence on provider selection, AI input scope, privacy boundaries, caching/cost control, and integrity validation. Frontend, portfolio simulation, advanced analytics, RAG, and a Historical Event Database are explicitly excluded; the Simulation Engine is not modified in any way.

**Scope**: `backend/app/ai/` (the Educational AI domain: exceptions, prompt templates, safety gates, provider abstraction, orchestrating service), a schema extension to the existing `ai_explanations` table (migration `0003`, no new table), the API layer (`schemas/explanations.py`, `services/explanation_service.py`, `routers/explanations.py`), a new `rate_limit_ai` dependency, and `Settings` additions for provider configuration and cost-control caps. Explicitly excluded: frontend, portfolio simulation, advanced analytics, RAG, a Historical Event Database, and any change to `app/simulation/`.

**Implementation Summary**: Built `app/ai/` as a domain module mirroring `app.auth`'s shape exactly — pure orchestration logic, no HTTP/DB session, following the same "domain computes, API-layer service commits and audits" split established at M3–M5. `providers/base.py` defines a single `AIProvider` Protocol (`generate(system_prompt, user_content, max_tokens) -> ProviderResult`), reusing the exact capability-`Protocol` pattern the ingestion milestone validated (ADR-013); `AnthropicProvider` wraps the `anthropic` SDK and translates every `anthropic.AnthropicError` into a generic `AIProviderUnavailableError`, while `NullProvider` (selected by the default `AI_PROVIDER=none`) always raises the same error with zero network calls — a `Settings`-driven factory (`get_ai_provider`) is the only place either concrete adapter is constructed. `prompt.py` builds two fixed, version-controlled system prompts (Explanation Engine, Financial Tutor); a follow-up question's raw text is placed only inside a delimited `QUESTION` block within the user turn, with the system prompt explicitly instructing the model to treat it as data, never as an instruction to follow — this project's primary, and first-ever-needed, prompt-injection defense. `safety.py` implements three post-generation gates: `check_output_structure` (the six required section headers present), `check_numeric_integrity` (every numeric token in the narrative traceable, within a small tolerance, to the simulation's own `simulation_facts` dict — with small counting numbers 0–12 allowlisted to avoid false positives on ordinary prose numbering), and `check_advice_language` (a best-effort regex blocklist against directive investment-advice phrasing). `service.py`'s `generate_explanation`/`generate_followup_answer` run all applicable gates and, on success, append a fixed, code-owned Educational Disclaimer string — never something the model is asked to generate, a deliberate hardening beyond the literal approved instruction, documented in ADR-023. On any failure (provider unavailable, missing sections, a fabricated number, disallowed advice language), the API-layer `explanation_service.py` marks the row `FAILED` with `explanation_text` left `NULL` forever and returns the exact founder-approved safe message — as a normal, successful HTTP response, never an error envelope, so an AI outage can never appear as a broken request. Caching (ADR-022) matches on `(simulation_id, explanation_type, model_name, prompt_version[, question_text])`: for the Explanation Engine, a match is returned regardless of status (a prior failure is also cached, since an explicit `regenerate` flag exists to force a retry, bounded by a regeneration cap); for the Financial Tutor, only a `COMPLETED` prior answer is cached, since follow-ups have no `regenerate` override and a permanently-cached failure would be a dead end. `_build_simulation_facts` (`explanation_service.py`) is the single, auditable allowlist deciding what `app.ai` is ever allowed to see — asset symbol, investment amount, dates (plus derived `start_year`/`end_year`/`duration_years`), simulation outputs, and dividend/inflation flags — excluding email, display name, user ID, IP address, session ID, request ID, and auth information by construction, closing off both the platform's first third-party-data-sharing risk and the most obvious prompt-injection vector in the same design.

**Files Created**: `backend/alembic/versions/0003_ai_explanation_type.py`; `backend/app/ai/{__init__,exceptions,prompt,safety,service}.py`; `backend/app/ai/providers/{__init__,base,anthropic_provider,null_provider}.py`; `backend/app/api/v1/schemas/explanations.py`, `services/explanation_service.py`, `routers/explanations.py`; `backend/tests/ai/{__init__,test_safety,test_prompt,test_service,test_providers}.py`; `backend/tests/api/test_explanations.py`; `backend/tests/core/test_ai_config.py`; `docs/MILESTONE_REPORTS/M6_REPORT.md`.

**Files Modified**: `backend/app/models/{enums,ai_explanation}.py` (new `AIExplanationType` enum, `explanation_type`/`question_text` columns, composite index); `backend/app/api/v1/{__init__,dependencies,errors,audit,exception_handlers}.py` (mount the explanations router, `rate_limit_ai`, `SimulationNotCompletedError`/`RegenerationCapExceededError`, `record_ai_audit`, two new exception handlers); `backend/app/core/config.py` (M6 settings + the AI-provider startup guard); `backend/requirements.txt` (`anthropic`); `backend/tests/api/conftest.py` (added `rate_limit_ai` to the default no-op overrides); `.env.example`; `.claude/{SECURITY_POLICY,DATABASE_RULES}.md`; `docs/{FOUNDER_DECISIONS,ARCHITECTURE_DECISIONS,KNOWN_ISSUES,CHANGELOG,SECURITY_LOG,TESTING_REPORT,PERFORMANCE_LOG,PROJECT_STATE}.md`.

**Architecture Decisions**: ADR-021 (Protocol-based provider abstraction, Anthropic first, `NullProvider` fallback), ADR-022 (asymmetric caching semantics — any-status for the Explanation Engine's explicit-retry design, `COMPLETED`-only for the Financial Tutor's no-retry design), ADR-023 (the Educational Disclaimer is code-appended, never model-generated), ADR-024 (`ai_explanations` extended with `explanation_type`/`question_text` rather than a new table) — all in `docs/ARCHITECTURE_DECISIONS.md`, all implementing Founder Decision 003.

**Problems Encountered**: (1) The initial caching design filtered cache lookups to `generation_status == COMPLETED` only — this would have silently defeated the regeneration cap's entire purpose, since every non-regenerate call after a failure would keep invoking the model indefinitely (no cache hit ever occurs against a `FAILED` row, and the cap only checks `regenerate=True` calls). Caught while writing the cache-hit integration tests, before it shipped, not after. (2) Two of those same cache-hit tests then failed for an unrelated reason: the test harness patched `get_ai_provider` to return a fake provider without also aligning `get_settings`'s reported `ai_model_name` — since `_configured_model_name` collapses to `"none"` whenever `Settings.ai_provider == "none"`, the pre-generation model-name placeholder used by the cache lookup didn't match what the fake provider actually reported after generation. (3) Running the full test suite once triggered the familiar M1-onward environmental behavior (KI-009): `tests/test_migrations.py`'s downgrade-to-base step wiped the local dev schema, causing every unrelated test file collected after it to fail with "relation does not exist." (4) This session's Docker daemon was not initially running, unlike prior sessions where it apparently already was.

**Solutions**: (1) Redesigned caching to match regardless of status for the Explanation Engine specifically (any prior attempt, success or failure, is "the stored explanation" until `regenerate=True` is passed), while keeping `only_completed=True` for the Financial Tutor, which has no equivalent override — documented as a deliberate asymmetry in `_find_cached`'s own docstring and in ADR-022, not left as an unexplained inconsistency. (2) Fixed `_patch_provider` (test helper) to also patch `get_settings` so `ai_provider`/`ai_model_name` stay consistent with whatever the fake provider reports — a test-harness bug, not a production one, since real code always derives both values from the same `Settings` object. (3) Restored the schema via `alembic upgrade head` and adopted running `tests/test_migrations.py` in isolation for the remainder of the session, matching the M1–M5 precedent for this known, documented, pre-existing behavior. (4) Started Docker Desktop and waited for the daemon; both containers (already existing from a prior session) came up healthy within seconds.

**Lessons Learned**: A cache-invalidation design's correctness can't be judged from its "happy path" alone — the M6 caching bug only became visible when writing a test for the *cap*, not the cache hit itself, because the two features' correctness is coupled (a cache filter that's too permissive silently defeats a cap that looks perfectly correct in isolation). This is the same category of lesson M4 drew from KI-026 (a cross-cutting requirement sitting orthogonal to the "main" feature work is easy to under-test) — write the test for the *interaction* between two features, not just each one independently. Separately: a test harness that patches two functions which are coupled in production (here, `get_ai_provider` and `get_settings`, both ultimately derived from the same `Settings` object) must patch both consistently, or the test can fail for a reason that has nothing to do with the code under test.

**Security Review Summary**: See `docs/SECURITY_LOG.md`'s M6 entry for the full AI Safety Review (six questions verifying the design's own core promises against the implementation) and Red Team Review (four additional adversarial questions). No critical or high-severity finding — the highest-severity residual item (KI-032, heuristic safety-check coverage gaps) is a documented limitation of a pattern-matching approach, not an architectural hole; two lower-severity items (KI-033, a cap-check race mirroring already-accepted precedent; KI-034, an unverified provider-echoed-model-name assumption) are cost/UX-only in their worst case, never a financial-integrity or privacy breach.

**Testing Summary**: See `docs/TESTING_REPORT.md`'s M6 entry. 55 new tests (276 total across the full project: 221 from M0–M5 + 55 new), all passing. 38 non-DB unit tests (`tests/ai/`: safety gates, prompt construction, service orchestration against a fake provider, the Anthropic adapter against a mocked SDK client — zero real network calls anywhere in this suite), 4 config-guard tests, 17 HTTP-integration tests (`tests/api/test_explanations.py`, exercising the real default `NullProvider` safe-fallback path through the actual HTTP stack, plus cache/cap/ownership/audit behavior via a monkeypatched fake provider).

**Technical Debt Introduced**: KI-032 (heuristic numeric-integrity/advice-language checks are not exhaustive — the most consequential open item from this milestone), KI-033 (low-severity cap-check TOCTOU race, mirrors KI-012/KI-027's already-accepted precedent), KI-034 (unverified assumption that Anthropic echoes back the exact requested model string, affecting cache efficiency only, not correctness). All three documented in `docs/KNOWN_ISSUES.md`, none silent.

**Performance Notes**: See `docs/PERFORMANCE_LOG.md`'s M6 entry. Real Anthropic API latency could not be benchmarked in this session (no live API key available) — the 12-second internal timeout leaving margin inside the Founder Specification's 15-second AI-explanation budget is a design-time guarantee, not yet an empirically verified one. A new composite index (`idx_ai_explanations_simulation_type`) was added specifically to support this milestone's cache/cap query pattern, which did not exist before M6.

**Recruiter Value**: Very High. This milestone demonstrates safe LLM integration under an explicit, non-negotiable constraint ("the AI never calculates, only explains") enforced structurally rather than by policy alone: a strict input allowlist that closes off both a privacy risk and a prompt-injection vector in one design decision; three independent post-generation safety gates that reject unsafe output outright rather than attempting to sanitize it; a deliberate, disclosed hardening beyond the literal spec (a code-appended, not model-generated, compliance disclaimer); and a genuine caching-design bug caught by reasoning about the *interaction* between two features (cache and cost-control cap) during test-writing, not after — plus an honest, structured two-part security review (Safety Review verifying the design's own promises, Red Team Review probing adversarially) that names its own residual gaps rather than declaring victory.

**Production Readiness Score**: 6/10 for the Educational AI System specifically — fully tested against the default (`NullProvider`) and a fake-provider success path, structurally safe against every named prohibited behavior, but never verified against a real Anthropic API call (no live key available this session — this is the one meaningful gap before real-world confidence, not a design flaw) and carrying two low-severity, already-precedented residual risks (KI-033, KI-034). Platform-wide production readiness remains gated primarily on KI-016 (carried from M3, still the single highest-priority open item) and the still-unbuilt Frontend/Deployment milestones.

**Recommended Next Milestone**: M7 — Frontend (Next.js), per the approved MVP build order — the Simulation Engine, API, Identity Management, and now the Educational AI System are all stable and tested, satisfying the standing "backend before frontend" rule. Before or alongside M7: verify the Educational AI System against a real Anthropic API key (closing KI-034 and this milestone's one open performance question) — a low-cost verification step, not a redesign.

---

## 2026-07-13 — M7 Phase 0: Design Foundation & Brand Constitution (not a coding milestone)

**Version**: 0.7.1

**Objective**: Before beginning any Next.js/React/Tailwind work, produce the design-review-then-implement precedent's equivalent for the frontend milestone (mirroring M3's design review): pressure-test the initially proposed frontend design system against the platform's own backend truth (`api_design.md`, `simulation_formulas.md`, `KNOWN_ISSUES.md`), validate it against installed frontend/UI/UX design skills, resolve the open brand-identity questions those passes surfaced, and consolidate everything into one authoritative brand/visual-identity document before any frontend code is written.

**Scope**: Documentation only — `docs/BRAND_CONSTITUTION.md` (new), `docs/PROJECT_STATE.md` updated. No `frontend/` directory content, no React components, no Tailwind config, no packages installed. `docs/frontend_design_system.md` (the implementation-level design system produced earlier in this same design-review phase) is unchanged by this pass but is now explicitly subordinate to the new constitution.

**Implementation Summary**: The review ran in three parts. Part one re-validated the originally proposed design system against this project's own backend contracts and installed design skills (`ui-ux-pro-max`, `design-system`), cross-checking chart-type and color choices against those skills' databases — the cross-check mostly validated the existing direction (e.g., a generic "fintech" keyword search in the skill's color/style database defaulted toward glassmorphism and gold/purple, confirming that this brand's Stripe/Linear-style restraint is a deliberate, load-bearing choice rather than a template default) while surfacing a few genuine gaps: no locked brand mark, an unresolved anonymous-AI-access ambiguity, and the growth-chart persistence gap (KI-021) needing to be reclassified from "design around it" to "blocking prerequisite." Part two developed the brand identity itself — personality, keywords, typography/color/motion reasoning, signature details, and a brutally honest recruiter-style critique of the (currently nonexistent) frontend. Part three consolidated all of it into `docs/BRAND_CONSTITUTION.md`, written as a single-authored standard rather than a concatenation of the three passes: overlapping or redundant guidance (e.g., near-duplicate anonymous-AI-access decisions) was merged into one clear statement, the open questions from parts one and two were resolved decisively rather than left as hedges (the wordmark is typographic, not iconographic; the AI palette is a neutral tint plus a label, never a dedicated hue; the time-axis motif's horizontal page-transition is adopted as the product's one standard transition), and nine new Founder Decisions (FD-004 through FD-012, including a new one — FD-012, "Confidence Without Ego" — added during this pass) were recorded in the constitution's §3.

**Files Created**: `docs/BRAND_CONSTITUTION.md`.

**Files Modified**: `docs/PROJECT_STATE.md` (version bump, M7 Phase 0 marked complete, Next Milestone updated to M7 Phase 1, FD-004–012 noted under Open Founder Decisions), `docs/DEVLOG.md` (this entry), `docs/CHANGELOG.md` (0.7.1 entry).

**Architecture Decisions**: None — this pass produced Founder Decisions (brand/product-direction choices), not engineering-implementation decisions; no ADR is warranted until Phase 1 makes an actual implementation choice (e.g., a specific Tailwind token-generation approach) that would need one.

**Problems Encountered**: None — a research, review, and consolidation pass, not implementation. The one genuine judgment call was scoping: FD-004 through FD-012 are fully recorded in `docs/BRAND_CONSTITUTION.md`, but were not also formalized into `docs/FOUNDER_DECISIONS.md`'s own "Founder Decision 00N" numbering convention, since doing so was outside this task's explicitly requested documentation scope (`PROJECT_STATE.md`/`DEVLOG.md`/`CHANGELOG.md` only) — noted as a recommended near-term follow-up rather than done unprompted.

**Solutions**: N/A.

**Lessons Learned**: Cross-checking a self-authored design proposal against an independent, keyword-driven design-recommendation tool produced a genuinely useful negative result — the tool's default "fintech" match (glassmorphism, gold, purple) is close to the exact aesthetic the brief explicitly rejects, which is stronger evidence for the chosen direction's deliberateness than either self-review alone would have produced. Separately: a "brand constitution" produced by literally merging three prior review documents would have read as three different authors' voices stitched together; the useful discipline was resolving each open hedge to a single decisive answer (wordmark, AI palette, page-transition motif) rather than preserving the earlier documents' "needs founder input" framing wherever a defensible answer was already implied by principles already agreed on.

**Security Review Summary**: Not applicable — no code changed.

**Testing Summary**: Not applicable — no code changed.

**Technical Debt Introduced**: None — no new KI. One pre-existing item (KI-021) is reclassified in status/urgency by FD-008 (from an accepted design-around to a blocking prerequisite for the Results screen specifically) but not newly created by this pass.

**Performance Notes**: Not applicable.

**Recruiter Value**: Medium-High — demonstrates the same "validate against source, don't rely on memory" discipline the M3 design review established, applied here to brand/UX rather than financial formulas: an independent tool cross-check that produced a real, citable negative-validation finding; a consolidated, single-voice standard rather than a pile of review notes; and an honest, in-document acknowledgment of a documentation-formalization gap (FD-004–012 not yet mirrored into `FOUNDER_DECISIONS.md`'s numbering) rather than silently leaving it ambiguous.

**Production Readiness Score**: Not applicable — documentation-only pass. Frontend production readiness remains at 0% built (the `frontend/` directory is still empty); this milestone closes out the design-foundation prerequisite for Phase 1, not any implementation progress.

**Next Milestone**: M7 Phase 1 — Design Token & Primitive Implementation (Tailwind/CSS token layer, both themes, and the shared primitive component set only — no page-level work until that checkpoint is reviewed and approved).

---

## 2026-07-14 — M7 Phase 0 Follow-up: Founder Decision 004 Formalized (not a coding milestone)

**Version**: 0.7.2

**Objective**: Close a documentation-consistency gap the prior M7 Phase 0 entry disclosed rather than hid: the nine brand/scope decisions recorded inside `docs/BRAND_CONSTITUTION.md` §3 (labeled FD-004 through FD-012 in that document's own local notation) had not yet been mirrored into `docs/FOUNDER_DECISIONS.md`'s own append-only "Founder Decision 00N" ledger, the project's established single place every founder-level decision must be traceable to.

**Scope**: Documentation only — `docs/FOUNDER_DECISIONS.md` (new Founder Decision 004, appended; Decisions 001–003 untouched), `docs/PROJECT_STATE.md` (version bump, cross-references updated to point at the new formal entry). No frontend code, no application code, no change to `docs/BRAND_CONSTITUTION.md`'s content.

**Implementation Summary**: Appended **Founder Decision 004** to `docs/FOUNDER_DECISIONS.md`, following the exact Context/Decision/Rationale/Scope/Deferred/Related structure established by Decisions 001–003, consolidating all nine brand-constitution decisions (visual design system approval, theme architecture, M7 feature scope exclusions, minimal Account/Settings scope, growth-chart consistency as a backend precondition, anonymous educational AI access, the anonymous-AI rate-limit-not-access-gate principle, trust/education over excitement, and confidence without ego) into one dated, numbered entry rather than nine separate ones — matching Founder Decision 003's own precedent of consolidating a milestone's several sub-decisions under one entry. `docs/PROJECT_STATE.md`'s "Open Founder Decisions" list and Current Milestone summary were updated to cite "Founder Decision 004" directly rather than the constitution's informal `FD-00N` labels, since `PROJECT_STATE.md` is overwritten (not append-only) and should always reflect the current, correct state rather than a historical snapshot.

**Files Created**: None.

**Files Modified**: `docs/FOUNDER_DECISIONS.md` (Founder Decision 004 appended), `docs/PROJECT_STATE.md` (version bump to 0.7.2; Current Milestone, Completed Milestones table, and Open Founder Decisions updated to reference Founder Decision 004).

**Architecture Decisions**: None — no engineering-implementation choice was made in this pass, only a documentation-ledger formalization of decisions already approved.

**Problems Encountered**: None.

**Solutions**: N/A.

**Lessons Learned**: The prior M7 Phase 0 entry's decision to explicitly disclose this gap ("noted as a recommended near-term follow-up rather than done unprompted") rather than silently leaving it implicit is what made this follow-up straightforward to schedule and execute correctly — an undisclosed gap would likely have been discovered later, if at all, only when someone went looking for "Founder Decision 004" in the ledger and didn't find it.

**Security Review Summary**: Not applicable — no code changed.

**Testing Summary**: Not applicable — no code changed.

**Technical Debt Introduced**: None.

**Performance Notes**: Not applicable.

**Recruiter Value**: Low-Medium — a small, honest housekeeping pass, but one that demonstrates the discipline of actually closing a self-identified documentation gap on a short follow-up rather than letting a disclosed "recommended follow-up" quietly go stale.

**Production Readiness Score**: Not applicable — documentation-only pass; no change to frontend build status (`frontend/` remains empty).

**Next Milestone**: M7 Phase 1 — Design Token & Primitive Implementation, unchanged from the prior entry — this follow-up does not itself unblock or change Phase 1's scope, since Founder Decision 004's content was already in effect via `docs/BRAND_CONSTITUTION.md` before this formalization.

---

## 2026-07-15 — M7 Phase 1: Frontend Foundation

**Version**: 0.8.0

**Objective**: Build the platform's first frontend code — a scalable architectural foundation every future page builds on — per the approved M7 Phase 0 design foundation (`docs/BRAND_CONSTITUTION.md`, `docs/frontend_design_system.md`, Founder Decision 004). Explicitly scoped to tokens, theming, shared providers, the API client, and eight primitive components only; no product page (Simulator, Results, Auth, Account) was built.

**Scope**: `frontend/` scaffolded from scratch (`create-next-app`, Next.js 16.2.10, App Router, TypeScript, Tailwind v4). Design tokens (three-layer CSS-variable architecture), light/dark theme switching, shared providers (Theme, TanStack Query, Toast, error boundaries), a centralized Axios API client with typed models and one error-code-to-copy table, eight primitive UI components, and a Vitest + React Testing Library test suite. Explicitly excluded per direct instruction: the Simulator, Results, Asset Explorer, Simulation History, and Auth pages, and any chart component.

**Implementation Summary**: Scaffolded `frontend/` via `create-next-app` and read this Next.js version's own bundled docs (`node_modules/next/dist/docs/`) before writing App Router code, per its own `AGENTS.md` warning that the version may differ from prior training data — this surfaced two real, version-specific behaviors used directly: Next 16.2's `unstable_retry` prop on `error.js`/`global-error.js` (replacing the older `reset`-only convention), and this version's own documented `data-theme` + inline-script flash-prevention pattern, which was adopted directly in place of a third-party theme library (ADR-026).

Implemented the design token system as three CSS files (`src/styles/tokens/{primitives,semantic,components}.css`) holding real, runtime-switchable CSS custom properties, bridged into Tailwind v4's `@theme inline` block (`globals.css`) so ordinary utility classes (`bg-surface`, `text-ink-primary`, `border-border-hairline`) resolve correctly in both themes without a single `dark:` variant anywhere in a component (ADR-025) — the direct implementation of `docs/BRAND_CONSTITUTION.md`'s "components reference semantic tokens only" rule. A z-index scale (six tiers) was invented as a new primitive token, since neither Tailwind v4 nor any Phase 0 document specified one.

Theme switching (`src/providers/theme-provider.tsx` + `theme-script.ts`) reads a stored preference (falling back to `prefers-color-scheme`) via a synchronous inline `<head>` script before first paint, with a `useTheme()`/`setTheme()` hook exposed for a future toggle — deliberately not rendered yet, per the Brand Constitution's "no toggle unless it naturally falls out of the architecture" instruction (ADR-026).

The API layer (`src/lib/api/`) centralizes every backend interaction behind one Axios instance (`withCredentials: true` for Founder Decision 002's httpOnly session cookies) and one `apiRequest<T>()` entry point: a response interceptor unwraps `{success:true,data}` and normalizes every failure — a backend error envelope or a transport-level failure — into one typed `ApiError`, via a pure, separately-exported `normalizeApiError()` function (ADR-027). Typed models (`src/types/api.ts`) mirror `docs/api_design.md` and `backend/app/models/enums.py` exactly, keeping every financial figure a `string` end-to-end (never parsed into a JS number) — the frontend's concrete expression of `.claude/CODING_STANDARDS.md`'s "presentation-only, never calculates" rule. `growth_series`'s per-point shape is flagged in a code comment as inferred, not confirmed, since it is always empty in practice today (KI-021) — tracked as new KI-036 rather than presented as verified.

Eight primitive components (`src/components/ui/`) were built and reviewed against the Brand Constitution's Component Review Checklist: hairline-only dividers, no scale/bounce transforms, icon+label on every Badge, tabular numerals via a shared `.figure` utility class, a native `<details>`-based provenance disclosure on `StatTile` (the constitution's own signature detail #5 — source formula on tap/hover, zero additional JavaScript, fully keyboard- and screen-reader-operable by construction), and `role="alert"`/`aria-invalid`/`aria-describedby` wired through `ErrorState` and `Input`. `class-variance-authority` was added for variant-driven components (Button, Badge) and `lucide-react` for the constitution's named icon family — both small, focused, explicitly justified additions beyond the required stack, not silent scope creep.

**Files Created**: `frontend/` (entire new directory tree) — `src/app/{layout,page,globals.css,error,global-error,not-found}.tsx`; `src/styles/tokens/{primitives,semantic,components}.css`; `src/providers/{theme-provider,theme-script,query-provider,toast-provider,app-providers}.tsx`; `src/components/ui/{button,input,card,badge,skeleton,empty-state,error-state,stat-tile,index}.tsx`; `src/lib/{utils,api/client,api/errors,api/index,api/endpoints/assets,api/endpoints/simulations}.ts`; `src/types/api.ts`; `src/config/env.ts`; `src/__tests__/` (15 test files, 42 tests); `vitest.config.mts`, `vitest.setup.ts`; `.env.example`, `.env.local`.

**Files Modified**: `.gitattributes` (explicit LF rules for frontend source file types, resolving KI-010), `docs/ARCHITECTURE_DECISIONS.md` (ADR-025 through ADR-027), `docs/KNOWN_ISSUES.md` (KI-010 resolved, KI-036 added), `docs/{TESTING_REPORT,SECURITY_LOG,PERFORMANCE_LOG,PROJECT_STATE,CHANGELOG}.md`.

**Architecture Decisions**: ADR-025 (CSS-variable three-layer token architecture bridged into Tailwind v4's `@theme inline`, plus a new z-index scale), ADR-026 (`data-theme` attribute + inline head script over a third-party theme library), ADR-027 (one centralized Axios client, one pure error-normalization function, one error-code-to-copy table) — all in `docs/ARCHITECTURE_DECISIONS.md`.

**Problems Encountered**: (1) Tailwind v4 (installed by default via `create-next-app`) removed the JavaScript `tailwind.config.ts` file the Phase 0 design system implicitly assumed, in favor of a CSS-first `@theme` directive — the wrong fit on its own for a token system that must switch every color at runtime. (2) An initial attempt to unit-test `client.ts`'s error-handling via a fake axios `adapter` returning a non-2xx status did not actually trigger axios's rejection path the way a real HTTP transport would, causing two tests to fail with "resolved instead of rejecting." (3) Vitest does not auto-register React Testing Library's DOM cleanup between tests the way Jest's globals detection does, causing "multiple elements found" failures once a second test in a file rendered into an already-populated `document.body`. (4) An unrelated `package-lock.json` in the user's home directory (outside this repository) made Next.js infer the wrong workspace root, emitting a build warning. (5) The frontend coverage report's default text-reporter table omitted two fully-covered files (`button.tsx`, `badge.tsx`) from its per-directory breakdown, even though their data was correctly included in the overall percentage — a cosmetic reporter quirk in this very new (`vitest@4.1.10`/`@vitest/coverage-v8@4.1.10`) tooling version, confirmed via the HTML report before trusting the terminal summary.

**Solutions**: (1) Adopted the CSS-variable-plus-`@theme inline`-bridge pattern (ADR-025) instead of fighting the framework's new default. (2) Refactored the interceptor's error-normalization logic into an exported pure function, `normalizeApiError()`, and rewrote the failing tests to call it directly with constructed `AxiosError` objects rather than depending on axios's `validateStatus`/custom-adapter interaction (ADR-027's stated rationale). (3) Added `afterEach(() => cleanup())` to `vitest.setup.ts`. (4) Set `turbopack.root` explicitly in `next.config.ts` rather than relying on lockfile auto-detection. (5) Cross-checked the terminal summary against the HTML coverage report (`coverage/components/ui/index.html`) before treating the lower headline number as real — both files were in fact at 100%, and the true aggregate (94.26% statements) was accurate throughout; only the per-file breakdown table had the display bug.

**Lessons Learned**: This Next.js version's own bundled `AGENTS.md`/`docs/` warning — read before writing any App Router code, not after hitting an error — paid off directly twice (the `unstable_retry` prop, the documented flash-prevention pattern), the same "consult the source, not memory" discipline the M3 design review already established for the Founder Specification, now extended to a fast-moving framework dependency. Separately: when a fake-transport test technique produces a surprising result (axios "not rejecting" a 404), the more robust fix is usually to test the pure logic directly rather than debug deeper into a third-party library's internals — matching this project's own preference (seen throughout the backend test suite) for isolating pure functions from their I/O boundary specifically so they're testable without simulating the boundary at all.

**Security Review Summary**: See `docs/SECURITY_LOG.md`'s M7 Phase 1 entry. No vulnerability found or fixed — this phase has no user-input-handling surface yet. Five forward-looking findings confirmed the foundation doesn't foreclose later security properties: correct `withCredentials` cookie handling, exactly one (inert) `dangerouslySetInnerHTML` use, fail-fast env validation, a type-system-enforced error-copy table, and no secrets committed.

**Testing Summary**: See `docs/TESTING_REPORT.md`'s M7 Phase 1 entry. 42 new tests (the platform's first frontend suite), all passing, 94.26%/97.29%/77.27%/93.87% (statements/lines/branches/functions) — comfortably above `.claude/TESTING_GUIDELINES.md`'s 60%+ frontend target. `npx eslint .` and `npx tsc --noEmit` both pass with zero errors; `npm run build` completes successfully with both routes prerendered as static content; a manual dev-server smoke check confirmed the placeholder page actually renders and hydrates with `data-theme="light"` correctly applied server-side.

**Technical Debt Introduced**: KI-036 (an inferred, unverified `growth_series` per-point type shape — low severity, no component consumes it yet). KI-010 was resolved, not introduced, by this milestone.

**Performance Notes**: See `docs/PERFORMANCE_LOG.md`'s M7 Phase 1 entry. `npm run build` completes in ~4-9s; total JS chunk size ~720KB for the current two-route app, with `recharts` (installed, unused) contributing zero bytes until a future chart component first imports it. No page-load budget has been measured yet against a real screen.

**Recruiter Value**: High. Demonstrates reading a fast-moving framework's own version-specific documentation before writing code rather than relying on (potentially stale) training data — directly catching two real API differences before they became bugs; a runtime-switchable design-token architecture solving a genuine Tailwind v4 framework-migration problem with a well-precedented pattern (matching shadcn/ui's own v4 approach) rather than a bespoke one; one centralized, type-safe error-handling boundary preventing every future screen from reinventing backend-error handling; and an honest test-suite correction story (a fragile fake-adapter test technique replaced with a robust pure-function test, documented as the reason for a code refactor, not hidden).

**Production Readiness Score**: Not yet meaningful for this phase specifically — no product-facing page exists. The foundation itself is complete and verified (tests, lint, typecheck, build, and a manual runtime smoke check all pass), de-risking Phase 2's actual screens.

**Next Milestone**: M7 Phase 2 — Product Screens, starting with the Simulator → Results flow. Two items flagged for resolution before or alongside that specific screen: `growth_series`/`disclosed_splits` persistence (KI-021/FD-008) and the anonymous-AI rate-limit specifics (FD-009/010).

---

## 2026-07-16 — M7 Phase 1.5: Frontend Foundation Hardening

**Version**: 0.8.1

**Objective**: Respond to an external architecture review's ten required fixes on the M7 Phase 1 foundation before any product page is built: a visual verification surface, a financial-formatting layer with practical never-calculates guardrails, an API-contract-drift strategy, an auth/CSRF posture review, a theme-script CSP posture note, accessibility evidence, KI-036 clarification, `unstable_retry` isolation, and fixed TanStack Query conventions.

**Scope**: `frontend/src/lib/format/` (new), `frontend/src/lib/query/` (new), `frontend/src/hooks/use-asset-search.ts` (new), `frontend/src/app/dev/playground/` (new, dev-only), `frontend/src/lib/next-error-boundary.ts` (new), `frontend/src/styles/tokens/{primitives,semantic}.css` (fixed), `frontend/src/types/api.ts` (fixed), `frontend/eslint.config.mjs` (new guardrail rule), 61 new tests. No product page was built, per this phase's own explicit scope boundary.

**Implementation Summary**: This phase's most consequential outcome was that the review process itself caught real, already-shipped defects M7 Phase 1's own verification had missed — not just added new tooling. (1) **Contrast/self-reference bug (ADR-028)**: computing actual WCAG contrast ratios for every ink/status color (rather than trusting the Phase 0-approved hex values as shipped) found that muted-ink and three of four status colors failed AA's 4.5:1 threshold in at least one theme; separately, inspecting the actual compiled production CSS output found `semantic.css`'s status-color declarations were literally self-referencing (`--color-status-good: var(--color-status-good)`), invalid CSS that would have silently dropped Badge's status-color differentiation. Both fixed with verified new hex values (`frontend/src/__tests__/lib/contrast.test.ts`, a known-answer test mirroring this project's own backend testing philosophy). (2) **API contract drift (ADR-030, KI-036/KI-038)**: building the promised drift-detection test required reading the real backend Pydantic schemas directly (`backend/app/api/v1/schemas/simulations.py`) rather than guessing — this immediately found genuine, already-shipped mismatches: `GrowthSeriesPoint`/`DisclosedSplit`'s date fields are named `point_date`/`split_date`, not `date` as Phase 1 had guessed; six `SimulationResponse` fields Phase 1 typed as always-present are actually nullable (null whenever a simulation is `pending`/`failed`); and `error_message` was missing from the frontend type entirely. Both the source-reading and the drift-detection test's live-path assertions were verified end-to-end against a real, running backend (Docker Desktop started, `postgres`/`redis` containers brought up, `uvicorn app.main:app` started directly, all four live assertions passed, then torn down cleanly) — not just read from source. (3) **Financial-math guardrail (ADR-029)**: a branded `DecimalString` type (`src/lib/format/decimal-string.ts`) plus a real ESLint `no-restricted-syntax` rule (a core rule, no new dependency) banning `Number(`/`parseFloat(`/`parseInt(`/unary-`+` in `src/app/**`/`src/components/**` — verified against a deliberate scratch violation (all four patterns caught) before confirming the real, already-written codebase passes cleanly. Every formatter (`currency.ts`, `percentage.ts`, `date.ts`, `nullable.ts`) operates on decimal-string digit characters directly, never converting to a JS `number`, including round-half-up rounding with carry propagation implemented as pure string/digit-array arithmetic. `nullable.ts` resolves the long-standing dual-meaning-null problem (`docs/frontend_design_system.md` §14 risk 7) with an explicit, mandatory reason parameter (`'not_requested'` vs. `'unavailable'`). (4) A dev-only visual playground (`src/app/dev/playground/`) renders every primitive/variant in both themes, guarded by `notFound()` in production — verified two ways: a manual `npm run dev`/`npm run build` + inspect-the-static-output check (confirming the production HTML is genuinely the not-found page's markup), and an automated test. (5) `RouteErrorBoundaryProps` (`src/lib/next-error-boundary.ts`) isolates Next.js's explicitly-unstable `unstable_retry` prop shape behind one shared type. (6) `src/lib/query/` fixes query-key-factory and error-handling conventions before any Phase 2 screen invents its own, demonstrated end-to-end by `useAssetSearch`, a genuinely cross-cutting reference hook. (7) Accessibility evidence: `axe-core` structural tests (zero violations) for every primitive, explicit keyboard-operability tests (Tab/Enter/Space on Button, a native `<details>`/`<summary>` disclosure on StatTile), and a static check that the global `prefers-reduced-motion` CSS override exists and is unconditional — each with an honestly-stated boundary of what jsdom cannot verify (real computed contrast, real animation timing) and what verifies it instead. (8) Auth/CSRF posture: verified `SameSite` is evaluated by registrable site, not port, so local dev (`localhost:3000`↔`:8000`) works correctly; verified CORS/cookie configuration matches Founder Decision 002/ADR-018 exactly; found one genuine, previously undocumented production risk (KI-039: nothing enforces the custom-domain requirement ADR-018's own text assumes). (9) Theme-script CSP posture documented as a forward-looking note (KI-040): no CSP exists yet, but hash-based CSP (not nonces, which would force the whole app into dynamic rendering) is identified as the better fit for this specific static, deterministic script, if CSP is ever added.

**Files Created**: `frontend/src/lib/format/{decimal-string,currency,percentage,date,nullable,index}.ts`, `frontend/src/lib/format/README.md`; `frontend/src/lib/query/{keys.ts,README.md}`; `frontend/src/hooks/use-asset-search.ts`; `frontend/src/lib/next-error-boundary.ts`; `frontend/src/app/dev/playground/{page,playground-client}.tsx`; 15 new test files under `frontend/src/__tests__/`.

**Files Modified**: `frontend/src/styles/tokens/{primitives,semantic}.css` (ADR-028 fix), `frontend/src/types/api.ts` (`DecimalString` branding; KI-036/KI-038 field-name and nullability fixes), `frontend/src/app/{error,global-error}.tsx` (ADR-031), `frontend/eslint.config.mjs` (ADR-029 rule), `frontend/next.config.ts` (unrelated Turbopack-root warning fix noticed in passing), `docs/frontend_design_system.md` (status/muted color table corrected to match ADR-028), `docs/{ARCHITECTURE_DECISIONS,KNOWN_ISSUES,SECURITY_LOG,PROJECT_STATE,CHANGELOG}.md`.

**Architecture Decisions**: ADR-028 (status/muted-ink colors split light/dark, fixing a real contrast bug and a self-reference bug), ADR-029 (branded `DecimalString` type + ESLint guardrail against numeric coercion in product code), ADR-030 (hand-written API types + live-schema drift detection, codegen deferred), ADR-031 (`unstable_retry` isolated behind a shared type, not a runtime wrapper), ADR-032 (TanStack Query key/error-handling conventions fixed before Phase 2) — all in `docs/ARCHITECTURE_DECISIONS.md`.

**Problems Encountered**: (1) A fake-axios-`adapter`-based test technique for simulating non-2xx HTTP responses did not trigger axios's own rejection path the way a real transport would (unrelated to this phase — already fixed at M7 Phase 1, not re-broken here, but relevant precedent for problem 4 below). (2) The static-analysis test scanning `src/lib/format/` for banned numeric-coercion tokens initially failed against its own module — because the module's own JSDoc comments *quote* `Number(...)` etc. as documentation of what not to do, which the naive regex scan correctly, if unhelpfully, flagged. (3) `Object.defineProperty(process.env, 'NODE_ENV', ...)` failed in the playground-guard test — Node's `process.env` does not accept a plain data-descriptor redefinition on some versions. (4) A second playground-guard test (verifying non-production rendering) hit an unrelated Vitest/React `jsxDEV is not a function` module-caching quirk when dynamically re-importing the same page module a second time after `vi.resetModules()` under a different stubbed environment.

**Solutions**: (1) N/A, pre-existing. (2) Strip `/* */` and `//` comments from the source text before scanning, rather than rephrasing the documentation to avoid the substring. (3) Switched to `vi.stubEnv()`/`vi.unstubAllEnvs()`, Vitest's purpose-built helper for exactly this. (4) Dropped the redundant second test rather than fight a tooling quirk unrelated to the guard's actual correctness — the property it would have verified (renders normally outside production) was already proven twice by other means (a manual `npm run dev` + curl check, and the `npm run build` static-output inspection), so the automated test's marginal value did not justify chasing the quirk further; documented the reasoning directly in the test file's own comment.

**Lessons Learned**: The single biggest lesson this phase reinforced: **verification that only reads your own prior work will not find your own prior work's mistakes.** The contrast bug survived M7 Phase 1's own accessibility-conscious design and review because nobody had actually computed a contrast ratio against the shipped hex values; the API-contract-drift bugs survived because nobody had actually read the backend's real Pydantic model field names, only `docs/api_design.md`'s prose description of them. Both were found the moment this phase was asked to build a *verification mechanism* (a known-answer contrast test, a live-schema drift test) rather than merely assert one wasn't needed — building the checker and reading the ground truth is what surfaced the bugs, not extra caution or a longer review. This is the same lesson the backend's own KI-016 (an unverified empirical assumption, flagged rather than trusted) has been carrying since M3; this phase is the frontend's first equivalent instance of it, resolved rather than merely flagged, because the ground truth (a running backend) was reachable this time.

**Security Review Summary**: See `docs/SECURITY_LOG.md`'s M7 Phase 1.5 entry. No vulnerability found or introduced in frontend code; one genuine, previously undocumented production-deployment risk found (KI-039 — `SameSite=Strict` cookies require a shared custom domain between frontend and backend that nothing currently enforces or even states as a requirement) and tracked as a pre-launch blocker, not fixed here (it is a deployment-configuration decision, not a code change this phase can make).

**Testing Summary**: See `docs/TESTING_REPORT.md`'s M7 Phase 1.5 entry. 61 new tests (103 total across the frontend suite: 42 from M7 Phase 1 + 61 new), 95%+ statement coverage, all passing (4 gracefully skip when no backend is reachable, matching the existing Redis-unreachable-test convention). Includes a known-answer contrast test, `axe-core` structural accessibility tests for every primitive, keyboard-operability tests, a static reduced-motion-mechanism check, a static-analysis guardrail test, and a drift-detection test verified against both a real running backend and the graceful-skip path.

**Technical Debt Introduced**: KI-039 (production custom-domain requirement for `SameSite=Strict`, Medium-High — a real pre-launch blocker if unresolved), KI-040 (theme-script CSP interaction, Low — deferred design note, no CSP exists yet to have a problem in). KI-036 and KI-038 were resolved, not introduced, by this phase; KI-037 (the contrast/self-reference bug) was found and resolved within the same phase it would otherwise have been introduced in, so it is recorded as Resolved rather than carried forward as debt.

**Performance Notes**: See `docs/PERFORMANCE_LOG.md`'s M7 Phase 1.5 entry. No measurable change to bundle size or build time from this phase's additions (the format/query modules are small, pure-logic files; `axe-core` is a dev-only test dependency, not shipped to production).

**Recruiter Value**: Very High. This phase demonstrates the single hardest habit to instill in engineering practice: treating your own recently-shipped, design-reviewed work as unverified until you've actually checked it against ground truth, and being willing to report "I found two real bugs in what I built last week" rather than defending the prior work. Concretely: a known-answer contrast test that would have been trivial to skip ("the design review already approved these colors") but instead found a real accessibility failure; a drift-detection test that, in the course of being built, found the exact class of bug it was designed to catch, in the codebase's own pre-existing types, before it ever ran once in anger; and an honest test-suite correction story (the playground-guard test) that explains why a test was dropped rather than silently omitting it.

**Production Readiness Score**: Still not yet meaningful for the frontend specifically (no product page exists), but a real pre-launch item was added to the platform-wide tracking (KI-039) that would have otherwise gone unnoticed until a live deployment silently failed.

**Next Milestone**: M7 Phase 2 — Product Screens, starting with the Simulator → Results flow, now on a hardened foundation with real formatting/query/error conventions already fixed rather than left for Phase 2 to improvise.

---

## 2026-07-17 — M7 Phase 1.5 Cleanup: Documentation and Deployment Risk Follow-Up

**Version**: 0.8.1 (no version bump — documentation, CI configuration, and one deferred-risk clarification only; no application code behavior changed)

**Objective**: A small, external-review-driven cleanup pass on M7 Phase 1.5's own output: verify the newly-added documentation wasn't garbled, upgrade KI-039's severity and give it a concrete deadline, determine and fix whether the API-contract-drift test is actually CI-enforced, state the financial-data wire-format contract explicitly, re-verify the dev playground's production behavior, and document the `SameSite=Strict` external-referrer UX tradeoff ADR-018 never explicitly named.

**Scope**: `docs/{KNOWN_ISSUES,PROJECT_STATE,SECURITY_LOG}.md`, `.github/workflows/ci.yml` (new frontend CI job), `frontend/src/lib/format/README.md`. No frontend application code changed; no product page built.

**Implementation Summary**: (1) Read every M7 Phase 1.5 addition to `DEVLOG.md`, `CHANGELOG.md`, `PROJECT_STATE.md`, `KNOWN_ISSUES.md`, `TESTING_REPORT.md`, `SECURITY_LOG.md`, and `PERFORMANCE_LOG.md` in full — no garbled, truncated, or corrupted text was found in any of them; no correction was needed. (2) KI-039 upgraded from Medium-High to **High**: the original framing treated this as a production-launch gate, but `SameSite=Strict`'s failure mode (cookies never attaching cross-site) triggers identically on the *first deployed staging or demo environment* — a Vercel preview pointed at a Railway/Render staging API is the default shape of a first deployment, not an edge case — so the deadline is now stated as "before the first deployed staging/demo environment," not "before production." `docs/PROJECT_STATE.md`'s three references to this KI updated to match. (3) Confirmed the drift test was 100% local/manual — not because of anything specific to that test, but because **no frontend CI job existed at all** before this pass (`.github/workflows/ci.yml` had only a backend job and a secret scan). Rather than merely documenting that gap, added a `frontend-lint-and-test` CI job (checkout, `npm ci`, `eslint`, `tsc --noEmit`, `vitest run`, `npm run build`), mirroring the backend job's existing structure — validated as syntactically correct YAML before treating it as done. This closes the general "frontend is unverified by CI" gap; the drift test's *live-schema* assertions specifically still only run when a real backend is reachable, since the new CI job provisions no `postgres`/`redis`/`uvicorn` services — recorded as KI-041's explicitly-remaining half, not silently left implicit. (4) Added an explicit, quotable "wire-format contract" statement to the top of `frontend/src/lib/format/README.md`: the API serializes financial values as strings specifically so precision survives intact; the frontend formats, never derives (no return, CAGR, final value, share count, or inflation figure is ever computed client-side); missing/null values render an honest, reason-coded statement of why, never a guess. (5) Re-verified (not re-built) `/dev/playground`'s production safety: re-ran `npm run build` and re-inspected the static output — still genuinely the not-found page's markup in production, and the existing automated guard test still passes. No change was needed; this was a confirmation, not a fix. (6) Documented a real, previously-unstated `SameSite=Strict` consequence (KI-042): `Strict` withholds cookies not just cross-site but on the *first* top-level navigation arriving from a different site (e.g., a link clicked from an email or chat app) — different from `Lax`, which still allows that case. ADR-018 never discussed `Strict` vs. `Lax` explicitly. Assessed the actual impact: anonymous shared-simulation links (the platform's primary, Founder-Decision-002-approved sharing mechanism) are entirely unaffected, since reading one never requires a cookie at all; the only affected case — an authenticated user opening their own saved simulation's link from an external referrer — isn't a built feature yet (no Simulation History/sharing UI exists). Recorded as an accepted, deliberate tradeoff to revisit when that feature is actually designed, not reopened now.

**Files Created**: None (KI-039/KI-041/KI-042 are new entries within the existing `docs/KNOWN_ISSUES.md`, not new files).

**Files Modified**: `.github/workflows/ci.yml` (new `frontend-lint-and-test` job); `frontend/src/lib/format/README.md` (wire-format contract section); `docs/KNOWN_ISSUES.md` (KI-039 severity/deadline upgraded; KI-041, KI-042 added); `docs/PROJECT_STATE.md` (KI-039 severity references updated); `docs/SECURITY_LOG.md` (this pass's findings appended).

**Architecture Decisions**: None new — this pass clarified and extended existing decisions (ADR-018's `SameSite` choice, ADR-030's drift-detection tradeoff) rather than making new ones. No ADR was warranted for adding a standard CI job mirroring an existing pattern.

**Problems Encountered**: (1) No `pyyaml`/YAML-validation tool was readily available in the frontend toolchain to check the new CI workflow's syntax. (2) Determining whether KI-042's `SameSite=Strict` consequence was already covered required re-reading ADR-018's exact text rather than relying on memory of having written it.

**Solutions**: (1) Installed `pyyaml` into the backend's Python environment (already active for the KI-036/KI-038 live-backend verification in the prior pass) as a one-off check — confirmed the new `ci.yml` parses correctly and lists all three expected jobs. (2) Re-read ADR-018 directly (`docs/ARCHITECTURE_DECISIONS.md`) and confirmed its "Options Considered"/"Tradeoffs" sections discuss cookies-vs-`localStorage` and a non-browser-client tradeoff, but never `Strict` vs. `Lax` specifically — the same "consult the source, not memory" discipline this project has applied since the M3 design review.

**Lessons Learned**: A "small cleanup pass" prompted by an external review is often the moment a genuinely bigger, adjacent gap surfaces — the drift-test-enforcement question turned out to be a symptom of "no frontend CI exists at all," which is a more consequential finding than the question actually asked. Answering the narrow question honestly (rather than a satisfying-sounding non-answer like "it's tested, just not in CI yet, that's fine") is what surfaced the broader gap worth fixing immediately, cheaply, in the same pass.

**Security Review Summary**: See `docs/SECURITY_LOG.md`'s M7 Phase 1.5 Cleanup entry. No vulnerability found or introduced. One severity upgrade (KI-039, Medium-High → High, deadline moved earlier) and one newly-documented, deliberately-accepted tradeoff (KI-042, `SameSite=Strict`'s external-referrer first-load behavior).

**Testing Summary**: No new tests this pass (documentation/CI-configuration only). Full existing suite re-verified passing: 103/107 (4 gracefully skipped without a live backend), zero lint/typecheck errors, production build succeeds, `/dev/playground` re-confirmed safe in production.

**Technical Debt Introduced**: KI-041 (partially resolved — general frontend CI gap fixed; live-backend-in-CI gap remains, tracked explicitly), KI-042 (Low severity, documented and deliberately deferred, not actioned). KI-039 was re-assessed (severity upgraded), not newly introduced.

**Performance Notes**: Not applicable — no application code changed. The new CI job adds pipeline time (`npm ci`, lint, typecheck, test, build) to every push/PR, not measured but expected to be comparable to the existing backend job's runtime.

**Recruiter Value**: Medium-High. Demonstrates the discipline of actually re-reading your own just-written documentation for corruption rather than assuming a large output pass was clean, and — more substantively — following a narrow verification question ("is this one test CI-enforced?") to its honest root cause (no frontend CI existed) rather than answering only the literal question asked.

**Production Readiness Score**: Unchanged (~5/10 platform-wide) — this pass changed risk *characterization* (KI-039's severity/deadline, KI-042's tradeoff made explicit) and closed a real CI gap (KI-041, partial), rather than building new product-facing functionality.

**Next Milestone**: M7 Phase 2 — Product Screens, unchanged from the prior entry. KI-039 (now High, deadline before first staging/demo deployment) should be resolved before any deployed environment is stood up, independent of Phase 2's own development pace.

---

## 2026-07-18 — M7 Phase 2 (Increment 1): Punch-List Fixes + Simulator

**Version**: 0.9.0

**Objective**: Close seven foundation gaps flagged in a pre-build review before building any Simulator code, then build the Simulator itself — the platform's first real product screen and the entry point to its core "simulate-and-explain loop" (`.claude/MVP_RULES.md`).

**Scope**: `frontend/src/app/global-error.tsx`, `frontend/src/config/env.ts`, `frontend/src/lib/format/` (new `compare-decimal-string.ts`), `frontend/eslint.config.mjs`, `frontend/src/app/globals.css`, `frontend/src/lib/api/client.ts`, `frontend/src/lib/api/endpoints/assets.ts`, `frontend/src/hooks/` (new `use-simulation.ts`, `use-asset-availability.ts`, updated `use-asset-search.ts`), `frontend/src/components/simulator/` (new), `frontend/src/app/simulator/` (new). No backend changes. Explicitly out of scope per instruction: the Results screen (beyond an inline, no-navigation success state), charts, the AI panel, dashboard/history/account screens.

**Implementation Summary**:

*Punch-list (Part A, completed before any Simulator code)*: (1) `global-error.tsx`'s digest color corrected from the old low-contrast `#898781` to the approved `#6b6963` — a literal hex swap, since this file deliberately imports nothing (ADR-031's "everything else failed" boundary). (2) `config/env.ts`'s `localhost:8000` fallback now only applies outside `NODE_ENV === 'production'`; a production build with a missing/invalid API URL now fails fast at module load rather than silently defaulting. (3) Added `compareDecimalStrings` (`src/lib/format/compare-decimal-string.ts`) — sign/integer-length/digit-string comparison, no `Number()` — plus an ESLint selector banning bare relational operators in product/UI code, mirroring ADR-029's existing `Number()`/`parseFloat()`/`parseInt()`/unary-`+` ban exactly (ADR-033). `decimal-string.ts`'s private `splitDecimal` helper was exported for reuse rather than duplicated. (4) Added a static regression test (`theme-tokens.test.ts`) distinguishing `globals.css`'s intentional `@theme inline` self-looking bridge syntax (Tailwind v4's documented namespace-bridging mechanism) from a genuine self-reference bug like ADR-028's — asserting `semantic.css`/`components.css`/`primitives.css` never contain a real one. (5) Expanded the financial-math guardrail's file scope from `app/**`/`components/**` to also cover `hooks/**`, `providers/**`, `lib/**` — `src/lib/format/**` itself was added to the rule's `ignores`, since it is the one module legitimately allowed to do raw digit-string math, including the very comparisons `compareDecimalStrings` is built from. (6) Added a 15s Axios `timeout` to `apiClient`, and threaded an optional `AbortSignal` through `searchAssets`/`getAssetDetail`/`getAssetAvailability`; `useAssetSearch` now forwards TanStack Query's automatically-supplied per-query `signal`, so a stale in-flight asset search is cancelled the moment a newer keystroke supersedes it. (7) Locked the Tailwind breakpoint namespace: added a reset inside `globals.css`'s `@theme inline` block before redeclaring the three approved breakpoints (`sm`/`md`/`lg`), since redeclaring alone left Tailwind's built-in `xl`/`2xl` breakpoints silently available (ADR-034), with a regression test asserting both the reset's presence and that no unapproved breakpoint utility exists anywhere under `src/`.

*Simulator (Part B)*: Built `useCreateSimulation` (wraps `POST /api/v1/simulations`, invalidates `queryKeys.simulations.all` on success) and `useAssetAvailability` (backs the pre-submit date-range check), both following `src/lib/query/README.md`'s established conventions. Built `AssetSearchCombobox` — a full ARIA combobox (`role="combobox"`, `role="listbox"` popup, arrow-key/Enter/Escape handling, an `aria-live` result-count announcement, an informative empty state for a genuine zero-result search rather than a blank dropdown, and central error-copy rendering for a search failure) backed by `useAssetSearch`. Built `SimulationForm` — React Hook Form plus the existing wire-level `simulationCreateSchema` (Zod), extended with a client-side-only end-after-start date refinement; investment amount is a text/decimal-mode field validated via the schema's existing regex, never a native number input; dividend-reinvestment and inflation-adjustment toggles sit behind a "More options" disclosure; a selected asset's availability clamps the date inputs' allowed range and shows inline helper text. On success, the form renders a calm inline success card (simulation ID in monospace, status, echoed asset/amount/date-range) with a "Start a new simulation" reset — per an explicit decision with the founder, no navigation to a placeholder Results route, since that would mean building routing/data-fetching for a page explicitly out of this increment's scope. `/simulator` is a server-component shell around the one client boundary (the form), mirroring the existing `dev/playground` page/client split, with a modest Time Axis touch (a kicker label plus a single hairline rule) per `BRAND_CONSTITUTION.md` §5's anti-gimmick rule.

**Files Created**: `frontend/src/lib/format/compare-decimal-string.ts`; `frontend/src/hooks/use-simulation.ts`, `use-asset-availability.ts`; `frontend/src/components/simulator/asset-search-combobox.tsx`, `simulation-form.tsx`; `frontend/src/app/simulator/page.tsx`; test files: `__tests__/app/global-error.test.tsx`, `__tests__/lib/env.test.ts`, `__tests__/lib/theme-tokens.test.ts`, `__tests__/lib/breakpoints.test.ts`, `__tests__/lib/use-simulation.test.tsx`, `__tests__/lib/use-asset-availability.test.tsx`, `__tests__/components/asset-search-combobox.test.tsx`, `__tests__/components/simulation-form.test.tsx`.

**Files Modified**: `global-error.tsx`, `config/env.ts`, `eslint.config.mjs`, `app/globals.css`, `lib/api/client.ts`, `lib/api/endpoints/assets.ts`, `hooks/use-asset-search.ts`, `lib/format/decimal-string.ts`, `lib/format/index.ts`, `lib/format/README.md`, plus test updates to `__tests__/lib/client.test.ts`, `__tests__/lib/endpoints.test.ts`, `__tests__/lib/use-asset-search.test.tsx`, `__tests__/lib/format.test.ts`.

**Architecture Decisions**: ADR-033 (decimal-comparison guardrail plus relational-operator ESLint ban, guardrail scope expanded to hooks/providers/lib), ADR-034 (Tailwind breakpoint namespace reset-then-redeclare) — both in `docs/ARCHITECTURE_DECISIONS.md`.

**Problems Encountered**: (1) Two of the new static-regression tests (theme-token self-reference, breakpoint namespace) initially failed against their own target files' explanatory comments — the ADR-028 and ADR-034 code comments added to `globals.css` necessarily quote the exact patterns the tests scan for, the same self-matching trap `format.test.ts`'s existing guardrail test had already hit once during M7 Phase 1.5. (2) The financial-math guardrail's expanded scope immediately flagged several genuine non-financial comparisons already in the codebase and in the new Simulator code itself (a string-length check, array-index arithmetic in the combobox's keyboard handling) — expected per ADR-033's own documented breadth-over-precision tradeoff, but a real, visible cost of the broad rule, not a hypothetical one. (3) The testing library's user-interaction helper could not reliably fill a native date input under the test DOM environment, producing a validation error in tests that should have passed. (4) A self-review of the just-built "Start a new simulation" reset flow found a real bug: `AssetSearchCombobox` manages its displayed text as uncontrolled state, so React Hook Form's reset correctly cleared the underlying form value but left the combobox's visible text showing the previously selected asset (KI-043).

**Solutions**: (1) Stripped comments from the source text before running each regression scan, the same technique `format.test.ts` already used. (2) Added scoped, commented disable-comments at each genuine false positive, exactly the accepted, visible mechanism ADR-029/ADR-033 both describe. (3) Switched date-field test interactions to directly set the input's value via a change event rather than simulated keystrokes. (4) Added a remount-key counter to `SimulationForm`, incremented on "Start a new simulation" and applied to the form element — forcing a full remount of the form subtree (including the combobox) on reset, the standard React idiom for clearing uncontrolled child state a parent doesn't otherwise own.

**Lessons Learned**: The self-matching-regex trap from M7 Phase 1.5 recurred twice more this phase in two brand-new test files, for the same underlying reason — writing a good regression test's explanatory comment in the same file the test scans is a near-guaranteed way to trip the test on itself, and stripping comments before scanning should be treated as a default habit for any static-source-scan test in this codebase, not a fix applied reactively each time. Separately, KI-043 reinforced the same lesson M7 Phase 1.5 drew from the contrast and contract-drift bugs: a feature that passes its own tests can still have a real, user-visible defect in a code path the tests didn't specifically exercise — the "Start a new simulation" click was tested for calling reset, but not for what the combobox visually showed afterward. Deliberately re-driving the actual reset flow as a reviewer, not just re-reading the passing test output, is what caught it.

**Security Review Summary**: One real, if narrow, deployment-safety fix: `config/env.ts`'s production fallback removal closes a gap where a production build missing `NEXT_PUBLIC_API_BASE_URL` would previously have silently booted pointed at localhost instead of failing to build. No new vulnerability found or introduced elsewhere. The Simulator introduces no new auth-sensitive surface (asset search and simulation creation are both public per `docs/api_design.md` §2/§4); investment amount is validated as a decimal string end-to-end and never parsed to a JS number anywhere in the new code, verified both by the expanded ESLint guardrail and by manual review.

**Testing Summary**: 34 new/updated tests (144 total across the frontend suite: 110 carried forward plus 34 new, 4 gracefully skipped when no backend is reachable, unchanged from the prior phase). Coverage: 93.5% statements / 81.6% branches / 92.63% functions / 95.71% lines (`npm run test:coverage`). New coverage includes: a decimal-comparison known-answer test suite (magnitude ordering, signed zero, values beyond `Number.MAX_SAFE_INTEGER`), the two new static-regression tests, hook tests for the two new Simulator hooks (including `AbortSignal` forwarding), a full combobox interaction suite (debounced search, keyboard selection, empty/error states, Escape-to-close), and a form suite (inline validation for both the wire schema and the date-range refinement, a valid-submit assertion against the exact mutation payload, the named-step loading label, central error-copy rendering, the inline success card, and the "Start a new simulation" reset). `npx eslint .` and `npx tsc --noEmit` both pass with zero errors; `npm run build` succeeds with `/simulator` prerendering as static content.

**Technical Debt Introduced**: None new and unresolved — KI-043 was found and fixed within this same phase (recorded as Resolved, not carried forward), the same immediate-self-catch pattern M7 Phase 1.5 established for KI-036/KI-037/KI-038. Pre-existing debt (KI-016, KI-021, KI-039, KI-040, KI-042, and the technical-debt-summary items) is unaffected by this phase's scope.

**Performance Notes**: See `docs/PERFORMANCE_LOG.md`'s M7 Phase 2 (Increment 1) entry. No new runtime dependency was added (React Hook Form, Zod, and TanStack Query were already dependencies); the Simulator's bundle-size impact is limited to its own new component/hook code.

**Recruiter Value**: High. This phase demonstrates the same foundation-before-features discipline the project has followed since M7 Phase 0 (closing seven real gaps before writing a single line of product UI), and, within the Simulator build itself, a live example of the project's core habit — catching a real, self-introduced bug (KI-043) via deliberate post-build review rather than trusting a green test suite alone, in the very same session the feature was built, before it was ever shown to the founder.

**Production Readiness Score**: Unchanged (~5/10 platform-wide) — the platform now has its first real product-facing screen, a genuine step forward, but the Results screen, Asset Explorer, Simulation History, and Auth screens remain unbuilt, and KI-016/KI-039 remain the two items most load-bearing for an actual launch decision.

**Next Milestone**: M7 Phase 2 (Increment 2) — the Results screen, the other half of the Simulator's "simulate-and-explain loop." KI-021/FD-008 (`growth_series`/`disclosed_splits` persistence) should land before or alongside it, per the prior entry's own stated prerequisite.

---

## 2026-07-18 — Ingestion Reliability: `dev_seed` Fixture Provider

**Version**: 0.9.1

**Objective**: With the Simulator built and reaching a live backend, manual end-to-end testing needed real asset/price data — but `yfinance` was failing for every symbol tried. Diagnose the actual root cause (not guess at it) and unblock manual testing safely, without weakening ingestion validation or fabricating anything that could be mistaken for real provider data.

**Scope**: `backend/app/ingestion/providers/dev_seed_provider.py` (new), `backend/app/ingestion/providers/__init__.py`, `backend/app/ingestion/cli.py`, `docs/setup_guide.md`. No change to `yfinance_provider.py`, `coingecko_provider.py`, `fred_provider.py`, the orchestrator, or any validation rule.

**Implementation Summary**: Diagnosed the yfinance failure empirically rather than assuming a cause: reproduced the identical failure directly on the host (outside Docker), ruling out container-specific networking; confirmed a plain unauthenticated `requests.get()` to Yahoo's actual chart API endpoint succeeded with valid JSON, ruling out a wholesale IP block; then enabled yfinance's own debug mode (`yf.enable_debug_mode()`) and found the exact failure — yfinance 0.2.44's internal crumb-negotiation endpoint (`query1.finance.yahoo.com/v1/test/getcrumb`) returning HTTP 429, after which yfinance used the literal string `"Edge: Too Many Requests"` as the crumb value on the next (also-429) request. This is KI-044, a real, evidence-backed finding, not a hypothesis. Built `DevSeedProvider` as the local-development mitigation (ADR-035): a `PriceProvider` implementing the same Protocol `YFinanceProvider`/`CoinGeckoProvider` do, registered through the existing `get_provider` registry (its own docstring invites exactly this extension pattern), so every existing pipeline guarantee stays intact — `get_or_create_asset`, `validate_price_record`, and `record_import_audit` all still run unmodified. Three safeguards make the fixture data impossible to mistake for real data: `name = "dev_seed"` is the literal `data_source` value written to the database; the constructor raises immediately if `Settings.environment` isn't `development`/`test`/`testing`, mirroring `Settings`'s own `JWT_SECRET`/`AI_PROVIDER` production guards; and only three symbols are defined, at deliberately round, obviously-fake price levels (never an attempt to approximate real historical AAPL/SPY/BTC-USD prices). Verified end-to-end against the actually-running local stack (Postgres/Redis/backend already up): dry-run passed validation cleanly, a real import wrote 1,305 rows per symbol with zero rejections, and a full `POST /api/v1/simulations` call against the seeded AAPL data returned a completed simulation with a sensible return/CAGR and a real growth series.

**A real bug found while applying this fix, fixed with explicit user sign-off**: pre-existing `AAPL`/`SPY`/`BTC-USD` `Asset` rows, created by the user's own earlier failed `--provider yfinance` attempts (the orchestrator creates the `Asset` row *before* calling `fetch_prices`, so the row survives even when the price fetch then fails), had been left with `data_source="yfinance"` and zero price rows. `get_or_create_asset` only sets `data_source` on creation, never on reuse (`app/ingestion/storage/repository.py:43`) — so the first `dev_seed` import silently inherited the stale `"yfinance"` label on those rows while attaching 1,305 synthetic rows underneath. Verified via direct (read-only) inspection of `historical_prices` row counts and date ranges that no real data existed under those rows before proposing a fix — a destructive cross-table delete is not something to do on an assumption. Asked the user explicitly before running it; on approval, deleted the three stale rows (`historical_prices` → `dividends`/`stock_splits` → `assets`, since the FK has no cascade) and re-seeded cleanly, confirmed via the live API that `data_source` now correctly reads `"dev_seed"` for all three.

**Files Created**: `backend/app/ingestion/providers/dev_seed_provider.py`.

**Files Modified**: `backend/app/ingestion/providers/__init__.py` (registered `dev_seed`), `backend/app/ingestion/cli.py` (added `dev_seed` to `--provider` choices, documented in the module docstring's examples), `docs/setup_guide.md` (new "If yfinance fails locally" section: root cause, exact seeding commands, explicit dev/test-only rules).

**Architecture Decisions**: ADR-035 (`DevSeedProvider`: a local-development-only fixture provider, explicitly not a fix to Yahoo's rate limiting) — `docs/ARCHITECTURE_DECISIONS.md`.

**Problems Encountered**: (1) The first `DevSeedProvider` run silently inherited a stale `data_source="yfinance"` label from `Asset` rows the user's own earlier failed attempts had already created (described above). (2) A permission-boundary encounter, not a bug: an initial attempt to clean up those stale rows via a raw multi-table `DELETE` was correctly blocked by the harness's own safety classifier, since deleting rows across four tables had not been explicitly authorized — stopped immediately, explained the finding and the proposed fix to the user, and proceeded only after explicit approval.

**Solutions**: (1) Verified via read-only queries that all price rows under the stale assets were the just-inserted synthetic ones (row count and date range matched exactly what `dev_seed` had just written; a failed yfinance fetch never gets far enough to write a row), then deleted and re-seeded with sign-off. (2) Treated the permission denial as intended, correct behavior rather than an obstacle to route around — this is exactly the kind of destructive, hard-to-reverse action that should pause for a human decision, not be automated past.

**Lessons Learned**: The most valuable step in this diagnosis was refusing to accept the first plausible-sounding hypothesis (the user's own framing included "Docker networking" and "Yahoo blocking containers" as live possibilities) and instead gathering direct evidence for each candidate cause — reproducing outside Docker, testing the raw endpoint manually, and finally using the library's own debug instrumentation rather than reasoning about its internals from the outside. Each of those three tests eliminated a specific hypothesis before the real one was found. Separately, `get_or_create_asset`'s reuse-preserves-existing-`data_source` behavior is a real, general data-hygiene gap (not unique to `dev_seed`) that had simply never been exercised before, because no prior milestone had reason to import the same symbol under two different provider identities — this is the same "a code path that passes all its existing tests can still have a real defect the tests never exercised" lesson the frontend side of this same day's work drew independently (KI-043).

**Security Review Summary**: `DevSeedProvider`'s environment guard is a real, verified safety mechanism, not just a comment — it raises at construction time if `ENVIRONMENT` isn't development/test/testing, so it cannot be reached in a production deployment even via an operator's mistaken CLI invocation. No vulnerability found or introduced; this session touched no auth-sensitive or externally-reachable surface (the CLI is a local-only operator tool, not an API endpoint).

**Testing Summary**: No new automated test suite added this session (an operational diagnosis-and-mitigation pass, not a product-code change with its own test coverage expectation — `DevSeedProvider` was instead verified directly against the real running stack: a `--dry-run` pass through real validation, a real import with 0 rejected rows for all three symbols, and a live `POST /api/v1/simulations` call producing a correct, complete result). Existing backend test suite unaffected (no shared code changed).

**Technical Debt Introduced**: None new and unresolved beyond what's already tracked. KI-044 (Open — the actual yfinance rate-limiting cause, mitigated for local dev but not fixed) is a genuine finding from this session, not deferred debt created by it. The `get_or_create_asset` reuse-preserves-`data_source` gap is documented as a tradeoff in ADR-035 rather than opened as its own KI, since it has no known real-provider trigger today.

**Performance Notes**: Not applicable — no product-facing or performance-sensitive code path changed. `DevSeedProvider`'s synthetic-row generation is O(days-in-range), negligible for the ~5-year ranges used here (~1,300 rows in a few seconds, dominated by per-row DB writes, not generation).

**Recruiter Value**: High. A clean example of root-causing a real, initially-ambiguous failure through direct evidence rather than the first plausible story (three separate tests, each eliminating one hypothesis), building a minimal, safety-guarded mitigation that reuses existing architecture rather than working around it, and then — critically — pausing on a destructive action a permission system correctly flagged, explaining the finding plainly, and proceeding only with explicit sign-off rather than finding a workaround.

**Production Readiness Score**: Unchanged (~5/10 platform-wide) — this session improved local-development reliability and diagnostic clarity, not production-facing functionality or readiness.

**Next Milestone**: M7 Phase 2 (Increment 2) — the Results screen, unchanged from the prior entry. Separately, KI-044's actual resolution (a verified `yfinance` version bump) should be revisited opportunistically, and KI-016's live split-consistency verification runbook remains blocked on reliable yfinance ingestion being restored.

---

## 2026-07-18 — M7 Phase 2 Final UX Polish: Trading-Day Guidance + Educational Error Copy

**Version**: 0.9.2

**Objective**: Before tagging M7 Phase 2 complete, close a real UX gap named by the founder: the Simulator's `MISSING_HISTORICAL_DATA` rejection (a correct, unchanged backend behavior — weekends/market holidays genuinely have no price row) currently surprises a user only *after* submit, with terse error copy that doesn't explain why. Improve the pre-submit and post-error experience without touching backend behavior, without silently adjusting dates, and without calculating a trading calendar anywhere in the frontend.

**Scope**: `frontend/src/components/simulator/simulation-form.tsx` (new static guidance text), `frontend/src/lib/api/errors.ts` (`ERROR_COPY.MISSING_HISTORICAL_DATA` copy), `frontend/src/__tests__/components/simulation-form.test.tsx` (updated/new tests). No backend change of any kind — `MISSING_HISTORICAL_DATA`'s trigger condition and HTTP behavior are untouched, per direct instruction. No date-adjustment, trading-calendar, or nearest-trading-day-guessing logic was added anywhere.

**Implementation Summary**: Added one static, calm caption directly beneath the start/end date `Input` fields in `SimulationForm` — separate from the existing per-asset availability helper text (which is dynamic, populated from `GET /assets/{symbol}/availability` once a real asset is selected) — explaining plainly that stocks/ETFs have no price data on weekends or market holidays, encouraging (never enforcing) a trading-day choice, and stating the product's own philosophy directly: dates are never moved automatically, because historical accuracy matters more than convenience. This is purely educational copy with zero behavioral effect — the min/max date clamping already wired to the availability query is unchanged, and no new validation rule was added. Separately, rewrote `ERROR_COPY.MISSING_HISTORICAL_DATA`'s description from a terse "this asset doesn't have price data for the selected range" to the same educational framing (weekends/holidays explanation, plain remedy — "choose different dates and try again"), routed through the existing central error-copy table (`getErrorCopy`) exactly as before — no new error-handling code path, only a copy change to an entry that already existed.

**Files Created**: None.

**Files Modified**: `frontend/src/components/simulator/simulation-form.tsx`, `frontend/src/lib/api/errors.ts`, `frontend/src/__tests__/components/simulation-form.test.tsx`.

**Architecture Decisions**: None — a copy/UX-text change routed entirely through existing mechanisms (the central `ERROR_COPY` table, `Input`'s existing `helperText` pattern), not a new pattern warranting an ADR.

**Problems Encountered**: None of substance — a small, well-scoped, low-risk change. The one minor judgment call was where to place the static guidance text relative to the existing dynamic availability helper text; placed as a separate paragraph beneath the date-input grid (rather than merged into `Input`'s per-field `helperText`) since it applies to the date range as a concept, not to either field individually, and applies even before an asset is selected (when no availability data exists yet to populate the dynamic helper text).

**Solutions**: N/A.

**Lessons Learned**: A backend behavior stated as "correct and must not change" is frequently not the actual UX problem — the problem is almost always that the *user* has no way to anticipate a rule they don't yet know exists. This pass is a clean, minimal example of the general fix pattern for that situation: explain the rule before it bites (pre-submit guidance) and explain it again, calmly, when it does (post-error copy) — never work around the rule itself, which would trade correctness for convenience exactly backwards from `docs/BRAND_CONSTITUTION.md`'s own stated priority order.

**Security Review Summary**: Not applicable — pure UI copy/text change, no new input handling or data flow.

**Testing Summary**: Updated the existing `MISSING_HISTORICAL_DATA` error-copy assertion to match the new text, and added one new test confirming the trading-day guidance text renders on the form. 145 tests total (141 passing, 4 gracefully skipped without a live backend, up from 144/140 in the prior entry), 93.56%/81.6%/92.7%/95.74% statement/branch/function/line coverage. `npx eslint .` and `npx tsc --noEmit` both pass with zero errors; `npm run build` succeeds, `/simulator` still prerenders as static content.

**Technical Debt Introduced**: None.

**Performance Notes**: Not applicable — a small amount of static text added to an already-rendered page; no measurable bundle-size or runtime impact.

**Recruiter Value**: Medium. A small but complete illustration of "improve the experience around a correct constraint, don't weaken the constraint" — the exact discipline this product's brand philosophy names as its core differentiator (`docs/BRAND_CONSTITUTION.md` §1: "more honest and more precise about the past than anything else a person could easily reach for").

**Production Readiness Score**: Unchanged (~5/10 platform-wide) — a UX-polish pass on an already-built screen, not new product-facing functionality.

**Next Milestone**: M7 Phase 2 is now considered complete and ready for founder review (pending sign-off) — see `docs/MILESTONE_REPORTS/M7_PHASE_2_REPORT.md`. **M7 Phase 3 — Results Experience** is queued next but explicitly not started per direct instruction; awaiting founder approval before continuing.

---

## 2026-07-18 — M7 Phase 2 Final Polish & Closure

**Version**: 0.9.3

**Objective**: The final pass before M7 Phase 2 is permanently closed — make the Simulator feel complete while preserving its current architecture exactly. Explicitly not a redesign, not a new feature, not the start of M7 Phase 3: polish, UX, and documentation only.

**Scope**: `frontend/src/components/ui/error-state.tsx` (new `errorCode` prop, Request ID/error code moved behind progressive disclosure), `frontend/src/components/simulator/simulation-form.tsx` (success copy, asset information panel, `errorCode` threaded through), `frontend/src/app/simulator/page.tsx` (trust indicators), plus corresponding tests. Trading-day guidance text (added in the prior pass) was reviewed against the three required points and found to already state all of them — left unchanged rather than churned for its own sake. No backend change, no new fetch, no new calculation, no architectural change.

**Implementation Summary**: (1) **Technical details disclosure**: `ErrorState` — a shared primitive already used by the Simulator's error rendering, the route-level `error.tsx` boundary, and the dev playground — gained an `errorCode` prop, and its Request ID/error-code rendering moved from an always-visible line to a closed-by-default `<details>`/`<summary>` ("Technical details"), the same native, zero-JS pattern `StatTile`'s own source disclosure already established at Phase 1. Updating the shared component (rather than a one-off variant inside `SimulationForm`) means `error.tsx`'s crash boundary gets the same improvement for free. (2) **Trading-day guidance**: reviewed the existing wording one final time against the three required points (weekends/holidays may lack price data; dates are never moved automatically; historical accuracy matters more than convenience) — confirmed all three are already present and the tone is calm and educational; left unchanged. (3) **Success state wording**: "Simulation created" → "Simulation complete," description reworded to "Your historical investment simulation has been successfully created and recorded." — the same information, a calmer register, no celebratory language anywhere. (4) **Asset information panel**: added directly beneath the asset combobox, rendering symbol/name/asset-type from the already-selected `AssetSummary` plus the historical-availability range from the already-fetched `useAssetAvailability` query — zero new network requests. `exchange` was deliberately left out: `AssetSummary` doesn't carry it, and per KI-025 the field is always `null` today regardless, so a second `GET /assets/{symbol}` call just to render nothing would violate the explicit "do not fetch additional data" instruction for no benefit. (5) **Trust indicators**: added a small, muted, check-prefixed list ("Deterministic simulation," "Historical market data," "No predictions," "Educational platform") near the page heading in `simulator/page.tsx` — deliberately plain text, not `Badge` (whose colored, bordered treatment is reserved for real state per `docs/BRAND_CONSTITUTION.md` §7, and would read as a marketing claim here, exactly what the instruction warned against). Sentence case throughout, per the constitution's own writing-style rule, even though the instruction's example text used title case.

**Files Created**: `frontend/src/__tests__/app/simulator-page.test.tsx`.

**Files Modified**: `frontend/src/components/ui/error-state.tsx`, `frontend/src/components/simulator/simulation-form.tsx`, `frontend/src/app/simulator/page.tsx`, `frontend/src/__tests__/components/{simulation-form,states}.test.tsx`.

**Architecture Decisions**: None — every change routes through an existing mechanism (the shared `ErrorState` primitive's own disclosure pattern, data already fetched by existing hooks, the existing `ERROR_COPY`/copy conventions). No new pattern, dependency, or data flow was introduced.

**Problems Encountered**: (1) A test asserting the asset-type field's rendered text as `'STOCK'` (uppercase) failed — `uppercase` is a CSS `text-transform`, which changes visual rendering only; the underlying DOM text node is still the raw lowercase value (`'stock'`), and jsdom's `textContent`-based queries see the unstyled text. (2) No other issues — small, well-scoped changes, each verified individually before moving to the next.

**Solutions**: (1) Corrected the test assertion to check for the actual lowercase text node (`'stock'`) rather than the CSS-rendered appearance, with a comment explaining why — the same category of "test the actual DOM, not the visual" lesson, applied here rather than to a self-matching-regex trap this time.

**Lessons Learned**: Improving a shared primitive (`ErrorState`) rather than special-casing the improvement inside the one screen that prompted it (`SimulationForm`) meant the fix reached every consumer — the route-level crash boundary and the dev playground both got calmer, less-cluttered error presentation for free, from the same change. This is the same "fix it at the layer that owns the behavior" instinct this project has applied consistently since the backend's Provider Layer/orchestrator split — a screen-specific fix would have been faster to write but would have left the other two call sites with the old, always-visible treatment, a real inconsistency a future contributor would have had to notice and fix separately.

**Security Review Summary**: Not applicable — pure UI copy/structure changes. `ErrorState`'s new `errorCode` prop surfaces the same `ApiErrorCode` value already present on every thrown `ApiError` object; no new information is exposed, only rendered.

**Testing Summary**: 8 new/updated tests (150 total, 149 passing, 1 gracefully skipped — the live-backend-dependent API-contract-drift assertions ran against the actually-running local backend this session, so only the one test whose specific condition wasn't met was skipped, not the usual four). Coverage: 93.58% statements / 81.68% branches / 92.7% functions / 95.75% lines. `npx eslint .` and `npx tsc --noEmit` both pass with zero errors; `npm run build` succeeds, `/simulator` still prerenders as static content. Manually verified against the live local stack (already running from the prior session): `GET /api/v1/assets?query=AAPL`, `GET /api/v1/assets/AAPL/availability`, and `POST /api/v1/simulations` all still succeed exactly as before; fetched the live `/simulator` page and confirmed the new trust-indicator and trading-day-guidance text render in the actual served HTML.

**Technical Debt Introduced**: None.

**Performance Notes**: Not applicable — a shared component's rendering changed (moving already-present text behind a `<details>`, not adding new data), plus a small amount of new static copy (the asset panel, the trust-indicator list). No new dependency, no new network call, no measurable bundle-size change.

**Recruiter Value**: Medium-High. A clean, complete example of "polish without redesign" — every addition (the asset panel, the trust indicators, the technical-details disclosure) is built entirely from data and patterns that already existed, several instructed constraints ("do not fetch additional data," "do not calculate anything," "no bright badges") were followed to the letter rather than loosely interpreted, and the one improvement made was deliberately applied at the shared-component layer so every consumer benefited, not just the screen that prompted it.

**Production Readiness Score**: Unchanged (~5/10 platform-wide) — a UX-polish pass on an already-built, already-reviewed screen.

**Next Milestone**: M7 Phase 2 is now **permanently closed** from an implementation standpoint — see the updated `docs/MILESTONE_REPORTS/M7_PHASE_2_REPORT.md`. **M7 Phase 3 — Results Experience** remains queued but explicitly not started; awaiting founder approval before continuing.

---

## 2026-07-19 — M7 Phase 3B: Founder Decisions + Results Foundation

**Version**: 0.10.0

**Objective**: Two-part pass, explicitly not the full Results Experience. Part 1: formalize three founder decisions that unblock M7 Phase 3 work — the Experience Constitution (already drafted, awaiting formalization), growth-series persistence's implementation mechanism (Founder Decision 004 approved the *principle*, not the *how*), and anonymous-vs-authenticated AI rate-limit specifics. Part 2: build only the non-chart Results foundation — route, fetch, loading/error/status-aware states, hero numbers, snapshot, technical details, no growth chart, no AI panel, per direct instruction.

**Scope**: `docs/FOUNDER_DECISIONS.md` (three new entries), `docs/EXPERIENCE_CONSTITUTION.md` (status flip), `docs/KNOWN_ISSUES.md` (KI-021 reopened with updated status), `docs/ARCHITECTURE_DECISIONS.md` (two new ADRs), a small additive backend schema change (`calculation_version` exposure), and the frontend's first dynamic route (`/simulation/[id]`). Explicitly excluded: growth chart, composition chart, AI panel, methodology section, table fallback, advanced disclosure system, and the actual `growth_series` persistence implementation approved by Founder Decision 014 (mechanism approved, not built this pass).

**Implementation Summary**: **Part 1 — Founder Decisions.** Formalized Founder Decision 013 (Experience Philosophy), transcribing `docs/EXPERIENCE_CONSTITUTION.md`'s own complete, ready-to-formalize proposal into `docs/FOUNDER_DECISIONS.md`'s numbering, and flipping that document's own status line from PROPOSED to APPROVED. Formalized Founder Decision 014 (Growth Series Persistence, Option A: persist at creation, backfill existing completed simulations, version against `calculation_version`, `GET` must never return empty `growth_series` for a completed simulation) — approved as policy, **not implemented this pass** (no growth chart exists yet to need it); KI-021 reopened from "partially resolved" to "Open" with the approved mechanism recorded, rather than left silently stale. Formalized Founder Decision 015 (Anonymous Educational AI Limits, Option D: anonymous keeps no-auth-wall access at a lower per-minute rate plus a new daily cap, authenticated keeps the spec-mandated 20/min at a higher daily cap, friendly non-punitive limit-reached messaging throughout) — also approved as policy only, unblocking M7 Phase 4's AI-panel design rather than being implemented against the currently-nonexistent AI panel. **Part 2 — Results Foundation.** Built `/simulation/[id]` (`frontend/src/app/simulation/[id]/page.tsx`, a thin async server component awaiting this Next.js version's Promise-typed `params`, mirroring the Simulator's own page/client split) and `SimulationResultClient` (`frontend/src/components/simulation-result/simulation-result-client.tsx`, the one `'use client'` boundary, calling a new `useSimulation(id)` React Query hook). Implemented literally every non-chart item on the task's list: the exact `"Loading simulation…"` loading copy, `ErrorState`-rendered fetch errors (`SIMULATION_NOT_FOUND`, `FORBIDDEN`, etc. via the existing `getErrorCopy` table), status-aware rendering for all three `SimulationStatus` values (`completed` → hero numbers; `pending` → a calm processing card with a manual `refetch()`-backed "Check again" action, no auto-polling since simulations complete synchronously within `POST` today; `failed` → the existing `error_message` through the same `ErrorState` pattern the Simulator's own success card already uses), a "Simulation Result" header with a status `Badge`, a worked-example sentence in the product's own "if you had invested…" language (`docs/EXPERIENCE_CONSTITUTION.md` §4) whose closing clause changes with status, a Simulation Snapshot card (every input, never a result), three hero `StatTile`s (Final Value/Total Return/CAGR, each carrying a `source` formula string), a collapsed Technical Details disclosure (simulation ID, `calculation_version`, created timestamp), a "Run another simulation" link, and a client-side-only "Copy link" affordance (`navigator.clipboard`, nothing sent anywhere). A real, live-rendering-caught layout defect was found and fixed in the same pass: `StatTile`'s existing `text-3xl`/`sm:text-4xl` value typography — never before exercised in a genuine three-tiles-in-a-row layout — visibly overflowed its card for a real five-figure `final_value`, caught only by screenshotting the page against a real backend-created simulation, not by the unit test suite. Fixed by adding an opt-in `size?: 'default' | 'compact'` prop to `StatTile` (default unchanged, `'compact'` used only by this page's three-tile row) plus `break-words`/`min-w-0` as a defensive floor — full reasoning in ADR-037. A second, small backend gap was closed alongside this: `calculation_version` had existed on every `Simulation` row since the first migration but was never exposed on `SimulationResponse`, required by the Results foundation's own technical-details item — added as a one-field, additive, non-breaking schema change (ADR-036), identical on both `POST` and `GET`, with `docs/api_design.md` updated to match.

**Files Created**: `frontend/src/app/simulation/[id]/page.tsx`, `frontend/src/components/simulation-result/simulation-result-client.tsx`, `frontend/src/__tests__/app/simulation-result-page.test.tsx`, `frontend/src/__tests__/components/simulation-result-client.test.tsx`.

**Files Modified**: `docs/FOUNDER_DECISIONS.md` (FD-013/014/015), `docs/EXPERIENCE_CONSTITUTION.md` (status), `docs/KNOWN_ISSUES.md` (KI-021), `docs/ARCHITECTURE_DECISIONS.md` (ADR-036, ADR-037), `docs/api_design.md` (`calculation_version` documented), `backend/app/api/v1/schemas/simulations.py` (`calculation_version` field), `backend/tests/api/test_simulations.py` (two new assertions), `frontend/src/types/api.ts` (`calculation_version`), `frontend/src/lib/api/endpoints/simulations.ts` (`getSimulation` now accepts `signal`), `frontend/src/hooks/use-simulation.ts` (new `useSimulation` hook), `frontend/src/components/ui/stat-tile.tsx` (`size` prop, `min-w-0`, `break-words`), `frontend/src/__tests__/lib/use-simulation.test.tsx` (new `useSimulation` coverage), `frontend/src/__tests__/components/simulation-form.test.tsx` (mock fixture updated for the new required field).

**Architecture Decisions**: ADR-036 (`calculation_version` exposure), ADR-037 (Results foundation page structure, status-aware rendering, `StatTile` compact variant) — both `docs/ARCHITECTURE_DECISIONS.md`.

**Problems Encountered**: (1) The dev Postgres database (a long-running local container from a prior session) had no schema at all — `alembic upgrade head` had apparently never been run against it, discovered only when a real `POST /api/v1/simulations` call failed with `UndefinedTable`. (2) The backend container runs `uvicorn` without `--reload`, so the live-mounted code volume did not reflect the `calculation_version` schema change until the container was restarted. (3) `StatTile`'s hero-number row visibly overflowed its card at the page's original `max-w-2xl` width — not caught by any unit test, since none of them render three `StatTile`s side by side against a real long value. (4) A `userEvent.setup()` call in the "Copy link" test silently stubbed `navigator.clipboard` itself, so an explicit `Object.defineProperty` mock for it was never actually reached by the component, making the test's `writeText` assertion fail even though the real feature worked correctly in the browser.

**Solutions**: (1) Ran `alembic upgrade head` against the dev database directly, then re-seeded AAPL price data via the existing `dev_seed` provider — a one-time local-environment fix, not a code change. (2) `docker compose restart backend` — a bind-mounted, non-`--reload` container only needs a process restart, not a rebuild, to pick up a code change; confirmed via `/openapi.json` before proceeding. (3) Tried `break-words` alone (legible but an ugly mid-digit-group wrap), then a wider container (`max-w-3xl`, measured still insufficient), before landing on the `StatTile` `size="compact"` variant (ADR-037) — each candidate fix was actually screenshotted and visually inspected before being accepted or discarded, not assumed correct from the diff alone. (4) Switched that one test from `userEvent.click` to `fireEvent.click`, which does not carry `userEvent`'s clipboard-stubbing side effect — the "Check again" test, which has no clipboard interaction, correctly kept using `userEvent`.

**Lessons Learned**: A component that "should obviously fit" (three tiles, plenty of desktop width) still needs to be rendered against a real, long value before being trusted — this is the second time this project has caught a real defect only by screenshotting live output rather than trusting a green test suite (the first was KI-043), and both times the unit tests were passing the whole time, because neither the exact overflow scenario nor a cross-component layout interaction is something a component-level `render()` test naturally exercises. Restarting a long-running local dev stack's containers before manual verification should be a standing first step, not an afterthought — code changes silently not reaching a non-`--reload` container is an easy, quiet way to "verify" against stale code and believe a fix works when it hasn't actually been exercised at all.

**Security Review Summary**: See `docs/SECURITY_LOG.md` M7 Phase 3B entry. No new attack surface: the Results page is a read-only `GET` against an endpoint that already existed and already enforced anonymous/ownership access rules (Founder Decision 002); `calculation_version` is a non-sensitive, non-PII string; the "Copy link" affordance reads `window.location.href` and writes to the clipboard, both same-origin, browser-mediated operations with no new data leaving the client.

**Testing Summary**: See `docs/TESTING_REPORT.md` M7 Phase 3B entry. Backend: 278/278 passing (2 new assertions for `calculation_version`), ruff clean on every file this pass touched. Frontend: 161 passing / 1 gracefully skipped (162 total, up from 150), 93.87%/81.41%/92.59%/96.16% statement/branch/function/line coverage, `npx eslint .` and `npx tsc --noEmit` both zero-error, `npm run build` succeeds with `/simulation/[id]` correctly registering as a dynamic (`ƒ`) route. Manually verified end-to-end against the live local stack: a real `completed` simulation (AAPL, `$10,000` invested `2020-01-02`–`2020-12-30`), a real `failed` simulation (`MISSING_HISTORICAL_DATA` on a deliberately out-of-range end date), and a genuinely nonexistent simulation ID were each fetched through the actual running Next.js dev server and the actual running FastAPI backend, screenshotted with a headless Playwright browser, and visually inspected — not simulated or mocked.

**Technical Debt Introduced**: None judged unintentional. Two items are explicitly deferred, not silently dropped: Founder Decision 014's `growth_series` persistence implementation (KI-021, reopened, Planned Resolution recorded) and Founder Decision 015's anonymous/authenticated rate-limit implementation (unblocks M7 Phase 4, not built this pass — no code path exists yet that would use it).

**Performance Notes**: See `docs/PERFORMANCE_LOG.md` M7 Phase 3B entry. No new backend query pattern (the Results page reuses the existing, unmodified `GET /api/v1/simulations/{id}` retrieval path); `calculation_version` adds one already-selected column to an existing response, not a new query.

**Recruiter Value**: Medium-High. Demonstrates the project's design-review-then-implement discipline applied to real ambiguity resolution (three founder decisions, each with genuine options considered and rejected, not rubber-stamped), a real cross-component layout bug found and fixed through actual live verification rather than trusted test-suite output, and a minimal, correctly-scoped backend schema change made only because a real frontend requirement needed it — not spontaneously "while in the area."

**Production Readiness Score**: ~5/10 platform-wide, unchanged by this phase's own scope — the Results screen now has a real, verified foundation (a genuine step beyond M7 Phase 2's scope), but the growth chart, AI panel, and this phase's own two deferred implementation items (KI-021's persistence, Founder Decision 015's rate limits) all remain open before the full Results Experience is complete.

**Next Milestone**: M7 Phase 3C — the full Results Experience (growth chart, composition/splits disclosure, AI panel), which depends on Founder Decision 014's `growth_series` persistence actually being implemented (not just approved) and, for the AI panel specifically, Founder Decision 015's rate-limit implementation landing first.

---

## 2026-07-20 — M7 Phase 3B.1: Results Opening Sequence

**Version**: 0.10.1

**Objective**: Implement, faithfully and without redesigning, the approved Results Opening Sequence storyboard — the calm, one-time animated bridge between "Run Simulation" succeeding on the Simulator and the completed Results screen it hands off to. Explicitly not a redesign of M7 Phase 3B's already-built hero numbers, snapshot, or technical details; only the opening sequence and the transition into the page those pieces already occupy.

**Scope**: The seven-scene storyboard (Simulator receding → phrases composing → user's values moving into the sentence → a silent ~1s pause → the answer arriving calmly, scale not animation → the sentence settling into the permanent page heading → the Results page growing beneath it), its accessibility requirements (`prefers-reduced-motion`, skippable, no keyboard blocking), and its replay rules (play exactly once, immediately after creation; never on refresh, browser-back, a shared link, or a revisited simulation). Explicitly excluded, per direct instruction: hero cards, charts, disclosures, methodology, AI, technical details — all pre-existing M7 Phase 3B content, untouched.

**Implementation Summary**: `SimulationForm` (`frontend/src/components/simulator/simulation-form.tsx`) no longer renders its own inline "Simulation complete" success card; on a successful mutation it now navigates directly to `/simulation/{id}` via `useRouter().push`, appending a one-shot `?new=1` marker only when the result is `completed` (a `failed` result has no answer to reveal, so it navigates plainly) — the button stays in its "Calculating historical returns…" state through the route transition itself, derived directly from mutation state rather than a separate flag. The Results screen's `completed` branch (`SimulationResultClient`) now renders a new `OpeningSequenceHeading` component (`frontend/src/components/simulation-result/opening-sequence-heading.tsx`) in place of the old static header; `pending`/`failed` states are untouched, still using the original `ResultHeader`. `OpeningSequenceHeading` composes a single sentence from the simulation's own values ("If you had invested $10,000.00 in AAPL, starting January 2, 2020 and held until January 2, 2024 — your investment would be worth $34,180.00.") — the input phrases (asset/amount/dates) reveal one at a time via `useOpeningSequence`'s `setTimeout`-scheduled timeline (`composing` → `paused` after ~1s of silence → `answered`, the figure arriving as a calm opacity fade at a larger, but not animated, size → `settled`), at which point the rest of the completed Results screen (hero `StatTile`s, Simulation Snapshot, Technical Details, the "Run another simulation"/"Copy link" row — all unchanged M7 Phase 3B content) mounts beneath it with no entrance animation of its own, matching the storyboard's explicit "no lingering animation" instruction for the settled state. Whether the sequence plays at all is gated by three independent, all-must-hold conditions: `useJustCreatedFlag()` (the `?new=1` marker, read once and immediately stripped from the URL — full reasoning for choosing a query marker over `sessionStorage` in ADR-038), `!useReducedMotion()` (a JS-level `matchMedia` check, needed because the project's existing global CSS reduced-motion override collapses `animation`/`transition-duration` but cannot collapse a JS-scheduled `setTimeout` delay), and `sim.status === 'completed'`. When any is false, the component starts already `settled` — the identical final markup, no animation scheduled at all, satisfying "information must remain identical" for a revisited simulation, a shared link, a refresh, or a reduced-motion preference. The real `<h1>` carries the complete final sentence (including the emphasized answer figure) from the very first render, `sr-only` until settled — a screen reader gets the whole answer immediately, never only through the decorative animation, which is a separate, `aria-hidden` element occupying the exact same visual position so nothing visibly travels when the swap happens. A visible, keyboard-reachable "Skip" button jumps straight to the settled state.

**Files Created**: `frontend/src/hooks/use-reduced-motion.ts`, `frontend/src/hooks/use-just-created-flag.ts`, `frontend/src/hooks/use-opening-sequence.ts`, `frontend/src/components/simulation-result/opening-sequence-heading.tsx`, `frontend/src/__tests__/lib/use-opening-sequence.test.tsx`, `frontend/src/__tests__/lib/use-just-created-flag.test.tsx`, `frontend/src/__tests__/components/opening-sequence-heading.test.tsx`.

**Files Modified**: `frontend/src/components/simulator/simulation-form.tsx` (navigation replacing the inline success card), `frontend/src/components/simulation-result/simulation-result-client.tsx` (completed branch now renders `OpeningSequenceHeading`), `frontend/src/app/simulation/[id]/page.tsx` (`<Suspense>` boundary for `useSearchParams`), `frontend/src/__tests__/components/simulation-form.test.tsx`, `frontend/src/__tests__/components/simulation-result-client.test.tsx`, `docs/ARCHITECTURE_DECISIONS.md` (ADR-038).

**Architecture Decisions**: ADR-038 (`?new=1` query marker over `sessionStorage` as the one-shot "just created" signal) — `docs/ARCHITECTURE_DECISIONS.md`.

**Problems Encountered**: (1) A real layout bug, found only by live-rendering the sequence in a headless-Chromium browser, not by the jsdom-based test suite: the decorative composing phrases used `inline-block` spans with a trailing space baked into each phrase's own text content; browsers trim whitespace at an `inline-block` box's edge, so consecutive words ran together ("invested$10,000.00in AAPL,"). (2) An initial hook design computed `useState`'s initial value from `active` (whether the sequence should play) at the exact render where the surrounding component first mounted — but that mount could happen *before* the simulation's `status` was actually known (still loading), meaning the state would lock in "settled" at construction and then have to un-settle once data arrived, which would visibly flash the fully-revealed answer for one frame before hiding it and re-animating. (3) Two ESLint rules not previously exercised by this codebase's animation-free component set fired on first pass: `react-hooks/set-state-in-effect` (calling `setState` synchronously inside a bare effect body) and the project's own `no-restricted-syntax` DecimalString-comparison guardrail (ADR-033), the latter correctly but incorrectly-for-this-case flagging a plain loop-counter and phrase-index comparison as if they were financial values.

**Solutions**: (1) Moved the space to a separate, plain (non-`inline-block`) text node rendered as a sibling immediately after each animated phrase span, using `phrase.trimEnd()` inside the span itself — verified fixed via a before/after headless-Chromium screenshot comparison. (2) Restructured so `useOpeningSequence`/`useJustCreatedFlag`/`useReducedMotion` are only ever called from inside `OpeningSequenceHeading`, a component the parent (`SimulationResultClient`) does not render at all until the simulation has actually finished loading and its `status` is known — meaning this component's very first render already has a correct, final `active` value, with nothing to un-settle later. (3) Followed the existing `ThemeProvider` convention (`readStoredTheme`/`getSystemTheme`) for `useReducedMotion`: compute the initial value via a pure `useState` lazy initializer (a plain `matchMedia(...).matches` read, not a `setState` call), and let the `useEffect` only *subscribe* to future `change` events — the pattern the lint rule is specifically designed to allow. For `no-restricted-syntax`, added the same scoped, commented `eslint-disable-next-line` escape hatch ADR-029/ADR-033 already establish for genuine non-financial comparisons.

**Lessons Learned**: The same lesson this project has now caught three separate times at three different milestones (KI-043, ADR-037, now this) — a passing, green unit-test suite and a correct live render are not the same claim, and specifically for anything involving real CSS box-model behavior (`inline-block` whitespace trimming, in this case) or real animation timing, jsdom's lack of a layout/paint engine means the test suite structurally cannot catch the defect no matter how thorough its assertions are; only an actual browser screenshot can. A temporary, dev-only preview route (mirroring the existing `dev/playground` pattern, deleted after use) was the fastest way to visually exercise a fully-animated component with mock data when no backend was reachable locally (Docker Desktop was not running this session) — a reusable technique for verifying a UI-only feature without needing full-stack infrastructure up.

**Security Review Summary**: No new attack surface. The `?new=1` marker only ever controls whether a client-side animation plays; it changes no query, no fetched data, and no authorization check. `router.replace` strips the marker from the browser's own address bar and history entry using the exact same `pathname` already being viewed — no user-supplied or externally-sourced URL is ever passed to it.

**Testing Summary**: See `docs/TESTING_REPORT.md` M7 Phase 3B.1 entry. Frontend: 169 passing / 4 gracefully skipped (173 total, up from 162), `npx eslint .` and `npx tsc --noEmit` both zero-error, `npm run build` succeeds with `/simulation/[id]` still correctly registering as a dynamic (`ƒ`) route. Manually verified in a real, headless-Chromium browser against a temporary dev-only preview route (Docker Desktop was not running this session, so the real backend/Postgres stack was unavailable): the full composing → pause → answer → settle timeline, the "Skip" affordance jumping straight to the settled state, a reduced-motion preference skipping the sequence entirely, and — critically — that reloading the page after the sequence had played showed the fully settled sentence immediately with no replay, confirming the `?new=1` marker had actually been stripped from the URL. The real `/simulator` and `/simulation/[id]` routes were also loaded directly (without a backend reachable) to confirm no console errors or crashes from the new `Suspense`/`useSearchParams` wiring.

**Technical Debt Introduced**: None judged unintentional. The temporary dev-only preview route created to verify the sequence without a backend was deleted before this pass closed, per its own "verification-only, not part of the shipped product" comment — matching the existing `dev/playground` route's own production guard convention had it been kept, but judged unnecessary as permanent tooling for a single component already covered by its own dedicated test file.

**Performance Notes**: No new network request pattern — the opening sequence is pure client-side state/timing over data the Results screen was already fetching; `useJustCreatedFlag`'s `router.replace` is a client-side history update, not a new navigation or server round-trip.

**Recruiter Value**: Medium. A small, self-contained motion feature that nonetheless required real judgment calls to implement correctly against a design-philosophy document (EXPERIENCE_CONSTITUTION.md) rather than a literal component spec — translating "scale creates emphasis, not animation" and "the sentence gently moves upward" into concrete, restrained, non-theatrical implementation choices, catching a real cross-browser CSS bug no test suite could have caught, and getting React Strict Mode's double-invocation semantics right for a one-shot signal (ADR-038) are all the kind of detail-level correctness this project's recruiter-facing narrative already leans on.

**Production Readiness Score**: ~5/10 platform-wide, unchanged by this phase's own scope — a UI/UX refinement to an already-built screen, not new backend surface or a new production blocker resolved.

**Next Milestone**: M7 Phase 3C — the full Results Experience (growth chart, composition/splits disclosure, AI panel), unchanged from the prior entry's dependencies (Founder Decision 014's `growth_series` persistence, Founder Decision 015's rate-limit implementation for the AI panel specifically).

---

## 2026-07-20 — M7 Phase 3B.2: Results Reading Experience

**Version**: 0.10.2

**Objective**: Redesign the completed-simulation Results screen from scratch, on direct instruction to reject M7 Phase 3B's own dashboard-pattern layout ("generic finance-dashboard pattern... do NOT continue from that design") in favor of a fixed, editorial reading order: the worked-example sentence as the page's sole hero, everything else — facts, growth, explanation, proof — supporting evidence, never competing with it. Explicitly keep reusable pieces (routing, `useSimulation`, the API client, the M7 Phase 3B.1 opening sequence's animation/replay logic, test conventions); redesign only the visual/structural composition. No motion added this pass beyond what M7 Phase 3B.1 already built; no AI.

**Scope**: `OpeningSequenceHeading`'s visual treatment (Sections 1–2), four new sections (`SupportingFacts`, `GrowthOverTime`, `WhyExplanation`, `TheProof` — Sections 4–7, Section 3 being pure whitespace), and `SimulationResultClient`'s `completed` branch wiring them together in the fixed order. `pending`/`failed` states explicitly out of scope — the new reading order describes "what actually happened," which only a `completed` simulation has answered; those two states keep M7 Phase 3B's original `ResultHeader`/`SimulationSnapshot`/`TechnicalDetails` treatment unchanged.

**Implementation Summary**: `OpeningSequenceHeading` (`frontend/src/components/simulation-result/opening-sequence-heading.tsx`) was restyled, not rebuilt — its timing/hook logic (`useOpeningSequence`, `useJustCreatedFlag`, `useReducedMotion`, the `?new=1` marker from ADR-038) is byte-for-byte the same decision-making, only the JSX changed. The sentence is now composed as seven stacked lines ("If you had invested" / the amount / "in {symbol}" / "between {start date}" / "and {end date}" / "your investment would be worth" / "{final value} today.") rather than one flowing paragraph, with the two figures (amount, final value) rendered at a large "Hero Figure" size (BRAND_CONSTITUTION.md §6's existing typography tier, applied literally for the first time) against the rest of the sentence's supporting size — typography and whitespace carrying the emphasis the storyboard calls for, explicitly not color or animation. Section 1's status `Badge` is removed entirely, leaving only the tiny "Simulation result" kicker label the instruction calls for ("nothing more"). Four new presentational components (`frontend/src/components/simulation-result/results-sections.tsx`) implement Sections 4–7: `SupportingFacts` renders Final Value/Total Return/Annual Return as plain `<dl>` label-value pairs with a native `<details>` source disclosure each — deliberately not `StatTile`, whose bordered-card treatment is exactly the pattern being rejected (`StatTile` itself is untouched, still available to any caller that legitimately wants a tile). `GrowthOverTime` renders a plainly-worded "not yet available" paragraph, since `growth_series` is empty on every retrieval today (KI-021); it also has a working, tested branch for a populated series, so M7 Phase 3C's eventual chart work only needs to replace the empty-state paragraph, not restructure the section. `WhyExplanation` writes three short, static (non-AI) paragraphs personalized to the simulation's own values: price appreciation always describes the actual initial→final price move; dividend contribution and inflation adjustment each state what was actually chosen for this simulation — an assumption stated plainly whether true or false, never silently applied or omitted, including the genuine "CPI data wasn't available" gap case distinct from "inflation adjustment wasn't requested." `TheProof` folds the former "Simulation Snapshot" and "Technical Details" cards into one collapsed-by-default `<details>` with three subsections (Methodology, Assumptions, Technical details), adding new methodology copy describing the `close_price` basis and split-handling (grounded in `docs/simulation_formulas.md` §§2–3). Full reasoning for both non-obvious calls — rejecting `StatTile`'s card treatment specifically for this screen without changing the component, and building an honest placeholder rather than adding a charting library against zero real data — is ADR-039.

**Files Created**: `frontend/src/components/simulation-result/results-sections.tsx`, `frontend/src/__tests__/components/results-sections.test.tsx`.

**Files Modified**: `frontend/src/components/simulation-result/opening-sequence-heading.tsx` (visual redesign, logic unchanged), `frontend/src/components/simulation-result/simulation-result-client.tsx` (`completed` branch rewired to the new sections; `pending`/`failed` untouched), `frontend/src/__tests__/components/opening-sequence-heading.test.tsx`, `frontend/src/__tests__/components/simulation-result-client.test.tsx`, `frontend/src/app/dev/opening-sequence-preview/page.tsx` (the temporary M7 Phase 3B.1 verification route, updated to render the real Sections 4–7 instead of a placeholder `<div>`, kept at the user's request as an ongoing no-backend preview rather than deleted), `docs/ARCHITECTURE_DECISIONS.md` (ADR-039).

**Architecture Decisions**: ADR-039 (editorial layout replacing the dashboard pattern; the growth-chart honest-placeholder decision, deferring a real chart to M7 Phase 3C behind KI-021) — `docs/ARCHITECTURE_DECISIONS.md`.

**Problems Encountered**: (1) A real accessibility regression, caught by the existing `opening-sequence-heading.test.tsx` suite immediately (not by visual inspection): the redesign's first draft wrapped the "Skip" button inside the same `aria-hidden="true"` decorative container as the composing animation, making it unreachable by `getByRole('button', { name: 'Skip' })` — and, identically, by a real screen reader or any role-based assistive tech, since `aria-hidden="true"` removes everything inside it from the accessibility tree regardless of the individual element's own semantics. (2) Restructuring the sentence from one flowing paragraph into seven stacked lines meant the input-phrase count driving `useOpeningSequence`'s timeline changed from 5 to 6 (the former closing clause, "your investment would be worth," became its own composing line rather than part of the answer), which silently shifted the exact millisecond the sequence reaches `settled` — a hardcoded `vi.advanceTimersByTime(2550)` in the existing animation-timing test needed updating to the new value or it would have failed for a reason unrelated to what it was actually testing. (3) The full `vitest run` suite showed two failures (`a11y.test.tsx`, `keyboard.test.tsx`) that did not reproduce when either file was run in isolation, or on a clean re-run of the full suite — judged transient resource contention (a background dev server and a headless Chromium instance were both running concurrently for manual verification during this same session), not a real regression, and confirmed by two subsequent full, clean runs both passing 38/38 files.

**Solutions**: (1) Moved the "Skip" button to be a sibling *outside* the `aria-hidden` boundary, alongside the decorative animation rather than inside it — the real `<h1>` above already carries the complete sentence via `aria-label`, so the decorative reveal can safely stay `aria-hidden` on its own without taking the Skip control down with it. (2) Recalculated the new settle time from `useOpeningSequence`'s own constants (`STAGGER_MS × phraseCount + PAUSE_MS + ANSWER_HOLD_MS` = `200×6 + 1000 + 550` = `2750`) and updated the test to match, rather than guessing or padding with an arbitrarily larger number. (3) Re-ran the full suite twice more, clean, both passing — not a fix, a confirmation that no fix was needed. `results-sections.tsx`'s new conditional-copy branches (dividend true/false, inflation true-with-value/true-without-value/false, growth-series empty/populated) were initially undertested (a coverage report showed `results-sections.tsx` at 50% branch coverage from the single "renders all sections" integration test alone) — a dedicated `results-sections.test.tsx` was added covering every branch explicitly, raising the file to 77.27% branch coverage overall (the remaining gap is the `initial_price`/`final_price`-null defensive fallback in `WhyExplanation`, unreachable for a `completed` simulation under this codebase's own invariants, the same judgment call already made for equivalent defensive branches in M7 Phase 3B/3B.1).

**Lessons Learned**: A visual redesign is not exempt from the accessibility discipline a purely additive feature gets — restructuring existing, already-correct markup (the Skip button had been correctly placed outside any `aria-hidden` boundary since M7 Phase 3B.1) is exactly the kind of change that can silently reintroduce a regression a first read-through of the new JSX wouldn't catch, and only running the existing test suite against the restructured component caught it immediately. Separately: a component's internal implementation detail (how many discrete phrases a sentence is broken into) can be an invisible dependency for a *different* file's test (the hardcoded settle-time constant in the animation-timing test) — when redesigning a component's content structure, its own timing/animation tests need to be re-derived from the underlying constants, not assumed unaffected just because the redesign was "only visual."

**Security Review Summary**: No new attack surface — a pure visual/structural redesign of an existing read-only screen. No new fetch, no new endpoint, no change to what data is requested or displayed for a given simulation, no new user input accepted anywhere on this screen.

**Testing Summary**: See `docs/TESTING_REPORT.md` M7 Phase 3B.2 entry. Frontend: 181 passing / 1 gracefully skipped (182 total, up from 173), 94.73%/82.2%/93.38%/96.59% statement/branch/function/line coverage, `npx eslint .` and `npx tsc --noEmit` both zero-error, `npm run build` succeeds with `/simulation/[id]` still registering as a dynamic (`ƒ`) route. Manually verified in a real, headless-Chromium browser (Docker Desktop was not running this session) against the same temporary dev-only preview route from M7 Phase 3B.1 (updated to render the real new sections instead of its old placeholder `<div>`): screenshotted the fully settled state in both light and dark themes, the mid-sequence animated composing state with the new stacked typography, and the expanded "The Proof" disclosure — all visually inspected against the seven-section reading order, with zero console/page errors in every screenshot pass.

**Technical Debt Introduced**: None judged unintentional. The `GrowthOverTime` "not yet available" placeholder is a deliberate, documented deferral (ADR-039), not a silent gap — it is directly tied to KI-021's existing tracked status and requires no new tracking entry of its own; M7 Phase 3C's dependency on KI-021 is unchanged by this pass.

**Performance Notes**: No new network request pattern or backend query — a pure frontend structural/visual change over data `SimulationResultClient` already fetches.

**Recruiter Value**: Medium-High. Demonstrates the project's willingness to actually discard prior work when a design review calls for it ("forget every previous Results page implementation") rather than incrementally patching a rejected pattern, translating an editorial-philosophy brief (typography/whitespace/rhythm, "does this feel like someone answering a question or software presenting metrics") into concrete component decisions without a literal spec to follow, and — again — catching a real, test-suite-confirmed accessibility regression introduced by the redesign itself before it shipped, consistent with this project's now well-established verification discipline.

**Production Readiness Score**: ~5/10 platform-wide, unchanged by this phase's own scope — a visual/structural redesign of an already-built screen, not new backend surface or a resolution to either of the two items most load-bearing for an actual launch decision (KI-016, KI-039).

**Next Milestone**: M7 Phase 3C — the full Results Experience, specifically now scoped by this pass's own placeholder: replace `GrowthOverTime`'s empty-state branch with a real chart once Founder Decision 014's `growth_series` persistence lands, plus composition/splits disclosure and the AI panel (unblocked separately by Founder Decision 015's rate-limit implementation).

---

## 2026-07-21 — Review, Fix-Investigation, Philosophy Audit & Reconciliation Pass

**Version**: 0.10.0 (unchanged — no new feature work, no version bump; this pass reviews, documents, and reconciles, per explicit instruction not to tag a new version until the open items below resolve)

**Objective**: A Principal Engineer + Principal Product Designer review pass over the uncommitted Results Opening Sequence (M7 Phase 3B.1) and Results Reading Experience (M7 Phase 3B.2) work, following an earlier damage-assessment pass that had confirmed the tree was healthy and the work functional. Four parts: (1) investigate a real CAGR display bug found during that earlier pass's live end-to-end verification; (2) audit the uncommitted work against `docs/EXPERIENCE_CONSTITUTION.md`/Founder Decision 013's motion philosophy; (3) reconcile a tag/doc drift (`v0.10.0` documented but never tagged); (4) commit what passes review, park what doesn't.

**Scope**: Investigation and documentation only for the CAGR defect (no code fix — the founder decides the layer). A full, file-by-file compliance audit of every uncommitted/untracked file (report only, nothing deleted). Git tag creation. `docs/PROJECT_STATE.md` reconciliation. Commit/branch operations. No frontend or backend source code was modified by this pass on `main` — `main`'s only changes are documentation and the retroactive tag.

**Implementation Summary — Part 1 (CAGR)**: Traced a live discrepancy ("+73.01% Total Return" alongside "+0.15% CAGR" for the same simulation, correct value +14.69%) through every layer that touches `cagr_percentage`. Root cause: `backend/app/simulation/formulas.py::calculate_cagr` returns a raw fraction (no `× 100`), matching its own docstring and `docs/simulation_formulas.md` §4's literal formula — internally consistent with itself, but not with its sibling `calculate_total_return_percent` (which does multiply by 100), not with the identically-typed/named `NUMERIC(10,6)` DB column pairing, and — the clearest evidence the *intended* behavior was always percentage-scaled — not with `docs/api_design.md`'s own worked-example response, which shows a percentage-scaled `cagr_percentage` value inconsistent with what the documented code actually produces for those inputs. The backend's own known-answer test asserts the unscaled (buggy) value, meaning the test suite currently certifies the defect as correct. Documented as `docs/KNOWN_ISSUES.md` KI-045 (High severity) and drafted as `docs/FOUNDER_DECISIONS.md` Founder Decision 016 — three options (fix at the source with a `calculation_version` bump + backfill; fix at the API response boundary only, no backfill; fix at the frontend only), with a recommendation (fix at the source, backfill immediately) but explicitly left for founder sign-off. No code changed.

**Implementation Summary — Part 2 (Philosophy Audit)**: Reviewed every uncommitted/untracked file against `docs/EXPERIENCE_CONSTITUTION.md` §6 (The Instrument) and §7 (Experience Principles) and Founder Decision 013. Core finding: `frontend/src/hooks/use-opening-sequence.ts`'s timeline (6 phrases × 200ms stagger + a 1000ms silent pause + a 550ms hold ≈ 2.75s total) and `frontend/src/components/simulation-result/opening-sequence-heading.tsx` (where it renders) are **BORDERLINE** — every individually-enumerated rule in §6/§8 is satisfied (no confetti/bounce/celebratory treatment, identical reveal regardless of a result's sign, fully deterministic, skippable, complete `prefers-reduced-motion` compliance verified by its own test suite), but the delay itself corresponds to no real computation — the answer is already known before the sequence starts — putting it in tension with §7's "speed is a form of respect... honest, named waiting where the product is doing real work" and §2's stated emotional register (a quant reporting a fact, not building toward a reveal). Noted for context, not as a defense: this exact pacing was specified verbatim in the M7 Phase 3B.1 task's own approved storyboard ("Pause. Approximately one second... This silence is intentional") — built faithfully to that brief, not freelanced, but the audit checks the artifact against the constitution, not against the brief that produced it. Every other file audited **COMPLIANT**: `use-reduced-motion.ts` and `use-just-created-flag.ts` (pure accessibility/replay-gating mechanisms, no narrative motion), `results-sections.tsx` (zero motion, static progressive disclosure), `simulation-form.tsx`/`simulation-result-client.tsx`/`app/simulation/[id]/page.tsx` (wiring, no motion of their own — though the completed-branch wiring is only reachable through the BORDERLINE hero component, making it non-separable in practice). Nothing was deleted; this is a report, not an edit.

**Implementation Summary — Part 3 (Tag/Doc Reconciliation)**: `git tag --list` stopped at `v0.9.3`, while `docs/CHANGELOG.md` and `docs/PROJECT_STATE.md` had documented `[0.10.0]`/`M7 Phase 3B` as complete since 2026-07-19 — the tag was simply never created. Created and pushed an annotated `v0.10.0` tag on commit `15474d3` (the actual M7 Phase 3B commit), closing the drift. Rewrote `docs/PROJECT_STATE.md`'s Current Milestone, roadmap table (adding 3B.1/3B.2 rows marked "parked pending founder review," not ✅ or ⏳ — a status this document hadn't needed before), Open Founder Decisions (FD-016 added), and Critical Known Issues (KI-045 added) to reflect the actual, current, honest state of `main` versus what exists only on the parked branch.

**Implementation Summary — Part 4 (Commit & Park)**: Given Part 2's finding, the entire Results Opening Sequence + Results Reading Experience changeset turned out to be non-separable: `simulation-result-client.tsx`'s new `completed` branch only functions by importing and rendering the BORDERLINE `OpeningSequenceHeading`; `results-sections.tsx` (itself compliant) is only reachable through that same wiring; `use-reduced-motion.ts`/`use-just-created-flag.ts` have no other consumer. Committing a subset to `main` would either break the build or land genuinely orphaned dead code — neither is honest "logically grouped, atomic" work per `.claude/GIT_WORKFLOW.md`. Resolution: created `results/pending-founder-review` from `main`'s tip (preserving the full uncommitted working tree, the same "preserve before you touch anything" discipline the earlier damage-assessment pass established), committed the entire changeset there as 5 logical, conventionally-prefixed commits (hooks; hero + editorial sections; wiring; tests; docs — including ADR-038/039, and this branch's own copies of KI-045/Founder Decision 016 for full reviewer context), verified it independently green (181/182 tests, zero lint/typecheck errors) before pushing, then returned to a clean `main` (`git checkout main`, safe because everything was already committed elsewhere first) and built the CAGR/tag/roadmap documentation fresh against that clean base, as three separate commits.

**Files Created**: None on `main` this pass. On `results/pending-founder-review`: `frontend/src/hooks/use-opening-sequence.ts`, `use-just-created-flag.ts`, `use-reduced-motion.ts`; `frontend/src/components/simulation-result/opening-sequence-heading.tsx`, `results-sections.tsx`; `frontend/src/app/dev/opening-sequence-preview/page.tsx`; 4 new test files (full list in that branch's own commits).

**Files Modified**: `main`: `docs/KNOWN_ISSUES.md` (KI-045), `docs/FOUNDER_DECISIONS.md` (Founder Decision 016), `docs/PROJECT_STATE.md` (full reconciliation). `results/pending-founder-review`: `frontend/src/components/simulation-result/simulation-result-client.tsx`, `frontend/src/components/simulator/simulation-form.tsx`, `frontend/src/app/simulation/[id]/page.tsx`, 2 updated test files, plus its own copies of `docs/ARCHITECTURE_DECISIONS.md` (ADR-038/039), `docs/CHANGELOG.md`, `docs/DEVLOG.md`, `docs/TESTING_REPORT.md`, `docs/PROJECT_STATE.md` (that branch's own view), and `docs/KNOWN_ISSUES.md`/`docs/FOUNDER_DECISIONS.md` (a full-context copy of KI-045/FD-016).

**Architecture Decisions**: No new ADR on `main` this pass (a tag creation and documentation reconciliation don't constitute an architectural decision). `ADR-038`/`ADR-039` exist on `results/pending-founder-review` only, documenting decisions specific to the parked work.

**Problems Encountered**: (1) The task's initial framing assumed a `v0.10.0` tag already existed as the "last known-good state" to reset to — it didn't; `git tag --list` stopped at `v0.9.3`. Caught before any reset was attempted, by actually checking rather than trusting the premise (the same discipline the immediately-prior damage-assessment turn had already established for this same repository). (2) Initial plan was to commit "the compliant subset" of the uncommitted work directly to `main` while parking only the BORDERLINE pieces — discovered mid-execution that no clean subset exists; every non-motion file's only purpose is to support the BORDERLINE hero component, making partial commit either build-breaking or dead-code-landing. (3) `results-sections.tsx` and the two accessibility/replay-gating hooks are each independently, genuinely compliant — tempting to commit them alone — but doing so would have shipped inert, unreachable code to `main` for no real benefit over parking them with their one consumer.

**Solutions**: (1) Verified the actual tag list and commit history directly before taking any action, then created the tag retroactively rather than treating a wrong premise as a directive. (2)/(3) Reframed "logically grouped commits" at the level of the *whole reviewable feature* rather than per-file — parked everything Results-redesign-related as one coherent, fully-tested, independently-buildable branch, and kept `main` limited to genuinely independent, non-code documentation work (the CAGR finding, which is a backend defect unrelated to whether the frontend redesign ships; the tag/roadmap reconciliation, which required no code at all).

**Lessons Learned**: A "commit what passes, park what doesn't" instruction is straightforward when violations are isolated, but when the flagged component is the single entry point into an otherwise-compliant feature, the honest execution is to park the feature as a whole, not to manufacture an artificial split that leaves `main` either broken or carrying dead code — atomicity applies at the level of "a reviewable, working unit," not "every individual file that happens to pass its own narrow test." Separately: verifying a task's stated premise (here, "the last tag, v0.10.0") before acting on it caught a second, real, independent piece of drift (the tag genuinely never existed) that the task's own framing had assumed away — the same "measure twice" discipline that mattered for the earlier damage-assessment turn mattered again here, for a different reason.

**Security Review Summary**: No new attack surface — this pass is documentation, a git tag, and commit/branch reorganization only. No source code changed on `main`.

**Testing Summary**: `main`: no test-affecting changes (documentation only); the pre-existing test suite is unaffected and was not re-run against `main` specifically, since nothing executable changed. `results/pending-founder-review`: independently re-verified green immediately before pushing — 39/39 test files, 181 passing / 1 gracefully skipped, `npx eslint .` and `npx tsc --noEmit` both zero-error, production build succeeds.

**Technical Debt Introduced**: None judged unintentional. KI-045 is newly *discovered* technical debt (pre-existing since the Simulation Engine's original M3 build, not introduced by this pass), now properly tracked rather than silently present. The parked branch itself is not technical debt — it is fully committed, tested, working code deliberately held back pending two explicit, documented founder decisions, not an abandoned or half-finished state.

**Performance Notes**: N/A — no code changed on `main`.

**Recruiter Value**: High. Demonstrates the willingness to hold back finished, working, tested code when it doesn't clear a governance bar the team itself established (FD-013), rather than shipping and rationalizing after the fact — and demonstrates catching a real, silently-wrong financial figure (the kind of defect that erodes trust fastest in a product whose entire premise is financial correctness) through actual live verification rather than trusting a green test suite, consistent with this project's now-repeated pattern (KI-043, ADR-037, the M7 Phase 3B.1 `inline-block` bug, and now KI-045) of finding real defects specifically by exercising the running system rather than stopping at "tests pass."

**Production Readiness Score**: ~5/10 platform-wide, unchanged — `main` carries no functional changes this pass.

**Next Milestone**: Founder resolution of Founder Decision 016 (KI-045) and the motion-philosophy BORDERLINE finding, both prerequisites for merging `results/pending-founder-review` — which is itself now the prerequisite for M7 Phase 3C.

---

## 2026-07-22 — Founder Decision 016 (CAGR Fix) + Founder Decision 017 (Opening Sequence Ruling)

**Version**: 0.11.0

**Objective**: Implement two founder-approved decisions from the 2026-07-21 review pass: Founder Decision 016 (fix `cagr_percentage`'s factor-of-100 scale defect at the source, bump `calculation_version`, backfill existing rows) and Founder Decision 017 (remove the Results Opening Sequence's staged composing→pause→reveal timeline; keep the surrounding editorial components). Both were left as open judgment calls by the prior pass; both are resolved here.

**Scope**: Backend: `calculate_cagr`, `DEFAULT_CALCULATION_VERSION`, a new Alembic backfill migration, the known-answer test, `docs/simulation_formulas.md`, `docs/api_design.md`. Frontend: `opening-sequence-heading.tsx`, a new `use-settle-in.ts` hook replacing `use-opening-sequence.ts`, removal of `use-just-created-flag.ts` and its `?new=1` mechanism, `simulation-form.tsx`'s post-submit navigation, the `Suspense` boundary in `app/simulation/[id]/page.tsx`, and the temporary dev preview route. Documentation: `docs/FOUNDER_DECISIONS.md` (FD-016 approved, FD-017 recorded), `docs/ARCHITECTURE_DECISIONS.md` (ADR-040, ADR-041, ADR-038 superseded), `docs/KNOWN_ISSUES.md` (KI-045 resolved), `docs/CHANGELOG.md`/`docs/PROJECT_STATE.md`, and — as a necessary prerequisite discovered mid-pass — resolving unresolved git-merge-conflict markers left in `docs/CHANGELOG.md`, `docs/DEVLOG.md` (this file), and `docs/PROJECT_STATE.md` by the prior pass's merge of `results/pending-founder-review` into `main`.

**Implementation Summary — CAGR (Founder Decision 016, Option 1a)**: `backend/app/simulation/formulas.py::calculate_cagr` now multiplies its result by `Decimal(100)`, matching `calculate_total_return_percent`'s existing convention exactly. `DEFAULT_CALCULATION_VERSION` (`backend/app/simulation/engine.py`) bumped `"v1"` → `"v2"`; the two versions' semantics are documented in `docs/simulation_formulas.md` §4a and `docs/ARCHITECTURE_DECISIONS.md` ADR-040. A new Alembic data migration (`backend/alembic/versions/0004_cagr_percentage_v2_backfill.py`, no schema/DDL change) rescales every existing `completed`, `calculation_version = "v1"` row with a non-null `cagr_percentage` by exactly ×100 and re-stamps it to `"v2"`, idempotent by construction (a second run matches zero rows) and safe against concurrent inserts (the engine now always writes `"v2"` directly). Rows whose backfilled value would overflow `NUMERIC(10,6)`'s bound (a stored "v1" value `> 99.999999`) are excluded from the `UPDATE` and logged individually rather than silently truncated or failing the whole migration — none matched against this platform's real data. The known-answer test (`test_engine_known_answer.py::test_basic_growth_matches_founder_spec_2_14_7_example`) now hand-derives its expected CAGR independently (`years = 3653/365.25 = 10.001369`; `cagr_fraction = 2.5**(1/years) - 1 = 0.095944`; `× 100 = 9.594448`, shown in-line as a comment) rather than asserting the pre-fix raw fraction. Two further formula-level tests (`test_formulas.py`) needed the same `× 100` correction, found only by running the full suite, not anticipated from the founder brief alone. `docs/api_design.md`'s worked example corrected from the internally-inconsistent `"9.596872"`/`"v1"` to the independently-derived `"9.594448"`/`"v2"`. `explanation_service.py` (the dormant AI-prompt-context leak KI-045 named) required no code change — it reads `simulation.cagr_percentage` directly, which is now correct at the source.

**Implementation Summary — Opening Sequence (Founder Decision 017)**: `frontend/src/hooks/use-opening-sequence.ts` (the `composing`/`paused`/`answered`/`settled` phase machine) is deleted, replaced by `frontend/src/hooks/use-settle-in.ts` — a single boolean, captured once at mount via a lazy `useState` initializer (Strict-Mode-safe, matching `useJustCreatedFlag`'s prior pattern), paired with two `requestAnimationFrame` calls to trigger a CSS opacity/translate transition from a real "unsettled" starting paint. `opening-sequence-heading.tsx` now renders the full sentence and every child section immediately on mount — no phrase-by-phrase reveal, no silent pause, no "Skip" button (nothing left to skip); the only motion is a `duration-[var(--duration-transition)] ease-in` (220ms, the existing token, close enough to "~200ms") opacity/translate transition on the `<h1>` alone, fully skipped (settled from the first paint) when `prefers-reduced-motion` is set. `frontend/src/hooks/use-just-created-flag.ts` and its `?new=1` query marker are removed entirely — with no staged timeline left to gate, the mechanism (ADR-038) had no remaining purpose; `SimulationForm` now navigates to the plain `/simulation/{id}` URL for both `completed` and `failed` outcomes alike. `app/simulation/[id]/page.tsx`'s `<Suspense>` boundary is removed along with it — it existed only because `useSearchParams()` was called transitively via `useJustCreatedFlag`, and a full-tree grep confirmed nothing under `SimulationResultClient` calls it anymore. The temporary `dev/opening-sequence-preview` route is deleted (its own header comment scoped it as removable "once the opening sequence is manually confirmed" — there is no longer a multi-second timeline worth a dedicated preview harness). M7 Phase 3B.2's editorial redesign (ADR-039, `results-sections.tsx`) is entirely unmodified.

**Implementation Summary — Documentation Reconciliation (prerequisite, discovered mid-pass)**: `docs/CHANGELOG.md`, this file, and `docs/PROJECT_STATE.md` all contained unresolved `<<<<<<<`/`=======`/`>>>>>>>` git merge-conflict markers, committed directly to `main` by the prior pass's merge of `results/pending-founder-review` (confirmed: that branch is a strict ancestor of `main`, so the merge should have been a fast-forward or a clean reconciliation, not a literal conflict left in the file). Resolved by reconstructing the intended content from both sides of each conflict — for the append-only journals (`CHANGELOG.md`, `DEVLOG.md`), both sides' content was kept, reordered into correct chronological sequence (nothing deleted, matching `.claude/DOCUMENTATION_POLICY.md`'s "never delete historical information"); for `PROJECT_STATE.md` (an overwritten-not-appended dashboard), the conflict was resolved by rewriting the document to reflect actual current state, which happens to be this pass's own end state anyway. `docs/FOUNDER_DECISIONS.md`'s Founder Decision 016 had also been accidentally duplicated in full (two near-identical entries from the same drafting pass) — consolidated into one entry on approval, since a duplicated in-progress draft is a documentation defect, not two distinct historical decisions requiring separate superseding entries.

**Files Created**: `backend/alembic/versions/0004_cagr_percentage_v2_backfill.py`, `frontend/src/hooks/use-settle-in.ts`, `frontend/src/__tests__/lib/use-settle-in.test.tsx`.

**Files Modified**: Backend — `backend/app/simulation/formulas.py`, `backend/app/simulation/engine.py`, `backend/tests/simulation/test_engine_known_answer.py`, `backend/tests/simulation/test_formulas.py`, `backend/tests/api/test_simulations.py`. Frontend — `frontend/src/components/simulation-result/opening-sequence-heading.tsx`, `frontend/src/components/simulator/simulation-form.tsx`, `frontend/src/app/simulation/[id]/page.tsx`, `frontend/src/__tests__/components/opening-sequence-heading.test.tsx`, `frontend/src/__tests__/components/simulation-form.test.tsx`, `frontend/src/__tests__/components/simulation-result-client.test.tsx`, `frontend/src/__tests__/components/results-sections.test.tsx`. Docs — `docs/FOUNDER_DECISIONS.md`, `docs/ARCHITECTURE_DECISIONS.md`, `docs/KNOWN_ISSUES.md`, `docs/simulation_formulas.md`, `docs/api_design.md`, `docs/CHANGELOG.md`, `docs/PROJECT_STATE.md`, this file.

**Files Deleted**: `frontend/src/hooks/use-opening-sequence.ts`, `frontend/src/hooks/use-just-created-flag.ts`, `frontend/src/__tests__/lib/use-opening-sequence.test.tsx`, `frontend/src/__tests__/lib/use-just-created-flag.test.tsx`, `frontend/src/app/dev/opening-sequence-preview/` (entire route).

**Architecture Decisions**: ADR-040 (CAGR percentage-scale fix — version-string choice, backfill mechanism, `NUMERIC(10,6)` overflow bound check), ADR-041 (removing the staged opening-sequence timeline — `useSettleIn`, retiring `?new=1`; also marks ADR-038 Superseded).

**Problems Encountered**: (1) The two `test_formulas.py` unit tests asserting `calculate_cagr`'s raw-fraction output were missed on the first backend test run (only the DB-integration known-answer test had been anticipated from the founder brief) — surfaced immediately by running the full suite rather than only the file explicitly named. (2) Running the full backend suite twice in a row against the same persistent local Postgres produced 98 unrelated-looking failures on the second run (`relation "assets" does not exist`) — traced to `tests/test_migrations.py`'s own downgrade-to-`"base"` test, which intentionally empties the schema as its last action; CI re-applies migrations before every `pytest` invocation (`.github/workflows/ci.yml`) specifically because of this, a step this session's first manual run had skipped. (3) `docs/api_design.md`'s pre-fix worked example and Founder Decision 016's own draft text both cited a "correct" CAGR value (`9.596872` and `0.095969` respectively) that turned out to be imprecise relative to an independent hand-derivation using the same Decimal arithmetic the production code actually uses — neither figure was pasted from running code, but neither was exactly right either. (4) An unrelated NUMERIC(10,6) overflow-threshold constant was first written as `99.99999990` (nine 9s, one short) instead of the mathematically exact `99.999999` in the ADR-040 draft text.

**Solutions**: (1) Ran the complete `pytest -q` suite (not a targeted file) before considering the backend change complete, catching both additional call sites. (2) Re-ran `alembic upgrade head` before the second full-suite run, matching CI's own explicit "Apply migrations" step immediately preceding "Test (pytest)" — confirmed this was the actual cause, not a regression, by the suite passing 278/278 clean immediately after. (3) Independently derived the correct value using Python's `decimal.Decimal` with the exact same precision/rounding context the production code uses (`prec=38`, `ROUND_HALF_EVEN`, final `quantize` to `NUMERIC(10,6)`) rather than trusting either pre-existing cited figure, arriving at `9.594448` — then verified this matches what the live, running engine actually produces (see Testing Summary). (4) Recomputed the exact threshold (`9999.999999 / 100`, then bounded to the column's own 6-decimal storage precision) and corrected both the ADR-040 prose and the migration's `OVERFLOW_THRESHOLD` constant to `99.999999`.

**Lessons Learned**: A "derive independently" instruction is only actually independent if every reference figure along the way — including ones already present in draft documentation, not just the code under test — is re-derived from first principles rather than treated as ambient ground truth; a plausible-looking cited number in a founder-decision draft is not automatically correct just because it predates the fix. Separately: this project's CI workflow encodes an operational assumption (fresh migrations before every test run) that isn't obvious from reading the test files themselves — `tests/test_migrations.py`'s downgrade-to-base behavior is only "safe" in the context of that CI step, and reproducing CI's behavior locally (not just its test *count*) matters for getting an accurate signal from a full local suite run.

**Security Review Summary**: No new attack surface. The CAGR fix is a pure calculation-scale correction to an already-existing, already-served field — no new endpoint, no new input accepted. The backfill migration is a data-only `UPDATE`/`SELECT` (no DDL) scoped by `calculation_version`/`cagr_percentage IS NOT NULL`/an explicit overflow bound, logged rather than silent for the one case it can't safely handle. The Opening Sequence change removes a client-side-only timing mechanism and a query-string marker that never carried or gated any sensitive data — its removal narrows attack surface trivially (one fewer URL parameter, one fewer `useSearchParams` call site) rather than adding any.

**Testing Summary**: Backend: 278/278 tests passing (`pytest -q`, real Postgres via Docker Compose), `ruff check` and `black --check` clean on every changed file. Frontend: 38/38 test files, 176 passing / 4 gracefully skipped, `npx eslint .` clean, `npx tsc --noEmit` clean, `npx next build` succeeds (confirmed `/dev/opening-sequence-preview` no longer appears in the route list). Live end-to-end verification for the CAGR fix specifically: seeded synthetic AAPL price data via `dev_seed`, created a real simulation through the running API reproducing KI-045's exact cited scenario ($10,000 → $17,305.00 over 2020-01-02–2024-01-02), and confirmed the API returns `"total_return_percentage": "73.050000"` alongside `"cagr_percentage": "14.694581"` and `"calculation_version": "v2"` on both the `POST` and a subsequent `GET` — squarely in the "10–15%" territory expected for a +73% multi-year gain, not the pre-fix "+0.15%." The pre-existing live database row originally cited in KI-045 was independently confirmed correctly backfilled by the new migration (14.6885%, matching the issue's own stated "+14.69%"). No browser-automation tool was available in this environment to capture a rendered screenshot of the Results page itself; the frontend claims (immediate render, correct CAGR formatting, settle-transition behavior, reduced-motion behavior) are verified by the passing component/integration test suite exercising the real rendering code against realistic fixtures, and by `formatPercentage`'s unchanged, no-scaling-needed implementation confirmed by direct code reading — not by a live browser screenshot, stated here explicitly rather than implied.

**Technical Debt Introduced**: None judged unintentional. The backfill migration's `downgrade()` is deliberately not a perfect inverse for rows created after `upgrade()` runs (documented in ADR-040) — matching this project's existing migrations, none of which have ever been downgraded in production. The flagged-but-unbackfilled overflow carve-out (expected to match zero rows against real data, confirmed by this pass's actual backfill run) is a documented, logged exception, not a silent gap.

**Performance Notes**: No new query pattern — the backfill migration is a single one-time `UPDATE`/`SELECT` pair, not a recurring code path. `useSettleIn` replaces a multi-timer `setTimeout` schedule with two `requestAnimationFrame` calls, marginally cheaper and paint-synchronized rather than wall-clock-synchronized.

**Recruiter Value**: High. Demonstrates closing out two founder-flagged judgment calls in the same pass rather than leaving either open — one a hard financial-correctness defect requiring a real backfill-migration design (version string, idempotency, an explicit overflow bound with a non-destructive carve-out), the other a judgment call about product motion philosophy requiring translating a founder ruling into a minimal, principled replacement mechanism rather than either over-correcting to zero motion or under-correcting by keeping a smaller version of the rejected pattern. Also demonstrates catching and fixing a pre-existing, unrelated documentation defect (unresolved git-merge-conflict markers committed to three journal files) discovered only because this pass needed those files in a valid state to append to — the same "verify before you build on it" discipline this project has applied at every prior milestone.

**Production Readiness Score**: ~5/10 platform-wide, unchanged in headline number — this pass fixes a real defect and resolves an open design judgment call rather than adding new production-blocking surface; KI-016 and KI-039 remain the two items most load-bearing for an actual launch decision.

**Next Milestone**: M7 Phase 3C — the full Results Experience (growth chart, composition/splits disclosure, AI panel), no longer blocked by KI-045 or the motion-philosophy finding (both resolved this pass). Remaining prerequisites unchanged: Founder Decision 014's `growth_series` persistence (approved, not yet implemented) and Founder Decision 015's AI rate-limit specifics (approved, not yet implemented) for the AI panel specifically.

---

## 2026-07-23 — CI Red on `main`: Diagnosis and Fix

**Version**: 0.11.1 (proposed — no feature work, a CI-only fix)

**Objective**: `main`'s CI ("CI / lint-and-test" and "CI / frontend-lint-and-test") was reported red after the 0.11.0 push (`15935ec`). Diagnose the real, exact failure per job (not assumed) and apply the minimal correct fix.

**Scope**: Diagnosis via `gh run view --log-failed` against the actual failing GitHub Actions run, followed by a local, step-by-step reproduction of both CI jobs exactly as `.github/workflows/ci.yml` specifies (not the normal local dev-loop commands). Fix scoped to exactly the two failing steps. No feature code touched.

**Implementation Summary**: Pulled the exact failure text via `gh run view 29056012575 --log-failed`. **`lint-and-test` (backend)** failed at the `Lint (ruff)` step: `app/ingestion/providers/__init__.py:31:101: E501 Line too long (102 > 100)`. **`frontend-lint-and-test`** failed at the `Build` step: `next build` threw `Error: Invalid environment configuration: [{ "path": ["NEXT_PUBLIC_API_BASE_URL"], "message": "Invalid input: expected string, received undefined" }]` while prerendering `/simulator`.

Investigated root cause per job before fixing either, per instruction not to assume the most recent push caused it:
- The offending `E501` line was introduced at commit `ad0e673` (2026-07-07, "feat: implement simulator experience"). `git log --oneline` cross-referenced against `gh run list --json headSha,conclusion` showed **every commit from `ad0e673` through `15935ec` (8 consecutive pushes, 2026-07-07 through today) reported CI failure** — this defect predates the 0.11.0 work entirely; the CAGR/Opening-Sequence push merely continued riding on top of an already-broken `main`. Root cause: prior sessions verified lint by running `ruff check <the files they'd changed>` rather than `ruff check .` (the whole repo, exactly what CI runs), so this pre-existing violation in an untouched file was never re-surfaced.
- The `NEXT_PUBLIC_API_BASE_URL` failure is `frontend/src/config/env.ts` working exactly as designed — a deliberate fail-fast guard (`docs/SECURITY_LOG.md`'s documented deployment-safety fix: a production build missing this var must fail to build, not silently ship pointed at `localhost`). The real gap: `frontend/.env.local` (gitignored, present in every local working tree, absent from every CI checkout) silently supplies the var for every local `npm run build`, making this exact failure mode structurally invisible to a normal local dev-loop command. `git log` on `.github/workflows/ci.yml` confirmed the workflow's frontend job has never set this var at any point since it was introduced — also traced to commit `ad0e673`, the same commit that introduced the `E501` line, both defects shipping together and both silently masked by locally-only state (an untouched file for one, a gitignored file for the other) ever since.

Confirmed both are genuinely CI-only, pre-existing gaps — not regressions the CAGR/Opening-Sequence work introduced — by reproducing CI's exact steps locally: created a fresh `itm_test` Postgres database (matching the workflow's ephemeral `postgres:16-alpine` service container exactly, not the persistent local `itm_dev` database used for normal dev work) and ran `ruff check .` / `black --check .` / `alembic upgrade head` / `pytest --maxfail=1` in that exact order; separately, moved `frontend/.env.local` aside and ran `npx eslint .` / `npx tsc --noEmit` / `npx vitest run` / `npm run build` in that exact order. The `next build` failure reproduced byte-for-byte identical to CI's logged error only once `.env.local` was actually absent — confirming the `.env.local`-masking theory precisely, not just plausibly.

Checked the task's other suspected culprits explicitly, both ruled out: (a) the new `0004_cagr_percentage_v2_backfill.py` migration runs cleanly against a genuinely empty database (`WHERE calculation_version = 'v1'` matches zero rows, no error) — confirmed by running `alembic upgrade head` against the freshly-created `itm_test` database and observing `rescaled 0 row(s)`; (d)/(e) no lint rule, flag, deleted-file reference, or version-pin drift (Node 20 confirmed locally matching the workflow's pin) was implicated in either failure. (b) line endings were checked (`.gitattributes` exists, no CRLF violations found in any file this pass touched) and ruled out as a contributor to either failure.

**Files Created**: None.

**Files Modified**: `backend/app/ingestion/providers/__init__.py` (wrapped `__all__` to multiple lines, under 100 chars), `.github/workflows/ci.yml` (added a job-level `env: NEXT_PUBLIC_API_BASE_URL: http://localhost:8000` to `frontend-lint-and-test`, matching `frontend/.env.example`'s documented local-dev placeholder), `docs/KNOWN_ISSUES.md` (new KI-046), `.claude/REVIEW_CHECKLIST.md` (new PR-checklist item requiring CI's exact steps be reproduced, not approximated, before claiming "green").

**Architecture Decisions**: None — both fixes are corrections to existing, already-approved design (the `E501` line was always a style violation; `env.ts`'s fail-fast behavior is unchanged, only CI's own configuration of it was missing), not new architectural decisions.

**Problems Encountered**: (1) The task's framing assumed the 0.11.0 push caused the red CI — cross-referencing `gh run list` against `git log` showed this was false; CI had been red for 8 prior commits. Caught by actually checking history rather than accepting the framing, the same discipline this project has now applied repeatedly (KI-043, ADR-037, the M7 Phase 3B.1 `inline-block` bug, KI-045, and now this). (2) The `NEXT_PUBLIC_API_BASE_URL` failure could not be reproduced by any normal local command — every local `npm run build` succeeded, because `.env.local` (never committed, never seen by CI) always supplied the var. Required deliberately moving the file aside to see CI's actual environment, not just running the same command again.

**Solutions**: (1) `gh run list --json headSha,conclusion` cross-referenced against `git log --oneline`, walking backward from the current failure to find exactly where CI first turned red, rather than assuming "most recent push = cause." (2) Temporarily relocated `frontend/.env.local`, confirmed the exact CI error text reproduced locally, applied the fix (workflow-level env var, since the code's fail-fast behavior is correct and intentional — this is a CI configuration gap, not a code bug), then confirmed the build succeeds with `.env.local` still absent and the new env var present, restoring `.env.local` only after both states were verified.

**Lessons Learned**: "Locally green" and "CI green" are not the same claim whenever local state (an `.env.local` file, a persistent dev database with pre-applied migrations, a narrower lint invocation scoped to changed files) differs from what CI actually starts from on every run. The only reliable way to verify "CI will pass" is to literally read the workflow file and execute its exact steps in a matching environment — not to run the closest-feeling local dev command and infer equivalence. Recorded as a standing guardrail (KI-046, `.claude/REVIEW_CHECKLIST.md`) so future sessions don't have to rediscover this.

**Security Review Summary**: No new attack surface. `NEXT_PUBLIC_API_BASE_URL: http://localhost:8000` in the CI workflow is a public, non-secret build-time placeholder (`NEXT_PUBLIC_*` vars are inlined into the client bundle by design — never a place secrets belong) identical to the value already documented in `frontend/.env.example`, and CI's `Build` step is a static/prerender-only build that never actually issues a request to it. The `__all__` line-wrap is a pure formatting change.

**Testing Summary**: Both CI jobs reproduced locally end-to-end and confirmed passing: backend — `ruff check .` (clean), `black --check .` (clean, 137 files unchanged), `alembic upgrade head` against a fresh `itm_test` database (clean, migration `0004` correctly matches 0 rows), `pytest --maxfail=1` (278 passed) — all against the fresh database, not the persistent dev one. Frontend — `npx eslint .` (clean), `npx tsc --noEmit` (clean), `npx vitest run` (179 passed, 1 skipped, without `.env.local`), `npm run build` (succeeds, all 6 routes generated including `/simulator`) — all with `frontend/.env.local` absent and only the CI-equivalent env var set. Regression check: full backend suite re-run against the normal persistent `itm_dev` database afterward, still 278/278 passing, confirming the fix introduced no change to normal dev-loop behavior.

**Technical Debt Introduced**: None. The systemic gap this incident surfaced (local verification not reproducing CI's exact environment) is now a documented, standing checklist item, not a newly-introduced debt.

**Performance Notes**: N/A — no runtime code changed; a lint-only formatting change and a CI workflow environment-variable addition.

**Recruiter Value**: High. Demonstrates real incident-response discipline under a false initial premise (the task assumed the most recent push caused the outage; verifying against actual CI history disproved that and found the true, older root cause instead of patching the wrong story), root-causing two independent failure classes precisely (a scoped-vs-full lint invocation gap, and a gitignored-local-file masking a CI-only failure mode) rather than guessing and re-pushing, and converting the incident into a durable process guardrail rather than a one-off fix.

**Production Readiness Score**: ~5/10 platform-wide, unchanged — this pass restores CI's own signal integrity (a prerequisite for trusting any future CI result) rather than changing product behavior.

**Next Milestone**: Unchanged — M7 Phase 3C, pending Founder Decision 014/015 implementation. Founder should confirm CI is green on the actual GitHub Actions run once these fixes are committed and pushed (this pass reproduced CI's steps locally byte-for-byte, but only a real CI run confirms the GitHub-hosted runner environment matches in every respect not locally reproducible, e.g. runner OS-level defaults).

---

## 2026-07-23 — M7 Phase 3C-2: Growth Series Persistence (Founder Decision 014) + KI-016 Verification

**Version**: 0.12.0 (proposed)

**Objective**: Implement Founder Decision 014 (Option A — persist `growth_series` at simulation creation, backfill pre-existing completed rows, version against `calculation_version`, never return an empty `growth_series` for a completed simulation), closing KI-021 in full. Separately, execute the KI-016 manual verification runbook (empirically confirm that raw `close_price` from yfinance is already retroactively split-adjusted, the assumption Founder Decision 001 depends on) against live data.

**Scope**: Backend only for Part 1 — `backend/app/models/simulation.py`, a new Alembic migration, `backend/app/simulation/engine.py`, `backend/app/simulation/growth_series_codec.py` (new), `backend/app/simulation/backfill_growth_series.py` (new), `backend/app/api/v1/services/simulation_service.py`, `backend/app/api/v1/routers/simulations.py`, `backend/app/api/v1/schemas/simulations.py` (docstring only). `frontend/src/types/api.ts` doc comments only — no shape change, confirmed against the running backend's `/openapi.json`. Part 2 is a read-only live-network verification exercise with no code changes.

**Implementation Summary — Part 1 (Founder Decision 014, Option A)**: Added a nullable `simulations.growth_series` JSONB column (`alembic/versions/0005_growth_series_persistence.py`, schema-only — an `ALTER TABLE ... ADD COLUMN`, matching `0003`'s precedent for a non-`0001` addition). `app/simulation/growth_series_codec.py` is a new, small module shared by the write path (engine) and read path (service): `serialize_growth_series` stores each point as a `point_date`/`value` object, `value` always a fixed-point string via `format(v, "f")`, never a JSON number, matching `DecimalStr`'s existing Decimal-safe convention exactly; `deserialize_growth_series` is its exact inverse. `run_simulation` (`engine.py`) now persists the computed series onto the `Simulation` row at creation for the `COMPLETED` branch only — the `FAILED` branch's `Simulation` constructor never receives a `growth_series` argument, so it stays real SQL NULL. The `POST` response shape is unchanged, confirmed by every existing `test_simulations.py` assertion continuing to pass unmodified. `simulation_service.get_simulation_by_id` now returns a three item result: the `Simulation`, its `disclosed_splits`, and its `growth_series`. `growth_series` is deserialized straight from the persisted column (never recomputed), and `disclosed_splits` is re-queried fresh from `stock_splits` on every `GET` via the same repository range query the engine itself uses at creation — no new column for splits, per Founder Decision 014 clause 5 (they already live queryably elsewhere). The router was updated to unpack and pass all three into `SimulationResponse.from_simulation`, which already accepted both parameters (only the `GET` call site had been omitting them). `backend/app/simulation/backfill_growth_series.py` is a new, standalone operator script, not an Alembic migration, since recomputing a series requires replaying `calculate_growth_series` against stored price/dividend history — Python calculation logic this project deliberately keeps out of raw SQL (ADR-016). It selects every completed row with a null `growth_series`, re-derives the exact inputs `run_simulation` itself would have used from that row's own stored fields, and writes the result stamped onto that row's own, already-stored `calculation_version` — never a newer one (clause 3). A row whose underlying price/dividend data can no longer support a recompute is left at NULL and reported in a skipped list, never allowed to fail the whole run (mirrors ADR-013's per-record isolation and ADR-040's own logged-carve-out precedent). Full engineering record in `docs/ARCHITECTURE_DECISIONS.md` ADR-042.

**Implementation Summary — Part 2 (KI-016 verification)**: Attempted the literal runbook first — the app's own yfinance-backed ingestion CLI — and it reproduced KI-044 exactly (debug mode showed the crumb endpoint returning the cached literal string "Edge: Too Many Requests" and HTTP 429 on every attempt). Three retries with a 20-second backoff did not clear it; NVDA (the runbook's own alternate) failed identically, ruling out a symbol-specific cause. Since the underlying Yahoo data endpoint itself is reachable and returns valid JSON without a crumb when called directly with a standard User-Agent header — the same fact KI-044's own original investigation established — fetched real AAPL daily OHLC and split-event data directly from that endpoint (a read-only GET, no code changed, nothing written via the app's ingestion pipeline through this path) to answer the runbook's actual empirical question. Confirmed: Yahoo's close series (the field the app's provider maps to `historical_prices.close_price`) shows AAPL at $124.8075 on 2020-08-28 (the last trading day before the 2020-08-31 4-for-1 split) — an exact match to AAPL's real, documented nominal close of $499.23 divided by 4. The response's own split-events data independently corroborates the 4-for-1 split on 2020-08-31. Full detail recorded in `docs/KNOWN_ISSUES.md` KI-016.

**Files Created**: `backend/alembic/versions/0005_growth_series_persistence.py`, `backend/app/simulation/growth_series_codec.py`, `backend/app/simulation/backfill_growth_series.py`, `backend/tests/simulation/test_growth_series_codec.py`, `backend/tests/simulation/test_backfill_growth_series.py`.

**Files Modified**: `backend/app/models/simulation.py`, `backend/app/simulation/engine.py`, `backend/app/api/v1/services/simulation_service.py`, `backend/app/api/v1/routers/simulations.py`, `backend/app/api/v1/schemas/simulations.py` (docstring only), `backend/tests/api/test_simulations.py`, `backend/tests/simulation/conftest.py` (added a `make_split` fixture helper), `frontend/src/types/api.ts` (doc comments only), `docs/ARCHITECTURE_DECISIONS.md` (new ADR-042), `docs/KNOWN_ISSUES.md` (KI-021 resolved, KI-016 resolved), `docs/CHANGELOG.md`, `docs/PROJECT_STATE.md`, this file.

**Architecture Decisions**: ADR-042 (`growth_series` persistence — JSONB column choice, string-serialized values via a dedicated codec module, `disclosed_splits`' no-new-column re-query design, the script-based backfill mechanism, and the skip-and-report unrecoverable-row edge case).

**Problems Encountered**: (1) The first version of the new backfill test file failed all 4 of its new tests — the backfill's null-series selection query matched zero rows even immediately after a test explicitly set `growth_series` to `None` and flushed. Root cause, confirmed via SQL echo logging: SQLAlchemy's JSON(B) type defaults to treating a Python `None` as the JSON literal null (a real, non-NULL stored value), not SQL NULL, unless `none_as_null=True` is set on the column type — silently breaking any IS NULL filter against it. This is a genuine, general JSONB footgun, not specific to the test: any future code path that ever explicitly reassigns a JSONB column back to `None` would hit the identical bug. (2) The local dev Postgres database was found in a schema-less state at the start of this session — a live instance of the exact behavior KI-009 already documents: the migration test suite's own downgrade-to-base test empties the schema as its last action, and a prior session's local test run had left it that way. (3) Docker Desktop was not running at the start of this session, blocking both the DB-integration test suite and any live end-to-end verification until it was started and the postgres/redis/backend containers came up.

**Solutions**: (1) Changed the model column definition to use `JSONB(none_as_null=True)`, with an inline comment explaining why this opt-in is necessary. All 4 backfill tests then passed as originally written, and the full suite confirmed no other test relied on the old, broken behavior. (2) Ran `alembic upgrade head` before both this session's DB-integration test runs and the live end-to-end verification, restoring the schema each time it was needed — matching this project's own established review-checklist guardrail (KI-046) of reproducing the exact steps a clean environment requires, not assuming persistent local state is already correct. (3) Started Docker Desktop and waited for the daemon; the already-defined containers came up automatically once it was ready.

**Lessons Learned**: A whole-attribute reassignment to `None` on a JSON(B)-typed SQLAlchemy column is not the same as leaving that attribute unset — the former can silently write a JSON null value instead of SQL NULL unless `none_as_null=True` is set explicitly, and this only surfaces the moment something filters on IS NULL against that column, which can be arbitrarily far downstream from wherever the `None` was assigned. Any future JSON(B) column on a nullable field in this codebase should default to `none_as_null=True` unless there's a specific reason a stored JSON null needs to be distinguishable from "column never set" — no such case exists in this codebase today.

**Security Review Summary**: No new attack surface. `growth_series` persistence stores data the engine already computed and already served on `POST` — nothing new is computed, accepted as input, or exposed that wasn't already part of the approved `POST` response contract; the `GET` path now serves the same data it always should have, closing a gap rather than opening one. The backfill script is a manually-invoked, read/write-to-`simulations`-only operator tool with no HTTP surface, mirroring the existing ingestion CLI's precedent. The KI-016 verification's direct-endpoint fetch was a read-only GET against Yahoo Finance's own public, unauthenticated chart data endpoint — no credentials, no write, no data stored via this path.

**Testing Summary**: Backend: 288/288 tests passing (real Postgres via Docker Compose), `ruff check .` and `black --check .` clean across the full repo, not scoped to changed files. Frontend: 38/38 test files, 179 passing / 1 gracefully skipped (an unrelated, pre-existing "backend unreachable" skip case), typecheck and lint clean. The API contract-drift test was run live against the actual running backend, not its graceful-skip path — all 4 live-schema assertions passed, confirming the growth-series/split field names needed no correction. Live end-to-end: ingested real dev-seed AAPL data, created two live simulations through the running API (one plain, one with dividends enabled to exercise the reinvestment path through the persisted series too), and confirmed byte-for-byte identical `growth_series`/`disclosed_splits` between each `POST` response and a subsequent `GET` for the same simulation — the literal closing condition Founder Decision 014 clause 4 requires. The backfill script itself was run against the live dev database (0 rows matched — the dev database currently has zero pre-existing completed simulations to backfill; the script's logic is independently verified correct by its own 5-test suite using a run-then-null-then-recompute fixture pattern that exercises the exact same code path a real pre-existing row would).

**Technical Debt Introduced**: None judged unintentional. The backfill script's skip-and-report behavior for an unrecoverable row is a documented, logged exception to "never empty for a completed simulation," not a silent gap — see ADR-042 and the updated KI-021. The KI-016 verification's direct-endpoint methodology deviation is fully disclosed in KI-016 itself, including that it does not confirm the app's own yfinance provider pipeline is currently usable (KI-044 remains open, unresolved, reconfirmed by this session).

**Performance Notes**: `disclosed_splits` is now computed on every `GET`, not only `POST` — one additional small, indexed range query per retrieval, judged negligible against this platform's existing query volume. `growth_series` on `GET` is now a JSONB deserialize in-process instead of an empty-list default — also negligible.

**Recruiter Value**: High. Demonstrates closing out a founder-approved persistence design end to end — schema, write path, read path, and a script-based backfill with an honest, tested unrecoverable-row carve-out — rather than a partial implementation that leaves the "never empty" guarantee aspirational. Also demonstrates catching a real, non-obvious ORM correctness bug (JSONB None vs. SQL NULL) through actual test failures rather than assuming a straightforward column addition needed no scrutiny, and demonstrates methodological transparency under a real external blocker (KI-044 still active) — obtaining a genuine, verifiable empirical answer to KI-016's question via the same real data source, while being explicit about exactly what was and wasn't exercised, rather than either fabricating a result or giving up at the first blocked path.

**Production Readiness Score**: ~5/10 platform-wide, unchanged in headline number, but two of the small number of items previously named as most load-bearing for a launch decision are now closed: KI-021 (growth chart data availability) and KI-016 (the single largest financial-correctness risk in the M3 design) are both Resolved as of this pass. KI-039 (custom-domain/SameSite=Strict) remains the most load-bearing open item for an actual deployed environment; KI-044 (yfinance pipeline reliability) remains open and is now more clearly the actual blocker for live yfinance ingestion specifically, separate from the split-consistency question KI-016 answered.

**Next Milestone**: M7 Phase 3C — the growth chart itself can now be built against real, persisted data on both `POST` and `GET` (the backend precondition Founder Decision 004 decision 5 named is now satisfied). Founder Decision 015's AI rate-limit implementation remains the other prerequisite for M7 Phase 4 specifically, unaffected by this pass.

---

## 2026-07-24 — M7 Phase 3C-3: Growth Chart, Why, and The Proof

**Version**: 0.13.0 (proposed)

**Objective**: Complete the Results Experience's remaining depth per approved philosophy — the Growth Chart (now buildable against real data since M7 Phase 3C-2/ADR-042), the full three-paragraph Why explanation, and The Proof's methodology/assumptions/provenance/accessible-data-table. Frontend-only; no backend code changed.

**Scope**: `frontend/src/components/simulation-result/growth-chart.tsx` (new), `chart-plot-value.ts` (new), `results-sections.tsx` (rewritten `GrowthOverTime`/`WhyExplanation`/`TheProof`), `frontend/src/hooks/use-asset-detail.ts` (new), `frontend/vitest.setup.ts` (Recharts jsdom compatibility), new/updated tests, and documentation.

**Implementation Summary — Growth Chart**: `GrowthChart` (`growth-chart.tsx`) renders `growth_series` as a single-hue Recharts line (`--color-chart-portfolio`, always — identical treatment for a gain or a loss, per EXPERIENCE_CONSTITUTION.md §6/§7), placed after Supporting Facts and before Why, with no card chrome — quiet axes (X shows a handful of real date ticks via `formatDate`; the Y-axis carries no tick labels at all, avoiding the question of formatting a synthetic, non-sourced axis value). Three honest, non-crashing states beyond the normal ≥2-point case: an empty series (a rare, logged Founder-Decision-014 backfill-skip edge case, not the pre-fix KI-021 default) renders a calm explanatory fallback naming the simulation ID; a single-point series renders the one point as text plus a dot, explicitly not drawn as a line, since a line would imply an interpolated trajectory that was never observed. Every displayed figure (tooltip, the "This chart traces..." summary sentence, split disclosures) is formatted from the *original* `DecimalString` via `src/lib/format`, never from the number Recharts needs internally for plotting geometry — that one narrow, disclosed conversion lives in a new file, `chart-plot-value.ts`, deliberately kept *outside* `src/lib/format/` so that module's own static guardrail test (which scans every file in it for banned numeric-coercion tokens) remains unconditionally true with zero carve-outs; the one `Number()` call site carries an explicit, justified `eslint-disable-next-line` and a full doc comment, per ADR-043. Disclosed splits are marked on the chart with a subtle dashed `ReferenceLine` when the split date lands on an actual plotted point, and are always disclosed in plain language beneath the chart regardless (e.g. "A 4-for-1 stock split occurred on Aug 31, 2020. Prices shown are adjusted; your return is unaffected.") — a reverse split is labeled honestly by its raw ratio rather than a fabricated "1-for-N" figure this codebase's financial-math guardrail (ADR-029/033) cannot safely derive (inverting a ratio is division). Motion is a single ~200ms settle (`useSettleIn`, the same mechanism `opening-sequence-heading.tsx` already uses), fully static under `prefers-reduced-motion`; Recharts' own default line-draw-in animation is explicitly disabled (`isAnimationActive={false}`) — a deliberate divergence from `frontend_design_system.md` §8–9's pre-Founder-Decision-017 "line draws in once, markers appear with a delay" language, since that staged-reveal pattern is exactly what Founder Decision 017 rejected for the hero sentence one milestone earlier and the Experience Constitution's motion rule doesn't carve out an exception for charts.

**Implementation Summary — Why**: `WhyExplanation` composes three deterministic, template-based paragraphs from the simulation's own fields — no AI call anywhere. Price appreciation now names the shares this investment purchased, not only the initial/final price. The dividend paragraph has two states, not the originally-specified three: `include_dividends` true or false. The third state ("the asset paid no dividends at all in this range") is a disclosed, deliberate scope decision, not an oversight — `SimulationResponse` exposes no dividend-event count or flag, and deriving one from other fields would require exactly the frontend-side financial arithmetic (`investment_amount / initial_price`, compared against `shares_purchased`) this codebase's guardrails permanently forbid; the reinvestment case is phrased with an explicit "any... if any occurred" hedge instead, true regardless of whether zero or many events actually happened. The inflation paragraph is now correctly omitted entirely when `adjust_for_inflation` is false, rather than rendered with filler copy explaining a choice the user already made and already sees in Supporting Facts — a direct behavior change from the M7 Phase 3B.2 stub this replaces.

**Implementation Summary — The Proof**: Methodology now states the `close_price`-not-`adjusted_close_price` policy, the dividend-reinvestment compounding mechanism, and the 365.25-day-per-year CAGR convention, each sourced directly from `docs/simulation_formulas.md` and rewritten for a general reader. Assumptions adds the exact-date-price policy (no silent nearest-trading-day substitution), the dividend timing convention (ex-dividend date, chronological order), and the CPI as-of lookup (most recent reading on or before, never interpolated) — all previously undocumented on this page. The former "Technical details" subsection is renamed Provenance and gains a "Data source" line, fetched lazily via a new `useAssetDetail` hook only once the disclosure is actually opened (tracked via the native `<details onToggle>` event) — a supplementary provenance detail never delays or complicates the page's primary load. A new "Growth chart data" subsection renders every `growth_series` point as a real, keyboard-navigable `<table>` — the chart's actual text alternative (not merely a nice-to-have), paired with a short visible summary sentence immediately below the chart itself as the first-line accessible description.

**Files Created**: `frontend/src/components/simulation-result/growth-chart.tsx`, `frontend/src/components/simulation-result/chart-plot-value.ts`, `frontend/src/hooks/use-asset-detail.ts`, `frontend/src/__tests__/components/growth-chart.test.tsx`, `frontend/src/__tests__/lib/chart-plot-value.test.ts`, `frontend/src/__tests__/lib/use-asset-detail.test.tsx`.

**Files Modified**: `frontend/src/components/simulation-result/results-sections.tsx`, `frontend/vitest.setup.ts` (ResizeObserver + `getBoundingClientRect` stubs for Recharts under jsdom), `frontend/src/__tests__/components/results-sections.test.tsx`, `frontend/src/__tests__/components/simulation-result-client.test.tsx` (BASE_SIM now carries a realistic, populated `growth_series`, matching post-M7-Phase-3C-2 reality), `docs/ARCHITECTURE_DECISIONS.md` (new ADR-043), `docs/CHANGELOG.md`, `docs/PROJECT_STATE.md`, this file.

**Architecture Decisions**: ADR-043 (the Growth Chart — the disclosed `toChartPlotNumber` numeric-coercion exception and why it's scoped outside `src/lib/format/`, honest reverse-split disclosure without derived arithmetic, the two-state dividend paragraph and why the third state is a disclosed gap not a bug, Founder-Decision-017-governed chart motion superseding `frontend_design_system.md`'s older chart-motion language, the summary-sentence-plus-Proof-table accessible-alternative shape, and the lazy `useAssetDetail` provenance fetch).

**Problems Encountered**: (1) Recharts' `ResponsiveContainer` requires `ResizeObserver` (unimplemented in jsdom) and real, non-zero measured layout geometry (jsdom always reports 0×0) to render its SVG children at all — every chart test would have silently rendered an empty container without a fix. (2) Recharts appends a persistent `#recharts_measurement_span` directly to `document.body` (outside the React tree Testing Library's `cleanup()` unmounts) for internal text-width measurement — a leftover span from one test's chart collided with a later, unrelated test's `screen.getByText('Jan 1, 2025')` assertion in the same file, since both happened to contain the same date string. (3) `fireEvent.click()` on a `<summary>` element does not natively toggle jsdom's `<details>` `open` state or dispatch a `toggle` event, unlike a real browser — the first version of the "fetches the data source once opened" test silently never opened the disclosure. (4) The single-point-series chart state initially rendered a Recharts `LineChart` with one point, which — while not literally crashing — did draw a degenerate, uninformative single-dot chart; re-read against the task's explicit "render honestly, no fake interpolation" instruction, this was changed to a plain text+dot presentation with no chart apparatus at all.

**Solutions**: (1) Added a global `ResizeObserver` stub and a `getBoundingClientRect` override returning a fixed 600×300 to `vitest.setup.ts` — verified safe by grepping the existing suite for any test relying on real geometry (`grep -r "getBoundingClientRect\|ResizeObserver"` — zero prior hits). (2) Scoped the colliding assertions to `within(table)` (Testing Library's DOM-subtree-scoped query), documented inline as a known, disclosed Recharts quirk rather than a flaky test needing an unrelated fix. (3) Set the `<details>` element's `open` property directly and dispatched a real `toggle` `Event` via `fireEvent`, matching what a real browser's click-to-toggle actually produces at the DOM level. (4) Rewrote the single-point branch to render `SinglePointFallback` — one formatted date/value pair and an explanatory sentence, no Recharts component involved at all.

**Lessons Learned**: A charting library's own internal implementation details (a global measurement DOM node, a `ResizeObserver` dependency, zero-geometry behavior under jsdom) are real testing surface area that a component-level test suite has to account for explicitly — treating them as "just render it and check the text" surfaces subtle, hard-to-diagnose cross-test pollution rather than a clean failure. Separately: a financial-math guardrail strict enough to ban `Number()` everywhere except one module forces a genuine design decision the moment a feature (a chart) has a real, unavoidable technical need for a JS number — the right response is a narrow, heavily-disclosed, single-call-site exception with its own test and ADR, not weakening the guardrail's own verification mechanism to accommodate it.

**Security Review Summary**: No new attack surface. The Growth Chart and Why/Proof sections render only data the API already returns and already serves elsewhere on the same page — no new input is accepted, no new financial calculation occurs (the frontend continues to display, never derive). `useAssetDetail` calls an existing, already-implemented, unauthenticated `GET /api/v1/assets/{symbol}` endpoint with no new parameters. The one numeric-coercion exception (`toChartPlotNumber`) is scoped to chart pixel geometry only and never influences a displayed figure, a comparison, or a calculation — verified by both its own unit test and the growth-chart component tests' consistent use of `src/lib/format` for every displayed value.

**Testing Summary**: Frontend: full suite green (`npx vitest run`), `npx eslint .` clean, `npx tsc --noEmit` clean, `npx next build` clean with `frontend/.env.local` moved aside and only the CI-equivalent `NEXT_PUBLIC_API_BASE_URL` env var set (the KI-046 CI-parity guardrail). New/updated test coverage: chart states (empty/single-point/normal/loss-vs-gain-identical-treatment/split-marker-present/split-marker-absent/reverse-split-honesty/reduced-motion), Why paragraph composition across all implemented dividend/inflation variants (including the inflation-omitted-when-not-requested behavior change), The Proof's methodology/assumptions/provenance/lazy-data-source-fetch/accessible-table content, and the disclosed `toChartPlotNumber` conversion itself. Live end-to-end: re-ingested `dev_seed` AAPL data (the dev database was found empty at session start — the prior session's data had not persisted), created a real simulation through the running API with dividends and inflation adjustment both requested, and rendered the actual `SimulationResultClient` component tree — unmocked, against the real running backend, via the same React Testing Library infrastructure the automated suite uses — confirming the full reading order (sentence → Supporting Facts → Growth Chart with a real SVG and 1,000-point series → Why, including the live CPI-unavailable paragraph since `dev_seed` ingests no CPI data → The Proof, opened, showing the complete real growth-data table and the lazily-fetched data source). No browser-automation tool (Playwright/Puppeteer or equivalent) was available in this environment, so no pixel-level visual screenshot was captured — stated explicitly rather than implied; the live check above exercises the real component tree, real network calls, and real backend data, which is a strong functional verification but not a substitute for a human or automated visual review of actual rendered pixels.

**Technical Debt Introduced**: The dividend paragraph's missing third state ("this asset paid no dividends in this range") is a disclosed, deliberate scope gap — closing it properly requires a new backend signal (a dividend-event count or flag on `SimulationResponse`), tracked in ADR-043's Future Implications rather than left as a silent limitation. No other technical debt judged unintentional.

**Performance Notes**: The Growth Chart adds `recharts` to this page's client bundle (already a locked dependency, already installed — no new package). `useAssetDetail`'s lazy, open-triggered fetch means the common case (a user who never opens The Proof) never issues that request at all.

**Recruiter Value**: High. Demonstrates navigating a real architectural tension (a charting library's hard technical requirement for JS numbers vs. a strict, test-enforced financial-precision guardrail) with a disclosed, minimally-scoped, independently-tested exception rather than either quietly bypassing the guardrail or refusing to ship the feature — and demonstrates the same discipline applied to a missing data signal (the dividend paragraph's third state) by implementing exactly what the data honestly supports rather than fabricating a plausible-looking guess, consistent with this project's standing "never fabricate, never estimate" principle extended to the frontend layer.

**Production Readiness Score**: ~5/10 platform-wide, unchanged in headline number — this pass completes a planned feature (the Results Experience's remaining depth) rather than closing a production-blocking gap. KI-039 remains the most load-bearing open item for an actual deployed environment.

**Next Milestone**: M7 Phase 4 — the Educational AI Experience, pending Founder Decision 015's rate-limit implementation. The Results Experience (M7 Phase 3B through 3C-3) is now functionally complete pending founder review.

---

## 2026-07-25 — M7 Phase 3D: Design Elevation

**Version**: 0.14.0 (proposed)

**Objective**: Implement the founder-approved visual mockup across the Results Experience and the Simulator — a serif/mono editorial type system, a warm near-black elevated dark palette with a static grain/gradient atmosphere, a one-shot digits-only scramble/settle on every financial figure, an upgraded Growth Chart treatment, and a flattened, borderless Simulator artboard — per direct instruction, with Founder Decision 018 (Motion & Atmosphere Amendment) formalizing the motion rules this pass implements.

**Scope**: Frontend only. `docs/FOUNDER_DECISIONS.md` (new Founder Decision 018), `docs/ARCHITECTURE_DECISIONS.md` (new ADR-044), `frontend/src/styles/tokens/*.css`, `frontend/src/app/globals.css`, `frontend/src/app/layout.tsx`, `frontend/src/hooks/use-scramble.ts` (new), `frontend/src/components/simulation-result/*`, `frontend/src/components/simulator/*`, `frontend/src/app/simulator/page.tsx`, `frontend/src/app/simulation/[id]/page.tsx` (untouched — only its client component changed), `frontend/src/lib/format/index.ts` (one new re-export), tests, `docs/KNOWN_ISSUES.md` (two new entries). The root `/` page and `/dev/playground` are explicitly untouched, per direct instruction.

**Implementation Summary — Mockup discrepancy resolved first**: The task named the approved mockup as `DOCUMENTS_AND_IDEAS/design-mockup-v2.html`, committed. That path does not exist. Located the actual mockup by content match — `DOCUMENTS_AND_IDEAS/Investment Time Machine Results/Investment Time Machine.dc.html` (exact `scramble()`/`sectionRise`/`chartDraw`/`popScale` mechanism and oklch palette) — inside an **untracked** folder (`git status` shows `??`), not committed as the task described. Confirmed the discrepancy and the choice to proceed against this file with the founder directly (`AskUserQuestion`) before implementing anything, rather than guessing. A sibling file in the same folder, `Dark Pool Liquidity Tide.dc.html`, is an unrelated, non-approved atmosphere exploration (infinite-loop `bandDrift`/`whaleDrift` animations) — identified and named explicitly as the "discarded looping background" (Founder Decision 018 rule 4, ADR-044), never ported. Both findings recorded as `docs/KNOWN_ISSUES.md` KI-047 (the mockup's untracked/mis-pathed source) rather than silently worked around.

**Implementation Summary — Founder Decision 018 and tokens**: Drafted and recorded Founder Decision 018 (Motion & Atmosphere Amendment), status Approved per direct instruction — amends Founder Decision 017's immediate-render principle to permit exactly: a one-shot digits-only scramble/settle (≤600ms hero/≤450ms supporting, ≤150ms stagger, a ≤650ms glow pulse on settle), one-shot section entrances (≤16px/≤350ms), a one-shot chart draw-in as an authorized *pattern* (not a reversal of ADR-043's specific `isAnimationActive={false}` decision), static-only atmosphere (explicitly rejecting the discarded looping background), a `matchMedia`-based hard gate on every rule above including the JS-driven scramble, and identical treatment for gains and losses. Token system extended additively: `primitives.css` gained an "elevated" namespace (oklch values ported verbatim from the mockup — atmosphere gradient/wash, cream/warm ink at three depths, a single accent hue, a restrained negative tint, a small hairline scale, a static SVG grain texture) alongside the existing primitives, never overwriting them. `semantic.css` gained base, non-elevated fallbacks for two new tokens (`--color-accent`, `--color-negative-tint`) so they're always valid app-wide. `globals.css` gained `.itm-elevated` — a single scoped class that locally remaps the semantic custom-property names (background, ink, hairline, primary/primary-foreground, button-radius) within whatever subtree wraps it, applied only to the Results and Simulator `<main>` wrappers — so every existing component (Button, Card, Input, disclosures) picks up the new palette with zero component-level changes, and the root/dev-playground pages remain untouched by construction, not by convention. `layout.tsx` now self-hosts Newsreader (display serif), IBM Plex Mono (figures/mono, replacing JetBrains Mono), and Public Sans (body, replacing Inter) via `next/font/google` — no external font CDN at runtime.

**Implementation Summary — the scramble hook**: `frontend/src/hooks/use-scramble.ts` mirrors `useSettleIn`'s established once-per-mount capture pattern exactly. A real bug was caught by this pass's own new test (`use-scramble.test.ts`): the first implementation used the `requestAnimationFrame` callback's own timestamp argument for elapsed-time math (matching the mockup's JS verbatim) and silently never completed under jsdom — the callback's `now` argument and a separately-captured `performance.now()` didn't share the same epoch in this test environment, so the eased progress never reached exactly `1`. Fixed by re-calling `performance.now()` inside every frame instead of trusting the callback's timestamp — portable and spec-safe. Wired into the hero sentence's two figures (`opening-sequence-heading.tsx`, delays 0/100ms, 600ms duration) and Supporting Facts' three figures (`results-sections.tsx`, delays 0/100/200ms, 450ms duration), matching the mockup's own 100ms-stagger cascade.

**Implementation Summary — Results restyle**: The hero sentence now renders as one flowing serif (Newsreader) paragraph with the two answer-bearing figures set in mono and scrambling, replacing the prior stacked-line layout — `aria-label` always carries the final, non-scrambling sentence text, decoupled from the live-scrambling visible DOM content. Supporting Facts, Why (now a 3-column grid under an italic "Why?" sub-heading), and The Proof (a small accent "+" mark on its summary, matching the mockup) restyled in the same language; a new shared `.kicker` class (mono, tracked, uppercase, muted) replaces the repeated `text-xs font-medium tracking-wide text-ink-muted uppercase` utility string at every label call site touched this pass. The Growth Chart gained the mockup's gradient area fill, an invested-amount baseline reference line with a mono label, an endpoint marker with an on-chart final-value label, and mono axis ticks — deliberately kept the existing CVD-validated `--color-chart-portfolio` blue for the data line itself rather than the mockup's gold accent (a disclosed, deliberate partial-port; full reasoning in ADR-044), and left decimation, split markers, the tooltip, the accessible table, and the `toChartPlotNumber` boundary untouched, per direct instruction. A restrained negative tint (a new `--color-negative-tint` token, never the existing status-critical hue) applies to Total Return/CAGR stat *values* only for a loss — confirmed by a new test that the hero sentence never carries it regardless of sign, and that a gain and a loss otherwise share byte-identical classes.

**Implementation Summary — Simulator restyle**: `SimulationForm` no longer wraps its fields in a `Card` — a flat, editorial layout matching the mockup, with no card-in-card nesting anywhere. Text/date inputs use a new underline-only treatment (`UNDERLINE_INPUT`, merged onto the shared `Input`/`AssetSearchCombobox` primitives via `cn`/tailwind-merge — the primitives' own bordered-box default is unchanged for any future, non-elevated consumer). The dividends/inflation checkboxes are now presented as toggle switches over real, native `<input type="checkbox">` elements (`sr-only`, never `display:none`, never removed from the tab order) — visual presentation only; `react-hook-form` registration, validation, and behavior are unchanged. `.itm-elevated`'s primary/primary-foreground remap means the existing `<Button variant="primary">` automatically becomes the mockup's gold CTA with zero `Button` component changes. The trading-day guidance copy and all validation behavior are unchanged, per direct instruction.

**Files Created**: `frontend/src/hooks/use-scramble.ts`, `frontend/src/__tests__/lib/use-scramble.test.ts`, `docs/ARCHITECTURE_DECISIONS.md` ADR-044 (in place), `docs/FOUNDER_DECISIONS.md` Founder Decision 018 (in place).

**Files Modified**: `frontend/src/styles/tokens/primitives.css`, `frontend/src/styles/tokens/semantic.css`, `frontend/src/app/globals.css`, `frontend/src/app/layout.tsx`, `frontend/src/lib/format/index.ts`, `frontend/src/components/simulation-result/opening-sequence-heading.tsx`, `frontend/src/components/simulation-result/results-sections.tsx`, `frontend/src/components/simulation-result/growth-chart.tsx`, `frontend/src/components/simulation-result/simulation-result-client.tsx`, `frontend/src/components/simulator/simulation-form.tsx`, `frontend/src/components/simulator/asset-search-combobox.tsx`, `frontend/src/app/simulator/page.tsx`, `frontend/src/__tests__/components/opening-sequence-heading.test.tsx`, `frontend/src/__tests__/components/results-sections.test.tsx`, `frontend/src/__tests__/components/simulation-result-client.test.tsx`, `frontend/src/__tests__/app/simulator-page.test.tsx`, `frontend/src/__tests__/lib/contrast.test.ts`, `docs/KNOWN_ISSUES.md`, `docs/CHANGELOG.md`, `docs/PROJECT_STATE.md`, this file.

**Architecture Decisions**: ADR-044 (font self-hosting via `next/font/google`; the additive "elevated" token namespace plus the scoped `.itm-elevated` remap class, chosen over overwriting base dark-theme tokens in place; the `useScramble` hook's guardrail-compliant design and the jsdom rAF-epoch bug it caught; the deliberate decision to keep the Growth Chart's data-line color validated-blue rather than the mockup's gold; extending `toChartPlotNumber` to a second, equally-disclosed call site for the chart's new baseline reference line; and the explicit exclusion of the discarded looping-background exploration).

**Problems Encountered**: (1) The task-stated mockup path didn't exist; resolved by content-matching the real file and confirming with the founder before implementing (see above). (2) A literal `*/` substring inside a CSS comment (`--color-ink-*/--color-border-hairline`) prematurely closed the comment block, passing `vitest`/typecheck/eslint cleanly but breaking Turbopack's stricter production CSS parser outright (`CssSyntaxError: Unknown word --color-border-hairline`) — caught only by actually running `npm run build`, not by the faster dev-loop checks alone. (3) The scramble hook's rAF-timestamp-epoch bug (above) — caught by its own new test, not by manual/visual inspection, which would have shown a plausible-looking (if perpetually mid-scramble) animation and not obviously read as broken.

**Solutions**: (1) See mockup discrepancy summary above. (2) Reworded the comment to avoid the adjacent `*`/`/` characters; re-verified with a raw open/close comment-marker count (`grep -c`) before re-running the build. (3) Re-call `performance.now()` inside the rAF callback instead of trusting its timestamp argument.

**Lessons Learned**: A CSS comment containing a stray `*/`-shaped substring is a real, silent production-build hazard that no non-Turbopack tool in this project's toolchain (vitest, tsc, eslint) catches — the standing "CI-parity, run the actual build" guardrail (KI-046) earns its keep again here, this time for a frontend-only, non-CI-workflow-related defect. Separately: `requestAnimationFrame`'s callback timestamp argument is not a portable substitute for `performance.now()` across every environment (specifically observed diverging under jsdom) — any future rAF-driven animation in this codebase should call `performance.now()` directly inside the callback rather than trust the argument, and this is now a disclosed lesson rather than a rediscovered one.

**Security Review Summary**: No new attack surface. This pass is presentational only — no new input is accepted, no new endpoint is called, no new financial calculation occurs; the scramble hook operates purely on already-formatted display strings (never `DecimalString` values) and the new `toChartPlotNumber` call site is chart-geometry-only, matching ADR-043's existing disclosed-exception shape exactly.

**Testing Summary**: Full frontend suite green (`npx vitest run`): 223 passed, 1 skipped, 224 total across 42 test files (up from 179 passing pre-pass) — new coverage includes `use-scramble.test.ts` (reduced-motion instant-render with no intermediate scrambled state ever observable, non-digit characters locked from frame one, settles to the exact final target once, unaffected by an unrelated re-render), gain/loss parity tests in both `opening-sequence-heading.test.tsx` and `results-sections.test.tsx` (identical classes regardless of sign, negative tint applied only to the two percentage stat values, never the hero), and 12 new contrast-check cases (`contrast.test.ts`) computing WCAG relative luminance directly from the ported oklch primitives (no lossy oklch→hex round trip) — all pass AA (4.5:1) against both the dark and light elevated backgrounds. `npx eslint .` and `npx tsc --noEmit` both clean. `npm run build` verified clean with `frontend/.env.local` moved aside and only the CI-equivalent `NEXT_PUBLIC_API_BASE_URL` env var set (KI-046 CI-parity guardrail) — this is where the stray `*/` comment bug was actually caught. **Bundle note**: the three self-hosted font families add 373,728 bytes (~365 KB) of `.woff2` across 24 subset files in the production build's aggregate output; a single page fetches only the specific weight/style/subset its rendered text needs (2–4 files in practice), not the full aggregate. Live-verified against a local `next dev` server (no backend/Docker available in this environment): `/simulator` and `/simulation/[fake-id]` both render 200 with the correct copy, `.itm-elevated` class, the new font-variable classes on `<html>`, and the toggle-switch/underline-input markup present — structural verification only; no browser-automation tool was available to capture pixel-level visuals or observe the scramble/atmosphere rendering live, stated explicitly rather than implied (matching this project's established practice, e.g. the M7 Phase 3C-3 entry above).

**Technical Debt Introduced**: Two new, disclosed `docs/KNOWN_ISSUES.md` entries — KI-047 (the mockup source file's untracked/mis-pathed status; this pass has no git authority to fix it) and KI-048 (`docs/BRAND_CONSTITUTION.md` §6–7 and `docs/frontend_design_system.md` §3–4 now factually disagree with the shipped typeface/palette for the elevated surfaces; a dedicated follow-up documentation pass should reconcile them, out of this task's explicit scope). Neither is a silent gap — both are named, reasoned about, and left open on purpose rather than guessed at.

**Performance Notes**: See the bundle note above. `Area`/`ReferenceDot`/`ComposedChart` are all Recharts components already bundled via the existing `recharts` dependency — no new package added. The scramble hook's `requestAnimationFrame` loop runs for at most 600ms per figure, once per mount, and is fully inert (no rAF scheduled at all) under reduced motion.

**Recruiter Value**: High. Demonstrates catching a task's own factual premise being wrong (the named mockup path) and resolving it by evidence and direct founder confirmation rather than either guessing silently or blocking entirely; demonstrates a real, non-obvious cross-environment bug (the rAF timestamp epoch mismatch) caught by a test specifically written to verify a safety-critical accessibility requirement (FD-018's reduced-motion hard gate), not by chance; and demonstrates deliberately preserving an existing, documented governance rule (the CVD-validated chart palette) against a visually tempting but ungoverned mockup literal-match, with the tradeoff disclosed rather than silently resolved either way.

**Production Readiness Score**: ~5/10 platform-wide, unchanged — this pass is a visual/presentational elevation of already-shipped screens, not a change to calculation correctness, security posture, or deployment readiness. KI-039 remains the most load-bearing open item for an actual deployed environment.

**Next Milestone**: Founder visual review of this pass (Phase 3D is implementation-complete, pending review — matching this project's established "review checkpoint before declaring complete" precedent). M7 Phase 4 — the Educational AI Experience — remains the next *feature* milestone, unaffected by this pass, still pending Founder Decision 015's rate-limit implementation.

---

## 2026-07-25 — M7 Phase 3D-1: Craft & Coherence

**Version**: 0.15.0 (proposed)

**Objective**: A founder-directed master polish elevating the shipped Design Elevation from "strong direction" to startup-grade finish — a product shell (header/footer/favicon/metadata), a full component-state audit, a unified accent discipline and label language, a full-bleed atmosphere, the Growth Chart raised to a genuine flagship asset, redesigned pending/error states, and four new `dev_seed` fixture scenarios (dividend payer, overall loss, disclosed split, ETF) plus a real display name for every seeded asset. Under FD-018's existing motion law — no new Founder Decision, this is implementation-level craft work.

**Scope**: `frontend/src/components/shell/*` (new), `frontend/src/components/ui/disclosure.tsx` (new), `frontend/src/components/ui/{button,input,error-state,skeleton}.tsx`, `frontend/src/components/simulator/{simulation-form,asset-search-combobox}.tsx`, `frontend/src/components/simulation-result/{opening-sequence-heading,results-sections,growth-chart,simulation-result-client}.tsx`, `frontend/src/app/{layout,icon.svg,simulator/page,simulation/[id]/page}.tsx`, `frontend/src/app/globals.css`, `frontend/src/styles/tokens/components.css`, `backend/app/ingestion/providers/dev_seed_provider.py`, `backend/app/ingestion/seed_dev_data.py` (new), tests, and documentation. No calculation logic, no Founder Decision, no backend API contract changed.

**Implementation Summary — Product shell & identity (task A)**: `ProductShell` (new) composes a minimal, sticky `AppHeader` (Newsreader wordmark, one "Simulator" nav link, a hairline bottom rule, atmosphere-transparent) and a one-line `AppFooter` (educational-purpose note + "Not financial advice" + the calculation version, when one exists) around every product route's content. This is also where the elevated atmosphere (`.itm-elevated`) now lives — moved from each page's own `max-w-[...]` container to this shared, full-width wrapper, closing the "full-bleed" gap (task C.12): the gradient/grain now covers the entire viewport at any width, with no black margin and no visible seam. A real favicon (`app/icon.svg`, a small monogram echoing the Growth Chart's own endpoint-dot) replaces the Next.js default; the Results page now gets a dynamic per-simulation `<title>` via `generateMetadata` ("If you had invested $X in SYMBOL — Investment Time Machine"), a second, server-side, best-effort fetch of the same simulation `SimulationResultClient` also fetches client-side — a deliberate, disclosed tradeoff for dynamic metadata. Themed `::selection`, a themed scrollbar, and a `-webkit-autofill` box-shadow override round out the bespoke chrome (task A.4).

**Implementation Summary — Component craft system (task B)**: Every interactive primitive now has an explicit default/hover/focus-visible/active/disabled state (`docs/frontend_design_system.md` §15, new). `Button` gained a firm `active:scale-[0.98]` press, and its working-state spinner is now reduced-motion-aware — omitted outright (not frozen mid-spin) under `prefers-reduced-motion`, leaving a static label change as the only signal. `Input`/`AssetSearchCombobox` gained a refined, muted required-marker glyph (replacing the harsh default red asterisk) and a warm-toned invalid state (`--color-status-serious`, a real semantic split from `--color-status-critical`, now reserved for hard system errors only), plus a `-webkit-autofill` override and `color-scheme`/`accent-color` theming for native `<input type="date">`. The Simulator's toggle switches now use a 150ms thumb transition. Five independent native-`<details>` disclosures — Supporting Facts' "Source," The Proof, the Simulator's "More options," Results' "Technical details," and `ErrorState`'s own — are unified into one shared primitive, `Disclosure` (a real `<button aria-expanded>` + a CSS grid `0fr`→`1fr` height/opacity reveal, `inert` while collapsed), replacing `<details>`'s un-animatable instant snap with a 150ms chevron rotation and a one-shot reveal transition. `CopyLinkButton` gained a checkmark icon alongside its existing "Link copied" text swap.

**Implementation Summary — Coherence pass (task C)**: The Results page's mono kicker is now the one section-label pattern everywhere — the Why section's redundant italic serif "Why?" title (duplicating its own kicker) is removed, not renamed. Accent usage was audited and tightened app-wide (`docs/frontend_design_system.md` §16, new): the Why section's three sub-headings, every disclosure's trigger glyph, and The Proof's growth-data table Value column are no longer accent-colored — three-plus accent instances on one screen was scarcity-breaking overuse the mockup's own single-hero-figure restraint doesn't license. Accent now has exactly five sanctioned uses: hero figures, primary interactive chrome (via the existing `--color-primary` remap — one mechanism, not per-component classes), and key data marks; the Growth Chart's data line/fill/endpoint deliberately stays the CVD-validated blue, per ADR-044's own standing reasoning. Off-scale spacing values (`gap-11`/`px-11`, not on the documented 4px-base scale) were corrected to `gap-12`/`px-12` as part of a spacing-rhythm audit.

**Implementation Summary — The Growth Chart as flagship asset (task D)**: Decimation (~150–200 drawn points, via evenly-spaced index sampling that always preserves the first/last point and any disclosed-split date) is implemented fresh — no prior decimation algorithm existed in this codebase to "adjust the threshold" of, contrary to the task's own framing, disclosed here rather than silently assumed. The chart's caption now honestly distinguishes "across N data points" (the true total, matching The Proof's table) from "a smoothed line of M is drawn for readability" only when decimation actually occurred. The endpoint value label now flips from right- to left-anchored for wide values (e.g. `$1,234,567.89`) that would otherwise clip against the chart's edge. A sparse (3–4 tick), hairline-only mono Y-axis was added, using one new narrow, disclosed numeric-formatting exception (`formatAxisTick`, scoped to Recharts' own computed axis-scale values, never a real data point). The tooltip was redesigned to the editorial language (mono figures, a kicker-style date label, a styled dashed crosshair, a 120ms reduced-motion-safe fade) and the active-point marker gained a themed ring. Two further defects were found only by live-screenshotting a real gain and a real loss chart (see Problems Encountered) and fixed within this same pass.

**Implementation Summary — States & flow (task E)**: The Results page's pending state is now a real `ResultsSkeleton` matching the completed layout's shape (hero, Supporting Facts row, chart), using a new one-shot `.skeleton-shimmer` (≤2 passes, then stops) — replacing Tailwind's `animate-pulse`, which is infinite by default and, caught this pass, a direct FD-018 motion-law violation nobody had previously flagged. `ErrorState`'s icon is now neutral ink, not `--color-status-critical` red — "no red alarm panels," a calm register even for a genuine failure. Form validation errors render inline, in the same warm tone as the invalid-input border, positioned consistently under each field (unchanged structurally, recolored).

**Implementation Summary — dev_seed data + hero asset name (task F)**: `DevSeedProvider` gained four new fixture symbols — KO (a dividend payer, with a new `fetch_dividends` capability), PTON (the one deliberately negative-drift symbol, an overall-loss scenario), TSLA (a disclosed stock split via a new `fetch_splits` capability, metadata-only per Founder Decision 001's model — the price series itself needs no discontinuity), QQQ (a second ETF) — alongside the original three, with the existing per-symbol drift formula parameterized (AAPL/SPY/BTC-USD's own values byte-for-byte unchanged, confirmed by a known-answer regression test). A new one-shot operator script, `seed_dev_data.py`, seeds all seven with correct display names in one idempotent command, including a scoped, explicit `UPDATE` correcting any already-seeded row's stale name (`AAPL` → "Apple Inc.") — `get_or_create_asset` itself only sets `name`/`asset_type` on first creation, never on an existing row, a real gap the per-symbol CLI's documented examples had silently left open since M7 Phase 2. The Results hero sentence now names the asset by its real display name plus ticker ("Apple Inc. (AAPL)"), fetched eagerly via `useAssetDetail`, falling back to the ticker alone until resolved or on any failure — the prior pass's italic ticker-only treatment ("the italic-accident") is removed per direct instruction.

**Live verification — a genuinely capable browser-automation tool was available this pass** (Playwright + a pre-installed Chromium, discovered mid-session — `npx playwright --version` resolved successfully, contrary to prior sessions' stated absence), used for real, not merely structural, verification: Docker/Postgres/Redis were already running; migrations (`0004`/`0005`, present on the host but not inside the running container's non-bind-mounted `alembic/` directory — run from the host against the container's exposed Postgres port instead) and the new `seed_dev_data` script were both applied live. Three real simulations were created via the live API and screenshotted end-to-end at 1440px, 1920px, and a 390px mobile width, in both light and dark color schemes: **AAPL (gain, +87.37%)**, **PTON (loss, −73.52%)**, and **KO (dividend-reinvested, +77.60%)** — all three rendered correctly, including the correct Why-section dividend paragraph branch for KO. This live pass caught three real, otherwise-invisible defects, all fixed within this same session (see Problems Encountered/Solutions) — the production build was also screenshotted directly to confirm two floating badges visible only in `next dev` (a Next.js dev-mode indicator and an unidentified secondary badge, most likely a related dev-tools element) are absent from the actual shipped build.

**Files Created**: `frontend/src/components/shell/product-shell.tsx`, `app-header.tsx`, `app-footer.tsx`; `frontend/src/components/ui/disclosure.tsx`; `frontend/src/app/icon.svg`; `frontend/src/__tests__/components/disclosure.test.tsx`; `backend/app/ingestion/seed_dev_data.py`; `backend/tests/ingestion/test_providers_dev_seed.py`.

**Files Modified**: `frontend/src/app/globals.css`, `frontend/src/app/layout.tsx` (favicon removal — superseded by `icon.svg`), `frontend/src/app/simulator/page.tsx`, `frontend/src/app/simulation/[id]/page.tsx`, `frontend/src/styles/tokens/components.css`, `frontend/src/components/ui/{button,input,error-state,skeleton}.tsx`, `frontend/src/components/simulator/{simulation-form,asset-search-combobox}.tsx`, `frontend/src/components/simulation-result/{opening-sequence-heading,results-sections,growth-chart,simulation-result-client}.tsx`, `backend/app/ingestion/providers/dev_seed_provider.py`, `docs/setup_guide.md`, various test files, `docs/ARCHITECTURE_DECISIONS.md` (new ADR-045), `docs/frontend_design_system.md` (new §15–16), `docs/CHANGELOG.md`, `docs/PROJECT_STATE.md`, this file.

**Architecture Decisions**: ADR-045 (the `ProductShell` full-bleed architecture; the shared `Disclosure` primitive; the CSS custom-property re-declaration bug — a `var()` reference resolves once, at its own declaration's scope, not dynamically per descendant, found live via a real keyboard-focus screenshot; the Growth Chart's Y-axis domain/label-position fixes; the dividend-date/weekend-mismatch fix).

**Problems Encountered**: (1) The task's own framing assumed an existing chart-decimation algorithm to "adjust the threshold" of — none existed; implemented fresh, disclosed rather than silently assumed. (2) `--focus-ring-color`/`--input-border-focus`, each declared once at plain `:root` as `var(--color-primary)`, silently rendered the base navy brand color instead of the elevated accent inside `.itm-elevated` — invisible to `tsc`/`eslint`/the unit suite, found only by a real keyboard-Tab screenshot (a synthetic `.focus()` call does not reliably trigger `:focus-visible` matching, and was tried first, misleadingly showing the same symptom for a different, red-herring reason before the keyboard-driven re-test isolated the real cause). (3) The Growth Chart wasted up to half its vertical height (Recharts' `Area` component defaults to a zero-based Y-axis domain) and its invested-amount baseline label visually collided with the price line specifically on a loss trajectory (where the invested amount is the series' own maximum, sitting at the domain's edge with minimal headroom) — found only by screenshotting a real loss simulation, not visible in the gain-only case a less thorough check might have stopped at. (4) A live KO dividend-reinvestment simulation failed with `MISSING_HISTORICAL_DATA` — `fetch_dividends`' fixed "15th of the month" ex-dividend dates could land on a weekend, with no matching price row (`fetch_prices` only emits weekdays). (5) The running backend container's `alembic/` directory is not bind-mounted (only `app/` is, per `docker-compose.yml`), so migrations added to the host repository were invisible inside `docker exec` — required running `alembic upgrade head` from the host against the container's exposed Postgres port instead.

**Solutions**: (1) Implemented `decimatePoints` (evenly-spaced index sampling, always preserving first/last/split-marker dates) at a 175-point target, with an honest caption disclosing the drawn-vs-actual count. (2) Re-declared `--focus-ring-color`/`--input-border-focus` inside all three `.itm-elevated` blocks, mirroring the existing `--color-primary` re-declaration pattern exactly; documented the general CSS custom-property lesson in `docs/frontend_design_system.md` §15 so it isn't rediscovered per-token in the future. (3) An explicit, padded Y-axis `domain` function (always including the invested-amount baseline, ~8% padding on both ends) plus a dynamic label-position flip mirroring the existing `endpointLabelFlipped` pattern; re-verified live on both a gain and a loss chart after the fix. (4) Shifted any weekend-landing dividend date back to the preceding Friday, deterministically, verified for every seeded year; deleted and re-seeded the 20 already-wrong local KO dividend rows as part of this pass's own live verification. (5) Ran `alembic upgrade head` and `seed_dev_data` from the host shell with `DATABASE_URL`/`ENVIRONMENT` set to match the container's own configuration, against `localhost:5432` (the same Postgres, exposed via the compose file's port mapping).

**Lessons Learned**: A CSS custom property's `var()` reference is resolved once, at its own declaration's computed-value time and scope — re-declaring a property it *references* at a more specific selector does not make the referencing property "dynamically follow" that override; anything composed this way must be re-declared at the same scope directly. Separately: a charting library's default behaviors (a zero-based domain, a fixed label-anchor position) are real design surface area a static review will not catch — both defects here only became visible by rendering a *loss* scenario specifically, reconfirming that testing only the "happy," gain-shaped case leaves real gaps even when the code path is nominally "the same" for both signs. Finally: environment assumptions should be re-verified per session, not carried forward from a prior session's stated limitation — this pass discovered a fully capable Playwright + Chromium install that a prior session's report had described as absent, and that discovery materially changed how much of this pass could be verified with real confidence rather than structural inference alone.

**Security Review Summary**: No new attack surface. This pass is presentational/craft work (fonts, tokens, motion, component states, a shell) plus a local-development-only fixture data extension (`dev_seed`, already gated to non-production `ENVIRONMENT` values by existing, unchanged guardrails). No new input is accepted, no new endpoint is exposed, no calculation logic changed. `generateMetadata`'s second server-side fetch calls the same already-public, unauthenticated `GET /api/v1/simulations/{id}` endpoint the client already calls.

**Testing Summary**: Full frontend suite green (`npx vitest run`): 238 passed, 1 skipped, 239 total across 43 test files — new coverage for `Disclosure` (open/close, `aria-expanded`, chevron rotation, `onOpenChange`), `Button` (reduced-motion spinner omission, press-compression class), and the Growth Chart (decimation target bounds, endpoint-label flip in both directions, sparse Y-axis tick presence). `eslint`/`tsc` both clean. `npm run build` clean with `.env.local` moved aside (KI-046 CI-parity guardrail); the favicon (`icon.svg`) confirmed as its own generated route in the build output. Backend: full suite green (`pytest`, 298 passed, up from 288), `ruff`/`black` both clean; 11 new tests for the extended `DevSeedProvider` (every new symbol's price/dividend/split behavior, a known-answer regression guard for the untouched original three symbols, and the dividend-weekend-date fix). Live end-to-end, described above in detail: three real simulations (gain/loss/dividend) created via the running API and screenshotted at three viewport widths in both color schemes, plus keyboard-focus, chart-hover, and disclosure-open interaction states — all via a real, launched Chromium browser, not a structural/HTML-only check.

**Technical Debt Introduced**: None judged unintentional. The two floating dev-mode badges observed in `next dev` screenshots (confirmed absent from the production build) remain unidentified beyond "Next.js's own dev-mode chrome" — not chased further given they don't appear in what actually ships; noted here rather than silently dropped.

**Performance Notes**: The Growth Chart's decimation reduces the number of SVG path points Recharts renders for a long series (e.g. 1,303 raw points down to 164 drawn) — a rendering-cost improvement as a side effect of the readability goal, not separately measured. No new dependencies added (`Disclosure`, `ProductShell` etc. are all plain React + existing Tailwind utilities).

**Recruiter Value**: High. Demonstrates a full design-craft pass executed with genuine empirical rigor rather than code-review-only confidence — three of five real defects fixed this pass (a CSS cascade bug, a charting-library default, a fixture data/business-logic mismatch) were only found by actually running the application in a real browser against a real backend, and each fix is documented with its root cause, not just its symptom. Also demonstrates re-evaluating a prior session's stated environment limitation rather than carrying it forward uncritically, which materially raised this pass's own verification confidence.

**Production Readiness Score**: ~5/10 platform-wide, unchanged — this pass is craft/polish work on already-shipped screens plus a local-dev-only fixture extension, not a change to calculation correctness, security posture, or deployment readiness.

**Next Milestone**: Founder visual review of this pass, alongside the still-pending M7 Phase 3D review. M7 Phase 4 (Educational AI Experience) remains the next feature milestone, unaffected, still pending Founder Decision 015's rate-limit implementation.

---

## 2026-07-26 — M7 Phase 3D-2: Regression Fixes & Founder Design Review Refinements

**Version**: 0.15.1 (proposed)

**Objective**: A founder-observed regression pass — five live, founder-reported defects (a non-clickable-looking primary button, zero-result asset search, a colliding/misaligned sticky header, unbounded date entry, and dev-mode overlays visible in demos) took absolute priority, followed by a founder design review's refinement list (layout centering, hero typography, an accent/color audit, a real combobox, example chips) and, lowest priority, five motion items. Per direct instruction, motion was the first bucket to cut if time ran short.

**Scope**: `frontend/src/components/ui/{button,input}.tsx`, `frontend/src/components/shell/{app-header,product-shell}.tsx`, `frontend/src/components/simulator/{simulation-form,asset-search-combobox}.tsx`, `frontend/src/components/simulation-result/{opening-sequence-heading,simulation-result-client}.tsx`, `frontend/src/hooks/use-entrance-dissolve.ts` (new), `frontend/src/app/globals.css`, `frontend/next.config.ts`, `frontend/src/providers/query-provider.tsx`, tests, and documentation. No backend code changed — the search bug's fix was environment state (migrations + seed), not code.

**Implementation Summary — Audit (bug 0)**: Traced the "no hover, no cursor:pointer" report to a single missing utility class. `Button`'s `hover:bg-primary-hover` was, and is, live and correct (confirmed via a computed-style probe: background genuinely changes on hover) — but a native `<button>`'s UA-default cursor is `default`, not `pointer` (unlike `<a>`, which is pointer by default), and `Button` never set `cursor-pointer` explicitly. Every 3D-1 state claim was literally true; the button simply gave no visual signal it was interactive at all before the pointer happened to land on it. One-line fix (`cursor-pointer` + `disabled:cursor-not-allowed` on the shared `buttonVariants`), documented in the component's own header comment so the discrepancy between "states are implemented" and "states are discoverable" isn't lost.

**Implementation Summary — Bug 1, asset search**: `curl`ing the search endpoint directly with the frontend's own request shape returned `{"assets": [], "total": 0}` — not a bug in `asset_service.search_assets` (a correct case-insensitive `ilike` partial match) or in `useAssetSearch`/`AssetSearchCombobox` (correct debounce, correct min-length, correct request shape). `docker exec`ing into `itm_dev` found `alembic_version` present but empty and every domain table missing — this environment's database had never been migrated, let alone seeded. Ran `alembic upgrade head` (five migrations, including the previously-unapplied `growth_series` persistence migration) then `python -m app.ingestion.seed_dev_data`; search now returns real results end-to-end, confirmed live for both `"a"` and `"aapl"`. The UI's no-matches/searching/failed trichotomy was already correctly distinguished in `AssetSearchCombobox` (an `EmptyState` for zero results, a spinner+"Searching…" while fetching, red critical-toned copy for a real failure) — no UI change was needed there, only the data.

**Implementation Summary — Bug 2, header**: `AppHeader` had no background at all ("atmosphere-transparent" by original design) — correct only while unscrolled; the instant a user scrolled, page content passed directly under and visually collided with the header's own wordmark/nav text (reproduced and screenshotted). Separately, `AppHeader`'s inner container was hardcoded to `max-w-[1120px]` while the Simulator's own content column is `max-w-[760px]` (Results' is `1120px`, so this specific mismatch was Simulator-only) — visually reading as "header pins to the viewport edges, content sits in a narrower column." Fixed with a translucent scroll backdrop (`bg-background/85 backdrop-blur-md`, keeping the existing hairline bottom rule as the only separator, matching the surrounding brand's "border-first, not filled-bar" elevation model) and a `ProductShell`-derived `maxWidthClassName` that extracts the `max-w-*` token straight out of each page's own `contentClassName`, so the header always matches whatever column the page beneath it actually uses, per-route, without a second hardcoded constant to drift out of sync.

**Implementation Summary — Bug 3, dates**: `noValidate` deliberately routes all form validation through react-hook-form (for consistent inline-error rendering), which meant the native `min`/`max` attributes already present on the date `<input>`s were cosmetic only — the browser's own `:invalid` gate never actually ran, so a manually-typed `1887-01-01` (or any other value) submitted cleanly. Fixed two ways: (1) date fields are now `disabled` with an explanatory hint ("Select an asset first to see its available date range.") until an asset is selected, closing off the pre-selection case entirely; (2) once selected, a `useMemo`-rebuilt Zod schema (`buildFormSchema`, closing over the asset's `availability` window, falling back to a global `1900-01-01`–today range) `superRefine`s both date fields against that window and surfaces a plain-language inline error naming the exact allowed range. React Hook Form re-reads its `resolver` prop on every validation pass, so no imperative `setError`/`clearErrors` bookkeeping was needed to keep the bound schema in sync with the selected asset. Dates are never auto-shifted — an out-of-range entry is rejected, never silently corrected, matching this product's own "Investment Time Machine never moves your dates automatically" copy one paragraph below the fields.

**Implementation Summary — Bug 4, interactive states**: Verified live, control by control. Header nav (`<Link>`, native pointer cursor, correct hover-to-primary-color transition — confirmed the *active* nav item shows no hover color *change* only because it's already at its "on" color, not a bug). `Disclosure` ("More options", "Source", "Technical details") already had `cursor-pointer`, a 150ms chevron rotation, and inherits the global `:focus-visible` ring — no change needed. The Simulator's toggle switches already had `cursor-pointer` on their `<label>` and a 150ms thumb transition. `Button`-backed controls (Run simulation, Copy link, Run another simulation's sibling button, Check again) were fixed by the same `cursor-pointer` change as the audit item above; `Input` gained a matching `disabled:opacity-50 disabled:cursor-not-allowed` it was previously missing entirely (surfaced by adding the new disabled date-field state in bug 3).

**Implementation Summary — Bug 5, dev overlays**: `next.config.ts` had no `devIndicators` key at all (Next.js 16's dev-mode build-activity indicator shows by default); `ReactQueryDevtools` was gated on `process.env.NODE_ENV === 'development'`, meaning *every* `next dev` session — including a founder demo — showed the devtools toggle. Set `devIndicators: false`; re-gated the devtools import behind `NEXT_PUBLIC_ENABLE_DEVTOOLS === 'true'` (a `NEXT_PUBLIC_*` var, so it's statically inlined and verifiably absent unless explicitly opted into, even at build time). Confirmed live: both overlays visible before the fix, both gone after, in the same running dev server (Next.js picked up the `next.config.ts` change without a manual restart).

**Implementation Summary — Refinements (design review, items 6–13)**: Item 6 (layout centering) resolved as a side effect of the bug-2 header fix — direct measurement (`main`/`header` bounding boxes at a 1920px viewport) confirmed the content column was already mathematically centered (`x: 400, width: 1120`, symmetric 400px margins); the perceived "dead left half" traced to the same header/content width mismatch bug 2 fixed, not a separate centering defect. Item 7: wrapped the hero sentence's two dates in `whitespace-nowrap` spans and sized `ScrambleFigure` down to `text-[0.85em]` (~15%) so the mono figures sit on the serif sentence's optical line. Item 8 (Supporting Facts baseline): already correct — measured identical `y`/`height` across all three stat columns at 1440px; no change needed. Item 9 (typography rule): converted `Input`/`AssetSearchCombobox`'s bold-sans field labels to the shared mono `.kicker` eyebrow style already used for "Asset information" and every Results-page section label. Item 10 (color/accent): already correct from 3D-1 — warm `--color-status-serious` validation tone (not raw orange), a muted required-marker asterisk (not red), and the documented accent-scarcity allowlist (`frontend_design_system.md` §16) were all already in place; verified live rather than re-implemented. Item 11 (real combobox): the ARIA combobox pattern (keyboard nav, `role="listbox"`, dropdown showing name/ticker/type) was already fully implemented; added the one missing piece — a one-shot `.rise-fade-in` on the "Asset information" confirmation block, played once when a selection resolves. Item 12 (chevron/label baseline): measured and screenshotted the `Disclosure` trigger — already visually aligned via `items-center`; no change made. Item 13 (example chips): new — three quiet pill chips under the form (Apple/gain, Peloton/loss, Coca-Cola/dividend-reinvested, all using `seed_dev_data`'s own fixture symbols and a shared safe 2020–2024 trading-day range), filling every form field on click without submitting; required extending `AssetSearchCombobox` with an externally-controlled `value` prop.

**Implementation Summary — Motion (items 14–18)**: Implemented item 14 only (`useEntranceDissolve` — a one-shot blur(16px)/opacity(0.4)-to-sharp dissolve on the Results content container, ~400ms, mirroring `useSettleIn`'s established two-`requestAnimationFrame` mount pattern), gated by `useReducedMotion` (`matchMedia`-based) and additionally gated by a `sessionStorage`-tracked per-simulation-id "seen" flag so it never replays on a back-navigation revisit to an already-viewed result — verified live via a fresh-context first-load vs. same-tab-reload comparison. Items 15 (chart tooltip/baseline sequencing), 16 (post-selection focus move + underline draw), 17 (submit button morph), and 18 (View Transitions API) were deferred — see Technical Debt / the final report for one-line rationale each, per this pass's explicit "cut motion first" priority order.

**Files Created**: `frontend/src/hooks/use-entrance-dissolve.ts`; `frontend/src/__tests__/lib/use-entrance-dissolve.test.tsx`.

**Files Modified**: `frontend/src/components/ui/{button,input}.tsx`, `frontend/src/components/shell/{app-header,product-shell}.tsx`, `frontend/src/components/simulator/{simulation-form,asset-search-combobox}.tsx`, `frontend/src/components/simulation-result/{opening-sequence-heading,simulation-result-client}.tsx`, `frontend/src/app/globals.css`, `frontend/next.config.ts`, `frontend/src/providers/query-provider.tsx`, `frontend/src/__tests__/components/{asset-search-combobox,simulation-form}.test.tsx`, `docs/CHANGELOG.md`, this file.

**Architecture Decisions**: None new — this pass operates entirely within already-approved patterns (FD-018's motion law for item 14, the existing `Disclosure`/`Button`/`Input` component contracts for everything else). No Founder Decision needed.

**Problems Encountered**: (1) The live app's `itm_dev` database had never been migrated in this environment (Docker Desktop itself was stopped at the start of this session) — not discoverable from code alone, only from actually running the stack. (2) `react-hooks/set-state-in-effect` (an ESLint rule from `eslint-plugin-react-hooks`'s newer ruleset) flagged the first implementation of `AssetSearchCombobox`'s external-`value` sync (a `useEffect` calling `setInputValue` directly) as a cascading-render anti-pattern. (3) An early version of the date-bounds fix showed the "select an asset first" hint under *both* date fields, tripping a test's `getByText` (which fails on multiple matches) — a real duplicate-content bug, not just a test artifact.

**Solutions**: (1) Started Docker Desktop, ran `alembic upgrade head` then `python -m app.ingestion.seed_dev_data` directly against the now-running Postgres container; documented the one-line re-seed command for the founder in the final report. (2) Rewrote the sync as a render-time "adjusting state when a prop changes" comparison (`if (value !== priorValue) { setPriorValue(value); ... }`) per React's own documented pattern, applying the update before the first paint instead of scheduling a second render from inside an effect. (3) Removed the hint from the End date field, keeping it only on Start date — matching the original single-hint design the availability-driven helper text already used.

**Lessons Learned**: A founder bug report of "no hover, no cursor" can be entirely accurate about the *symptom* while the actual claimed styles are fully implemented and functioning — the gap was discoverability (a missing `cursor-pointer`), not the hover/press/focus logic itself; worth checking the cheap, obvious CSS property before assuming a deeper state-management regression. Separately, an empty local database is invisible to `eslint`/`tsc`/the unit suite (which all mock the network layer) and only surfaces by actually running the stack end-to-end — this pass's live-verification-first approach (Docker → migrate → seed → screenshot, before touching any component code) caught it in minutes rather than after an otherwise-unnecessary code-level investigation of the search stack.

**Security Review Summary**: No new attack surface. All five mandatory bugs and the refinements are presentational/UX fixes or environment state (DB migration + seed); no new endpoint, no new input surface, no calculation logic touched. The date-bounds fix is client-side UX only — the backend's own `MISSING_HISTORICAL_DATA`/`INVALID_DATE_RANGE` validation remains the actual security/correctness boundary, unchanged by this pass.

**Testing Summary**: Full frontend suite green: 249 passed, 1 skipped, 250 total across 44 files (6 new tests: external `value` sync, date-field disabled/enabled/out-of-range/in-range, both example-chip presets; 1 new test file for `useEntranceDissolve`'s active/settled/reduced-motion/session-gating behavior). `eslint`/`tsc` both clean. Production build clean with `.env.local` moved aside and `NEXT_PUBLIC_API_BASE_URL` supplied as a real environment variable instead (matching how CI/a real deploy pipeline injects it, rather than relying on a git-ignored local file) — confirms the existing fail-fast `env.ts` design is working as intended, not a defect. Backend suite green: 299 passed (up from 298 — one new migration applied), `ruff`/no backend code changed. Live-verified with Playwright + Chromium for every mandatory bug and refinement claimed complete: cursor/hover computed styles, header scroll-backdrop and column-width measurement, date bounds/validation-message screenshot, example-chip fill via a fresh browser context, entrance-dissolve settle/reduced-motion/session-gating behavior, and dev-overlay absence — all against the actually-running dev server, not inferred from source.

**Technical Debt Introduced**: Motion items 15 (chart tooltip micro-settle + baseline draw-in), 16 (post-selection focus move + underline draw), 17 (submit button morph), and 18 (View Transitions API) remain unimplemented — deferred, not abandoned, per this pass's explicit priority order (mandatory bugs first, refinements second, motion last-to-cut). `StatTile` (`src/components/ui/stat-tile.tsx`) still uses a native `<details>`/`<summary>` rather than the shared `Disclosure` primitive — confirmed unused outside the out-of-scope root `/` and `/dev/playground` pages, so left as-is rather than touched speculatively.

**Performance Notes**: No new dependencies added. `useEntranceDissolve`'s `sessionStorage` read/write is a single synchronous key lookup per mount, negligible. The header's `backdrop-blur-md` is a standard, GPU-accelerated CSS filter already used elsewhere in this codebase's atmosphere treatment.

**Recruiter Value**: Medium-high. Demonstrates root-causing a founder bug report to its actual mechanism rather than its symptom (the button audit, the empty-database diagnosis), disciplined priority triage under an explicit "mandatory bugs first, motion last" instruction, and live browser verification (Playwright screenshots + computed-style probes) as the actual acceptance bar rather than code-level inference.

**Production Readiness Score**: ~5/10 platform-wide, unchanged — this pass is regression fixes and UX polish on already-shipped screens, not a change to calculation correctness, security posture, or deployment readiness. The empty-database finding is an environment/ops gap (no seed step in the local setup flow), not a production code defect.

**Next Milestone**: Founder review of this pass's live-verified fixes and the deferred motion items (15–18), alongside the still-pending M7 Phase 3D/3D-1 reviews. M7 Phase 4 (Educational AI Experience) remains the next feature milestone, unaffected.

---

## 2026-07-27 — M7 Phase 3D-3: Founder Review Round 2

**Version**: 0.15.2 (proposed)

**Objective**: A second founder design-review round over six items: the dev_seed fixture data's own periodicity (the root cause behind "a useless graph"), the content column's width, a slower/more legible scramble effect (recorded as Founder Decision 018.1), removing noisy per-stat Source disclosures, restructuring The Proof for a non-technical reader, and four analytical upgrades to the Growth Chart.

**Scope**: `backend/app/ingestion/providers/dev_seed_provider.py`, `backend/tests/ingestion/test_providers_dev_seed.py`; `frontend/src/components/{shell/*,simulation-result/*,simulator/*}`, `frontend/src/hooks/use-scramble.ts`, `frontend/src/lib/format/date.ts`, `frontend/src/app/globals.css`, various tests; `docs/{FOUNDER_DECISIONS,KNOWN_ISSUES,setup_guide,CHANGELOG}.md`.

**Implementation Summary — Item 1, seed realism**: `DevSeedProvider.fetch_prices`'s close price was `(base_price + day_index * daily_drift) * (1.01 if day_index % 10 < 5 else 0.99)` — a linear drift with a fixed two-state multiplier alternating every 5 trading days, producing an unmistakable saw-tooth pattern on the Growth Chart (values genuinely repeated to the cent on a 10-day cycle). Replaced with a geometric random walk with drift: a fixed, symbol-derived seed (`random.Random(f"dev_seed_random_walk:{symbol}")`) drives one `gauss(0,1)` draw per trading day, applied as `price *= 1 + daily_drift + daily_volatility * z`, with new per-symbol annualized `_ANNUAL_DRIFT`/`_ANNUAL_VOLATILITY` parameters (AAPL 18%/25%, SPY 10%/16%, BTC-USD 35%/75%, KO 6%/15%, PTON -30%/55%, TSLA 20%/55%, QQQ 14%/20%). Verified directly (a standalone script run against the real provider, not just unit assertions): all seven symbols produce plausible, non-periodic 5-year paths with zero floor-clamp hits; a spot-check of AAPL's first 30 trading days shows genuine day-to-day variation with no repeating cycle. `docs/KNOWN_ISSUES.md` KI-049 records the defect and its resolution; `docs/setup_guide.md`'s `dev_seed` section now describes the random-walk model.

**Implementation Summary — Item 2, layout width**: Content max-width raised from 1120px to 1200px (Results, and `AppHeader`/`AppFooter`'s own hardcoded matches) and from 760px to 860px (Simulator) — both within or near the founder-specified 1150-1250px range for the primary reading column. Hero/display type scaled up proportionally at large viewports: the Results hero's `clamp()` ceiling raised from 3.5rem to 4.25rem (with a slightly higher vw coefficient so it actually keeps growing past 1440px instead of hitting its cap early) and its wrap width from `max-w-4xl` to `max-w-5xl`; the Simulator's H1 ceiling raised from 2.75rem to 3.25rem. Body prose is untouched (`max-w-prose`, 65ch, already well under the ~70-80ch ceiling the founder specified as a ceiling, not a target). The Growth Chart gained a small negative-margin "bleed" past the text column into the page's own horizontal padding gutter, per the founder's explicit allowance ("the chart may bleed slightly wider than text"). Verified live at 1440/1920/2560px via Playwright screenshots and direct `getBoundingClientRect`-equivalent measurement (confirmed `main`/`header` both centered, `x: 400, width: 1200` on a 2000px-equivalent viewport check — precise centering was never actually broken; the earlier "dead void" complaint traced to the width itself, now addressed).

**Implementation Summary — Item 3, Scramble v2 (Founder Decision 018.1)**: `useScramble` re-rolled its still-cycling digits on every `requestAnimationFrame` (~16ms) regardless of the overall animation duration — at the original 600ms (hero)/450ms (stats) durations this read as a brief, illegible flicker rather than a perceivable "cycling" effect. Fixed two ways, both recorded as a founder-approved amendment to FD-018 rule 1 (Founder Decision 018.1, `docs/FOUNDER_DECISIONS.md`): (1) durations raised to 900-1000ms (shipped 950ms) for hero figures, 600-700ms (shipped 650ms) for supporting stats; (2) unlocked digits now re-randomize at a fixed ~55ms tick, independent of the frame rate, so each intermediate digit is actually visible for a perceivable moment rather than blurring past. A new `cycling: boolean` return value lets each call site render a subtle, constant-intensity accent glow for the whole cycling phase (8px hero / 6px stats), distinct from and lower-intensity than the existing brighter post-settle pulse (20px / 16px, unchanged) — closing the gap where the multi-hundred-millisecond cycling phase itself was visually inert, with all the "arrival" signal concentrated only at the very end. The `.scramble-figure` CSS transition was shortened from 650ms to 200ms so the new in-cycle glow doesn't still be fading in by the time the cycle's already half-settled. Every other FD-018 rule-1 guarantee (once per load, digits-only, `matchMedia`-gated reduced-motion with zero intermediate scrambled state ever observable, identical treatment regardless of a result's sign) is unchanged and re-confirmed by the existing test suite, unmodified.

**Implementation Summary — Item 4, Supporting Facts simplified**: The per-stat `Disclosure` ("Source") previously nested under each of Final Value/Total Return/Annual Return (CAGR) — three independent formula-reveal toggles per page — is removed from `Fact` (`results-sections.tsx`) entirely; Supporting Facts is now label+scrambled-figure only, no `source` prop, no trigger. The three formula strings the disclosures used to hold move to The Proof (item 5b below) instead, stated once rather than three times.

**Implementation Summary — Item 5, The Proof restructured**: Reordered and rewritten per the founder's explicit five-part structure. (a) A new "In plain terms" section leads, personalized to the simulation's own inputs — 4 short bullets covering what data was used (the asset's actual daily close price, explicitly "never an estimate, a model, or a prediction"), how dividends were handled (conditional on `include_dividends`), how inflation was handled (conditional on `adjust_for_inflation`), and a consolidated "what this does not do" bullet (no future prediction, no fee/tax modeling, no automatic date-shifting). (b) Methodology tightened roughly 40% by word count (two dense paragraphs collapsed to one, with the CAGR-formula sentence trimmed since the formula itself now lives in the new list below it) and gained "How each figure is computed" — the three formulas moved off the now-removed per-stat Source disclosures. (c) Assumptions kept, prose tightened similarly; the existing input-echo `dl` (asset/amount/date-range/dividends/inflation) stays here, unchanged. (d) A new "Technical record" sub-block — visually quieter (mono, smaller, muted-ink, behind its own hairline top border) than every section above it — holds exactly the four fields the founder specified (data source, calculation version, simulation ID, created timestamp), closing a real regression along the way: "Created" had been rendering `sim.created_at` raw (an unformatted ISO 8601 string) instead of through `src/lib/format`, this codebase's one sanctioned formatting layer. A new `formatDateTime` export (`src/lib/format/date.ts`) — fixed 'en-US'/UTC, matching `formatDate`'s own established rationale, but for a full timestamp rather than a plain calendar date — closes it (`"2026-07-18T00:00:00Z"` → `"Jul 18, 2026, 12:00 AM UTC"`). (e) The accessible growth-chart data table is unchanged.

**Implementation Summary — Item 6, Growth Chart analytical upgrades**: (a) The area fill now splits at the invested-amount baseline: two `<Area>`s share one derived dataset (`aboveValue`/`belowValue`, each clamped to the baseline on the "wrong" side so one fill is a zero-height no-op wherever the real line is on the other side of it), both anchored via Recharts' `baseValue` prop — accent-toned above the baseline, a muted `--color-chart-negative` tone below it. That hue is not a new, arbitrary "cool" color invented for this task; it's the already-validated, CVD-safe "diverging pair" slot `frontend_design_system.md` §3 already reserved for exactly this use, at the same low opacity the original single fill used — "muted" is the opacity, not an unvalidated hue swap, keeping ADR-044's closed-palette constraint intact for the actual price LINE (unchanged, still one hue regardless of sign). (b) A thin vertical crosshair (`ChartCrosshair`, a custom Recharts `cursor` component) tracks the cursor alongside the existing tooltip, with a 120ms CSS `@starting-style` fade-in (`.chart-crosshair`, `globals.css`) — a disclosed, deliberate asymmetry: fade-out on mouse-leave is an instant unmount (Recharts conditionally mounts/unmounts its cursor rather than toggling an always-present element's opacity, so a true bidirectional fade isn't achievable without patching Recharts internals for a proportionally small polish gain). Reduced motion drops the crosshair entirely (`cursor={false}`) rather than rendering it unanimated. (c) Quiet, muted high/low markers (a hollow dot + a compact "value · Mon YYYY" label) mark the series' own genuine extremes — found over the FULL, undecimated series (never dropped by decimation, since their dates are now force-included in `decimatePoints`'s `keepDates` set alongside disclosed-split dates). (d) Dividend-reinvestment tick marks are explicitly **deferred, not implemented** — `SimulationResponse` exposes only `include_dividends: boolean`, no per-event reinvestment dates; there is nothing real to mark without fabricating dates the API doesn't provide, and item 6's own instruction explicitly named this exact "don't fake it, defer it" outcome as acceptable.

**Files Created**: None.

**Files Modified**: `backend/app/ingestion/providers/dev_seed_provider.py`, `backend/tests/ingestion/test_providers_dev_seed.py`; `frontend/src/components/shell/{app-header,app-footer,product-shell}.tsx`, `frontend/src/app/simulator/page.tsx`, `frontend/src/components/simulation-result/{opening-sequence-heading,results-sections,growth-chart,simulation-result-client}.tsx`, `frontend/src/hooks/use-scramble.ts`, `frontend/src/lib/format/{date,index}.ts`, `frontend/src/app/globals.css`, `frontend/src/__tests__/{lib/use-scramble,lib/format,components/results-sections,components/growth-chart,components/simulation-result-client}.test.tsx`, `docs/FOUNDER_DECISIONS.md` (new Founder Decision 018.1), `docs/KNOWN_ISSUES.md` (new KI-049), `docs/setup_guide.md`, `docs/CHANGELOG.md`, this file.

**Architecture Decisions**: None new. Founder Decision 018.1 (item 3, `docs/FOUNDER_DECISIONS.md`) is a founder-level amendment, not an engineering-level ADR — it changes what the product does, not how an already-approved decision is implemented.

**Problems Encountered**: (1) `test_migrations.py`'s own upgrade-then-downgrade-to-`base` pattern (an intentional, documented DB-integration test — see `docs/setup_guide.md`) wiped the local dev database's schema entirely every time the backend suite was run in this session, including mid-pass, twice — each time requiring a re-migrate-and-reseed before live verification could continue. (2) A stray `stock-backend-1` Docker container (auto-resumed by Docker Desktop from a prior session, running a stale image with an outdated `alembic/` copy baked in — that directory isn't bind-mounted, only `./backend/app` is) intermittently served requests instead of, or in a way that raced with, the host `.venv` process. (3) The first collision-avoidance heuristic for item 6c's high/low markers (skip only on an EXACT date match with the endpoint) was insufficient — live-verified against a real near-monotonic-loss simulation (PTON), the true low landed several weeks before the endpoint but at an almost-identical dollar value, and its label still crowded the endpoint's label badly since both render in the same bottom-right corner regardless of the horizontal (date) distance between them.

**Solutions**: (1) Re-ran `alembic upgrade head` + `python -m app.ingestion.seed_dev_data` from the host `.venv` after every backend suite run, before resuming any live-verification step. (2) Stopped the stray container (`docker stop stock-backend-1`) and used the host-run backend consistently for the remainder of the session. (3) Extended the collision guard to a combined date-OR-value proximity check: a marker is now omitted if it falls within a day-based edge guard (4% of the series' total span, 14-day floor) OF EITHER endpoint, OR within a value-based guard (6% of the series' own high-low range) of the endpoint's specific value — re-verified live against the same PTON simulation, confirmed the crowding is gone.

**Lessons Learned**: A collision-avoidance heuristic keyed to only one geometric dimension (date) is not sufficient when the visual collision it's meant to prevent actually depends on BOTH dimensions a chart plots — two points can be weeks apart on the X-axis and still crowd each other's labels if they're pixel-close on the Y-axis (or vice versa). This mirrors a lesson from an earlier pass in this same file's own history (the Y-axis domain padding / endpoint-label-flip fixes, M7 Phase 3D-1): a chart-geometry edge case is often invisible in code review and only surfaces against a real data trajectory with the specific shape (here: near-monotonic, ending near its own extreme) that triggers it. Separately: this session re-confirmed last session's own finding that running the backend test suite locally against a live dev database is destructive (`test_migrations.py`'s documented downgrade-to-base behavior) — worth internalizing as a standing operational rule for this project (re-seed after `pytest`, every time) rather than being surprised by it per-session.

**Security Review Summary**: No new attack surface. All six items are presentational/UX changes or a local-development-only fixture data algorithm change (`dev_seed`, still gated to non-production `ENVIRONMENT` values by its existing, unchanged constructor guard). No new endpoint, no new input surface, no calculation logic touched — `formatDateTime` and the Growth Chart's new derived fields are pure display-layer transforms of already-fetched, already-public data.

**Testing Summary**: Backend: full suite green (`pytest`, 303 passed, up from 299 — 4 new tests for the random-walk provider), `ruff check .`/`black --check .` both clean. Frontend: full suite green (`npx vitest run`, 264 passed, 1 skipped, up from 259 — new/updated coverage across `use-scramble.test.ts` (cycling-phase/glow behavior), `format.test.ts` (`formatDateTime`), `results-sections.test.tsx` ("In plain terms," the formula list, Technical Record with a formatted timestamp, no per-stat Source), `growth-chart.test.tsx` (split-fill area count, high/low labeling, both collision-avoidance guards), `simulation-result-client.test.tsx` (disambiguated a label query the new formula list made ambiguous)). `eslint`/`tsc` both clean. `npm run build` clean with `.env.local` moved aside and `NEXT_PUBLIC_API_BASE_URL` supplied as a real environment variable (CI parity). Live-verified end-to-end with Playwright + Chromium for every item: a real BTC-USD simulation's chart screenshotted showing a genuinely non-periodic, spike-and-drawdown curve; the content column measured at 1920px confirming the wider 1200px column; a real PTON (loss) simulation's chart confirming the split-baseline fill (accent above/muted below the invested line), a working hover crosshair + tooltip, a high marker, and — after the collision-guard fix — no more low-marker/endpoint-label crowding; The Proof screenshotted open, showing "In plain terms," the formula list, and a correctly-formatted "Jul 12, 2026, 6:38 AM UTC" Created timestamp; the scramble's new ~950ms duration spot-measured in a live browser (~780ms observed from an imprecise polling start-point, consistent with — not contradicting — the shipped 950ms value once hydration/measurement lag is accounted for).

**Technical Debt Introduced**: None judged unintentional. Item 6d (dividend-reinvestment tick marks) remains unimplemented — deferred, not abandoned, exactly as this pass's own instruction anticipated as an acceptable outcome when the underlying data isn't exposed by the API. The crosshair's fade-out asymmetry (instant unmount vs. an animated fade-in) is a disclosed, deliberate scope boundary, not a bug — documented in `.chart-crosshair`'s own CSS comment.

**Performance Notes**: No new dependencies added. The Growth Chart's two-Area split-fill doubles the SVG path elements Recharts renders for the fill layer (one now-mostly-empty `<path>` in the common case where a trajectory stays entirely above or below the baseline) — negligible at this chart's own already-decimated (~175 point) scale. The 55ms scramble tick throttle is a net reduction in re-renders compared to the previous every-frame (~16ms) update rate.

**Recruiter Value**: Medium-high. Demonstrates root-causing a "useless graph" complaint to its actual generating formula (not just re-styling the chart around bad data), a founder-review process where the founder's specific new instruction (Founder Decision 018.1) is properly recorded as a decision amendment rather than silently implemented, and a real example of iterating a collision-avoidance heuristic against live data until it actually held (single-dimension check → found insufficient live → combined two-dimension check).

**Production Readiness Score**: ~5/10 platform-wide, unchanged — this pass is fixture-data realism, UX polish, and content restructuring on already-shipped screens, not a change to calculation correctness, security posture, or deployment readiness.

**Next Milestone**: Founder review of this pass. M7 Phase 4 (Educational AI Experience) remains the next feature milestone, unaffected.

---
